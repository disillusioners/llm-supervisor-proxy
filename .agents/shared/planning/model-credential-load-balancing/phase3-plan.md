> **REVISED 2026-08-21 (reviewer pass — C1):** the `/v1/messages` Anthropic internal path is the **PRIMARY client endpoint** (Claude Code) and is added to scope. The verified call chain is `cmd/main.go:139` (route) → `pkg/proxy/handler_anthropic.go:348` (call into `doAnthropicInternalRequest`) → `handler_anthropic.go:455` (function def) → `handler_anthropic.go:478` (`NewInternalHandler` construction) → `pkg/proxy/internal_handler.go:109` (`HandleRequest` signature) → `internal_handler.go:111` (current 5-tuple `ResolveInternalConfig` seam) — **cites refreshed to 8f67bdf (Round 3); fea5874-era: 340/447/461 → 67/69**. The swap to `ResolveInternalConfigWithAffinity` is in this phase's scope.

# Phase 3: Handler & Runtime Wiring

> **AMENDED 2026-08-25 (Round 3c — Reviewer Findings):** reviewer verdict APPROVED-WITH-NOTES,
> gated on one mandatory pass (3 critical, 10 warnings, 6 suggestions — all pin-level; leader
> pre-ruled every decision point, including a RE-SCOPE of Round-3b ruling (iii)). Round 3 +
> Round 3b semantics STAND except where explicitly superseded below. This file's Round 3c items:
> **C1** (Task 18 — HOIST the credFailover pre-checks above the `:338` window gate; the Round-3b
> exemption-only form dead-loops in the core scenario), **C2** (Task 18 consumer logic follows the
> phase2-pinned unified mode mapping), **C3** (Task 18 — three enumerated sub-tasks for
> currently-referenced-but-nonexistent code: `spawnTriggerInfo.credentialID`, the `spawn()` switch
> case, and `newRaceCoordinatorWithEvents` engine+conversationKey params), **W1** (Task 22 — the
> passthrough branch produces NO `*ProviderError`; classify via `arc.lastStatusCode == 429` OR
> body `type` substring; new additive `arc.retryAfter`), **W2** (Task 17 — `ProviderError` gains
> `ErrorType`/`ErrorCode`), **W3** (Task 18 — SoonestExpiry single-shot flag), **W4** (Task 18 —
> ruling (iii) RE-SCOPED: Case 2 spawns unchanged; the eventual 429 is adjudicated by Case 1),
> **W6** (Task 24 — all FOUR LB'd paths in the acceptance), **S4** (Task 18 —
> `GetRequestStatuses` credFailover-row handling folded in). Per-item → task mapping table in the
> **Round 3c — Reviewer Findings** changelog at the end of this file.

## Objective
Wire the `pkg/credentiallb.Engine` (delivered by Phase 2) into the live request path so every internal-model chat completion picks one of the configured credentials via conversation-sticky weighted load balancing, while keeping single-credential models byte-identical to today. Concretely: extend the proxy/ultimate handler signatures (7th constructor param for the engine; no `Config.CredEngine` field per W-3), cache `rc.firstUserMessage` in `initRequestContext` and compute the token-salted `rc.conversationKey` at the post-auth wiring site (`handler.go:401+`, where `rc.tokenID` lands — A-1), swap the two `ResolveInternalConfig` call sites for `ResolveInternalConfigWithAffinity(modelID, conversationKey)`, construct + subscribe the engine in `cmd/main.go`, and emit `model_credential_selected` ONLY when the engine returns `newlyBound=true` (W-1 — first-binding signal, not per-request bool). The Anthropic `/v1/messages` internal path (`pkg/proxy/internal_handler.go:109/111` via `handler_anthropic.go:455/478`; cites refreshed to 8f67bdf) gets the same treatment — C1.

## Context
Phase 1 delivered the persistence layer (migration 028, `models.credentials_json`, `ModelConfig.Credentials []CredentialRef`, JSON scan in `scanModels`, extended `Validate`, in-use guard rewrite, engine publish on Add/Update/Remove/credential-delete).
Phase 2 delivered `pkg/credentiallb.Engine` with the exact contract in `technical-analysis.md §API Contract` (as amended): `NewEngine(ttl, sweepInterval, rngSeed)`, `GetOrSelect(modelID, conversationKey) (credentialID, newlyBound, err)` (W-1), `OnModelChanged` (filter-survivors, E-2), `OnCredentialDeleted`, `RebindFromStore`, `Stop`; plus `ComputeConversationKey(modelID, tokenID, firstUserMessage)` (A-1), `ExtractFirstUserMessage([]interface{})` (canonical-JSON multimodal, A-2), `ErrNoCredentials`. Phase 3 does NOT modify `pkg/credentiallb` internals — it only consumes them.

> **AMENDED 2026-08-21 (leader rulings):** this section is amended per architect council rulings A-1 (post-auth key wiring), A-2 (multimodal hashing — no fallback), W-1 (`GetOrSelect` returns `newlyBound`), W-2 (empty-key no binding), W-3 (constructor-only injection). The amended API Contract in `technical-analysis.md` (lines 1349-1373 changelog + §API Contract) is authoritative. This phase consumes the engine; it does not modify it. See "Amendment Changelog" at the end of this file for the ruling-by-ruling diff.

The contract names and call sites are authoritative (per amended contract):
- `pkg/proxy/handler.go:108` — `NewHandler` gains a 7th parameter `credEngine *credentiallb.Engine` (nil-safe). **No** `Config.CredEngine` field — W-3 removes it; constructor-only injection is the law.
- `pkg/proxy/handler_helpers.go:23-108` — `requestContext` gains BOTH `firstUserMessage string` (cached in `initRequestContext`) and `conversationKey string` (computed POST-AUTH at `handler.go:401+`); `reset()` clears both.
- `pkg/proxy/handler_functions.go:23-131` (cite refreshed to 8f67bdf — was 23-126 @ fea5874) — `initRequestContext` (at `handler.go:352`; was 353 @ fea5874) caches ONLY `rc.firstUserMessage = ExtractFirstUserMessage(rc.originalMessages)` (cheap map walk). It does NOT compute the key — `rc.tokenID` is unset at this line. The token-salted key is computed at the post-auth wiring site (see below).
- `pkg/proxy/handler.go:401+` — **NEW POST-AUTH wiring site (A-1, A-1-wiring)**: AFTER `rc.tokenID` is populated from `auth.AuthToken`, `rc.conversationKey = credentiallb.ComputeConversationKey(rc.resolvedModel.ID, rc.tokenID, rc.firstUserMessage)`. Anonymous/unauthenticated requests degrade to `rc.tokenID == ""` → unsalted key (graceful, not an error).
- `pkg/proxy/race_executor.go:137` — primary internal resolution → `cfg.ModelsConfig.ResolveInternalConfigWithAffinity(req.modelID, req.conversationKey)`. **#3 — REVISED 2026-08-21 (reviewer pass)**: the return shape is `(ResolvedCredential, bool)` — struct `ResolvedCredential{Provider, APIKey, BaseURL, InternalModel, CredentialID, NewlyBound}` plus trailing `ok bool` (per `technical-analysis.md §API Contract` lines 624-664). `NewlyBound` is the engine's W-1 first-binding signal; `CredentialID` is the engine-selector output (for `model_credential_selected` event payload + `#16f` prompt-cache observability).
- `pkg/proxy/race_executor.go:101-133` — secondary upstream path reuses the primary credential (per contract §D "secondary upstream model reuses primary model's credential" — no engine call for the secondary).
- `pkg/proxy/race_executor.go:279-300` — race-external `UpstreamCredentialID` path **UNCHANGED** (contract §D).
- **`pkg/proxy/handler_anthropic.go` + `pkg/proxy/internal_handler.go` (NEW 2026-08-21 — C1, Anthropic `/v1/messages` internal path)**: verified chain — `cmd/main.go:139` registers `HandleAnthropicMessages` → `handler_anthropic.go:348` calls `h.doAnthropicInternalRequest` → `handler_anthropic.go:455` (function def) → `handler_anthropic.go:478` constructs `NewInternalHandler(modelConfig, arc.conf.ModelsConfig)` → `pkg/proxy/internal_handler.go:109` (`HandleRequest` signature) → `internal_handler.go:111` (current 5-tuple `ResolveInternalConfig` seam to swap; all cites refreshed to 8f67bdf — fea5874-era: 340/447/461/67/69). The Anthropic path threads `conversationKey` via a NEW `anthropicRequestContext.conversationKey string` field (struct lives at `handler_anthropic.go:740`; was 697 @ fea5874); the post-auth wiring site for the Anthropic path is the equivalent of `handler.go:401+` for the chat-completions path — see Scope item #11 below for the exact site.
- `pkg/ultimatemodel/handler.go:625-635` — ultimate-external provider probe (cite refreshed to 8f67bdf — was 287-293 @ fea5874; **MAJOR +337 drift**: the D3 probe relocated into `Execute`) uses a backward-compat shim that reads `Credentials[0].CredentialID` instead of the removed `CredentialID`.
- `pkg/ultimatemodel/handler_internal.go:46` — ultimate-internal → `ResolveInternalConfigWithAffinity(modelCfg.ID, conversationKey)`.
- `cmd/main.go:108` — pass `modelsMgr.Engine()` (obtained from the store-side accessor; ModelsManager owns the engine — Dispatcher Ruling #2) as the 7th arg to `proxy.NewHandler`. Main.go does NOT construct the engine and does NOT call `RebindFromStore` — ModelsManager init (Phase 2) owns engine construction + rebind.
- **Engine signature (W-1)**: `GetOrSelect(modelID, conversationKey) (credentialID string, newlyBound bool, err error)`. `newlyBound=true` ⇔ a fresh binding was created for this call; `newlyBound=false` ⇔ an existing in-TTL binding was reused. This is the SOLE signal for `model_credential_selected` event publish.
- **Empty-key semantics (W-2 + C2)**: `conversationKey == ""` ⇒ NO binding stored, fresh weighted pick per request, engine returns `newlyBound=false`. The ""-as-own-bucket reading is REMOVED. The caller MUST NOT publish `model_credential_selected` for empty-key picks (no binding was stored → `newlyBound=false` → event NOT published). Phase 3 emits a DEBUG log at the post-auth wiring site for observability (the canonical C2 observability mechanism; see Scope #8).
- **Multimodal (A-2)**: `ExtractFirstUserMessage` returns the canonical content (string OR canonical JSON of `[]interface{}` array — precedent `hash_cache.go:172-186`). ALL requests get affinity; no fallback.

## Scope

### In Scope
1. **Modify** `pkg/proxy/handler.go`: `Config` (no new field — engine is constructor-injected), `Handler` struct gains `credEngine *credentiallb.Engine`, `NewHandler` signature gains 7th param.
2. **Modify** `pkg/proxy/handler_helpers.go`: add `conversationKey string` to `requestContext`; clear it in `reset()`.
3. **Modify** `pkg/proxy/handler_functions.go` (initRequestContext, `handler.go:352` — cite refreshed to 8f67bdf):
   - In `initRequestContext`, AFTER `rc.resolvedModel` and `rc.originalMessages` are populated, cache ONLY `rc.firstUserMessage = credentiallb.ExtractFirstUserMessage(rc.originalMessages)` (cheap map walk). The token-salted key is **NOT** computed here — `rc.tokenID` is unset at this line.
   - **NEW post-auth wiring site** (see File changes for `handler.go:401+`): `rc.conversationKey = credentiallb.ComputeConversationKey(rc.resolvedModel.ID, rc.tokenID, rc.firstUserMessage)` runs AFTER `rc.tokenID` is populated by auth. Anonymous/unauthenticated requests pass `tokenID=""` → unsalted key (A-1 graceful degradation).
   - If `rc.resolvedModel` is nil (external/unknown model), both `firstUserMessage` and `conversationKey` stay `""` and the engine path is skipped at every call site.
4. **Modify** `pkg/proxy/race_executor.go`:
   - Replace `cfg.ModelsConfig.ResolveInternalConfig(req.modelID)` at line 137 with `cfg.ModelsConfig.ResolveInternalConfigWithAffinity(req.modelID, req.conversationKey)`.
   - **W-1 signature propagation (REVISED 2026-08-21 — reviewer pass #3, struct form):** `ResolveInternalConfigWithAffinity` returns **`(ResolvedCredential, bool)`** — struct `ResolvedCredential{Provider, APIKey, BaseURL, InternalModel, CredentialID, NewlyBound}` + trailing `ok`. The race coordinator reads `resolved.NewlyBound` and publishes `model_credential_selected` ONLY when it is `true` (single source of truth; per the contract, only the proxy emits this event; per C2, empty-key picks return `NewlyBound=false` → no event).
   - Update the secondary upstream path (lines 101-133): keep using the primary `ResolveInternalConfigWithAffinity(req.modelID, req.conversationKey)` — secondary model name uses the primary credential (no per-credential LB on the secondary path; the contract says "secondary upstream model reuses primary model's credential"). The secondary path inherits `newlyBound` from the primary call (do NOT call the engine a second time).
   - `executeExternalRequest` (lines 216-300) **UNCHANGED** — env-only path.
5. **Modify** `pkg/ultimatemodel/handler.go`:
   - Lines 625-635 (cite refreshed to 8f67bdf; was 287-293): replace `if modelCfg.CredentialID != ""` with a `getEffectivePrimaryCredentialID(modelCfg)` helper that returns `modelCfg.Credentials[0].CredentialID` when `len(modelCfg.Credentials) > 0`, else `""` (preserves the `modelCfg.CredentialID != ""` boolean semantics for ultimate-external provider detection only).
6. **Modify** `pkg/ultimatemodel/handler_internal.go`:
   - Line 46: replace `h.modelsMgr.ResolveInternalConfig(modelCfg.ID)` with `h.modelsMgr.ResolveInternalConfigWithAffinity(modelCfg.ID, conversationKey)`.
   - **Add** the `conversationKey` parameter to `Handler.executeInternal(...)` and `Handler.Execute(...)` so the call site can thread it down from `pkg/proxy/handler.go:635` (cite refreshed to 8f67bdf — was 627 @ fea5874) (where `rc.conversationKey` is now available post-auth). This crosses the package boundary, mirroring how `interleaved` is already threaded (see `handler_internal.go:29-44` doc comment).
   - **W-1 signature propagation (REVISED 2026-08-21 — reviewer pass #3, struct form):** `ResolveInternalConfigWithAffinity` returns `(ResolvedCredential, bool)` — struct `ResolvedCredential{Provider, APIKey, BaseURL, InternalModel, CredentialID, NewlyBound}` + trailing `ok`. Callers read `resolved.NewlyBound`; no side-channel, no extra return value. The ultimate-internal call site does NOT publish `model_credential_selected` itself; the publish happens in `pkg/proxy/race_executor.go:executeRequest` only, gated on `newlyBound=true` (single source of truth for event emission).
7. **Modify** `cmd/main.go`:
   - Do NOT construct an engine — obtain it: `credLB := modelsMgr.Engine()` (**REVISED 2026-08-21 — reviewer pass #7**; Dispatcher Ruling #2: `ModelsManager` owns the engine and rebinds at init — a second engine would split-brain affinity). Grep-guards: `credentiallb.NewEngine` and `RebindFromStore` must return ZERO hits in `cmd/main.go`.
   - Subscribe `bus` to `credentiallb.EventCredentialsChanged` (typed `Data: {model_id}`) and call `Engine.OnModelChanged(modelID, refs)` — registered only if `credLB != nil` (nil-safe). The publish side already runs in `ModelsManager.AddModel/UpdateModel/RemoveModel` from Phase 1; Phase 3 only consumes it.
   - Pass `credLB` as 7th arg to `proxy.NewHandler(proxyConfig, bus, reqStore, bufferStore, tokenStore, usageCounter, credLB)` (line 108).
   - Shutdown order (#11): register `defer dbStore.Close()` first (existing), then `defer credLB.Stop()` before the shutdown signal handler — LIFO fires `srv.Shutdown(ctx)` → `credLB.Stop()` → `dbStore.Close()`.
   - On shutdown: call `credLB.Stop()` before `srv.Shutdown` completes (so the janitor exits cleanly). Wire it in the same `defer` block that calls `dbStore.Close()`.
8. **Modify** `pkg/proxy/handler.go` and `pkg/proxy/race_executor.go` (additive logging):
   - In `executeInternalRequest` line ~151 (the existing `[DEBUG] Race attempt …` log), add the engine-selected credential ID: `[DEBUG] Race attempt %d calling internal provider: %s (model=%s, baseURL=%s, credential_id=%s)`. Read from `req.selectedCredentialID` (set in `executeRequest`).
   - In `initRequestContext`, log at DEBUG after caching firstUserMessage: `[LB] cached firstUserMessage modelID=%s len(firstUserMessage)=%d` (does NOT log the hash — the hash is computed post-auth and logged at the post-auth site to avoid double-logging).
   - **NEW** at the post-auth wiring site (`handler.go:401+`): log at DEBUG `[LB] computed conversationKey hash=%s modelID=%s tokenID=%s len(firstUserMessage)=%d` — tokenID rendered as `"<empty>"` when `rc.tokenID == ""` (anonymous).
   - **W-2** when `rc.conversationKey == ""` (no first user message extracted): log at DEBUG `[LB] empty conversationKey for modelID=%s; engine will pick fresh per request (no binding stored)`. Do NOT crash; do NOT skip the engine call. Logged ONCE per request, after the post-auth wiring site.
   - When the engine returns `ErrNoCredentials`, the existing `fmt.Errorf("failed to resolve internal config for model %s", req.modelID)` stays; we only add `[LB] %s has empty Credentials; legacy fallback returned ok=false` at DEBUG (covers the `Credentials == nil || len == 0` fast path).
9. **Modify** `pkg/proxy/handler.go` (`publishEvent` calls) — emit `model_credential_selected` event ONLY when the engine returns `newlyBound=true` (W-1). Per-request bool `bindingEmitted` is **REMOVED** — it was unimplementable as a first-binding detector (it only detected first-selection-within-a-request, not first-binding-ever). The engine's `newlyBound` return is the sole signal. Publish payload: `{model_id, credential_id, conversation_key_prefix (first 8 chars)}`. Mirrors the SSE feed shape used by `race_secondary_model_used` (line 121). Place the publish call in `executeRequest` (which has access to `cfg.EventBus`), immediately after the engine call returns and ONLY if `newlyBound == true`. Empty `conversationKey` (W-2) requests STILL trigger the event on `newlyBound=true` (they pick fresh; from the caller's perspective, that is a "first selection").
10. **Modify** test fixtures to support new constructor signatures:
    - `pkg/proxy/handler_test.go` — call sites of `NewHandler` (pass `nil` for `credEngine` to keep tests single-credential).
    - `pkg/proxy/handler_integration_test.go` — same (Phase 5 adds the multi-cred test).
    - `pkg/proxy/handler_anthropic_test.go` — **NEW (C1)**: any test that drives the `/v1/messages` route path passes `nil` for `credEngine` (single-credential fast path is unchanged); tests that wire the engine follow the same `nil`/non-nil convention as the chat-completions tests.
    - `pkg/ultimatemodel/handler_test.go` — `mockModelsConfig` gains `ResolveInternalConfigWithAffinity` returning `(ResolvedCredential{Provider, APIKey, BaseURL, InternalModel, CredentialID, NewlyBound}, bool)` per #3 — mocks return the struct with `NewlyBound` settable per-test via a helper.
    - `pkg/ultimatemodel/handler_internal_test.go` — same.
    - All other test files that construct `proxy.NewHandler`, `ultimatemodel.NewHandler`, or `proxy.NewInternalHandler` (grep for `NewHandler(`, `ultimatemodel.NewHandler(`, `NewInternalHandler(`).
11. **NEW 2026-08-21 — C1 (reviewer pass):** Modify `pkg/proxy/handler_anthropic.go` + `pkg/proxy/internal_handler.go` to wire the engine on the Anthropic `/v1/messages` internal path (the PRIMARY client endpoint for Claude Code).
    - Add `conversationKey string` to `anthropicRequestContext` (the struct lives at `handler_anthropic.go:740`; cite refreshed to 8f67bdf — was 697 @ fea5874).
    - Compute the Anthropic-path `conversationKey` AFTER the Anthropic request is authenticated. The Anthropic path's auth equivalent of `handler.go:401+` is the auth-resolution site within `HandleAnthropicMessages` (the handler that dispatches from `cmd/main.go:139`); find where `arc.authToken` / `arc.tokenID` is populated and compute `arc.conversationKey = credentiallb.ComputeConversationKey(arc.modelConfig.ID, tokenID, firstUserMessage)` immediately AFTER, mirroring the chat-completions post-auth wiring site. Cache `arc.firstUserMessage` in the Anthropic-path equivalent of `initRequestContext` (cheap map walk on the parsed Anthropic messages array). Anonymous Anthropic requests degrade to `tokenID=""` → unsalted key (A-1 graceful).
    - Extend `NewInternalHandler` (`pkg/proxy/internal_handler.go:48`) and `HandleRequest` (`pkg/proxy/internal_handler.go:109`) to take an extra `conversationKey string` parameter (cites refreshed to 8f67bdf — was 38/67 @ fea5874).
    - At `pkg/proxy/internal_handler.go:111`, swap `h.resolver.ResolveInternalConfig(h.config.ID)` for `h.resolver.ResolveInternalConfigWithAffinity(h.config.ID, conversationKey)`. The 5-tuple return is replaced by `(ResolvedCredential, bool)` per #3.
    - At `pkg/proxy/handler_anthropic.go:478` (`NewInternalHandler` construction), pass `arc.conversationKey` through to the new arg.
    - The Anthropic path does NOT publish `model_credential_selected` itself (single source of truth in `pkg/proxy/race_executor.go:executeRequest` — the engine's `NewlyBound` field flows through `ResolveInternalConfigWithAffinity` only where the race-executor can publish; the Anthropic path passes through the resolved credential to the upstream call without re-publishing). If the Anthropic path needs event visibility, route the `NewlyBound` flag through `anthropicRequestContext` and publish from the call site that already has `cfg.EventBus` (mirrors `race_secondary_model_used` at `race_executor.go:121-129`).
    - **C1 grep-guard**: after the swap, `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` MUST return ZERO matches in this file. Only `ResolveInternalConfigWithAffinity` remains. `grep -n "ResolveInternalConfig(" pkg/proxy/handler_anthropic.go` MUST return ZERO matches in the `/v1/messages` internal path (the Anthropic file uses `HandleAnthropicMessages` + `doAnthropicInternalRequest`, not the legacy `ResolveInternalConfig` directly).

### Out of Scope
- **NOT modifying** `pkg/credentiallb` internals. The package is delivered by Phase 2 and frozen for this phase.
- **NOT modifying** the race-external path (`race_executor.go:216-300`) — env-only, per contract §D.
- **NOT modifying** the `secondary upstream model` selection logic — the credential is still resolved from the primary `req.modelID`; only the secondary `internalModel` name comes from `modelConfig.SecondaryUpstreamModel`. The engine call happens once for the primary path.
- **NOT implementing** cross-instance affinity (Redis-backed) — deferred to v2 per `decisions.md §C "Restart Stickiness"`.
- **NOT publishing** `model_credential_binding_dropped` events — those come from inside the engine (Phase 2) which the contract says is decoupled from `events.Bus`; Phase 3 only wires the consumer side for `model.credentials.changed`.
- **NOT changing** the existing `ModelsConfigInterface.ResolveInternalConfig` signature (legacy single-credential). It remains as the inner fallback for the `Credentials == nil || len == 0` fast path inside `ModelsManager.ResolveInternalConfigWithAffinity`.

## Files

### Create
- **None.** All work is additive edits to existing files.

### Modify
| File | Lines | What changes |
|---|---|---|
| `pkg/proxy/handler.go` | 97-143 | `Handler` struct adds `credEngine *credentiallb.Engine`; `NewHandler` signature adds 7th param; thread `credEngine` to the `ultimatemodel.NewHandler` call so `handler_internal.go:46` can see it (see #11 below). **W-3**: `Config` struct is UNCHANGED — no `CredEngine` field. |
| `pkg/proxy/handler.go` | 830 (**NEW — Round 3c C3 sub-task 3**) | The `newRaceCoordinatorWithEvents` call site passes the two NEW constructor args: the engine (`h.credEngine`) and the request's conversation key (`rc.conversationKey`) — wiring `cmd/main.go` → `NewHandler` → coordinator without any `Config.CredEngine` (constructor-only injection per W-3 holds end-to-end). |
| `pkg/proxy/handler.go` | 145-153 | `publishEvent` already exists; no change. |
| `pkg/proxy/handler.go` | **NEW: post-auth wiring site at handler.go:401+** | After `rc.tokenID` is set from `auth.AuthToken`, compute `rc.conversationKey = credentiallb.ComputeConversationKey(rc.resolvedModel.ID, rc.tokenID, rc.firstUserMessage)`. Anonymous requests pass `tokenID=""` → unsalted key (graceful). Log at DEBUG (see Scope #8). |
| `pkg/proxy/handler.go` | (in `executeRequest` around the call to `executeInternalRequest`) | After the engine returns and ONLY if the resolved `ResolvedCredential.NewlyBound == true`, publish `model_credential_selected` event (W-1 — see #9). The `NewlyBound` flag propagates from `engine.GetOrSelect` → `ResolveInternalConfigWithAffinity` (via the `ResolvedCredential.NewlyBound` struct field per #3) → here. The caller reads `req.resolved.NewlyBound` (struct field), NOT a 6th tuple element. |
| `pkg/proxy/handler_helpers.go` | 23-108 | Add BOTH `firstUserMessage string` (cached in `initRequestContext`) AND `conversationKey string` (computed post-auth) to `requestContext`. |
| `pkg/proxy/handler_helpers.go` | 113-132 | `reset()` adds `rc.firstUserMessage = ""` and `rc.conversationKey = ""`. |
| `pkg/proxy/handler_functions.go` | 23-131 (refreshed to 8f67bdf; was 23-126) | In `initRequestContext`, after `rc.originalMessages` and `rc.resolvedModel` are set, cache ONLY `rc.firstUserMessage = credentiallb.ExtractFirstUserMessage(rc.originalMessages)`. The token-salted key is computed at the post-auth wiring site (`handler.go:401+`), NOT here. Add `credentiallb` import. |
| `pkg/proxy/race_executor.go` | 100-138 | Replace the two `cfg.ModelsConfig.ResolveInternalConfig(req.modelID)` calls (lines 112 and 137) with `cfg.ModelsConfig.ResolveInternalConfigWithAffinity(req.modelID, req.conversationKey)`. Thread `conversationKey` AND `newlyBound` through `upstreamRequest` (new fields) and `executeRequest` (line 73). **#3 — REVISED**: the return is `(ResolvedCredential, bool)`, NOT a 6-tuple. `req.resolved` is the struct (with `.NewlyBound` field), `req.resolvedOK` is the trailing bool. **C2**: when `conversationKey == ""`, the engine returns `NewlyBound=false` (no binding stored) — caller MUST NOT publish `model_credential_selected` for empty-key picks. |
| `pkg/proxy/race_executor.go` | 151 | Augment debug log with `credential_id=%s`. |
| `pkg/proxy/race_executor.go` | 73-91 (`executeRequest`) | Read `req.resolved.NewlyBound` (struct field per #3) and publish `model_credential_selected` ONLY when `req.resolved.NewlyBound == true` AND `req.resolvedOK == true` (W-1 + C2). Remove any per-`upstreamRequest` bool `bindingEmitted` — it was unimplementable as a first-binding detector. **C2**: empty-key requests emit ZERO events (engine returns `NewlyBound=false` because no binding was stored). |
| `pkg/ultimatemodel/handler.go` | 625-635 | Add `getEffectivePrimaryCredentialID(modelCfg *models.ModelConfig) string` helper (free function); replace the `modelCfg.CredentialID != ""` check with the helper. |
| `pkg/ultimatemodel/handler.go` | 229-238 (`Execute`) | Add `conversationKey string` parameter; pass it through to `executeInternal` and `executeExternal`. |
| `pkg/ultimatemodel/handler_internal.go` | 36-49 (`executeInternal`) | Replace `h.modelsMgr.ResolveInternalConfig(modelCfg.ID)` with `h.modelsMgr.ResolveInternalConfigWithAffinity(modelCfg.ID, conversationKey)`. **#3 — REVISED**: the return is `(ResolvedCredential, bool)` — discard `NewlyBound` (event publish lives in `pkg/proxy/race_executor.go:executeRequest`, single source of truth). Add `conversationKey` parameter. |
| `pkg/ultimatemodel/handler_internal.go` | (interface change) | `Handler.executeInternal` signature now takes `conversationKey string`; thread through `Execute` from `handler.go:635`. |
| **`pkg/proxy/internal_handler.go`** (NEW 2026-08-21 — C1) | 48, 109, 111 (cites refreshed to 8f67bdf; was 38, 67, 69 @ fea5874) | `NewInternalHandler(config, resolver)` (line 38) gains no new field at struct level — but `HandleRequest(ctx, requestBody, w, isStream)` (line 67) gains a new `conversationKey string` parameter; the resolver seam at line 69 swaps from `h.resolver.ResolveInternalConfig(h.config.ID)` (5-tuple) to `h.resolver.ResolveInternalConfigWithAffinity(h.config.ID, conversationKey)` (struct + trailing `ok bool` per #3). |
| **`pkg/proxy/handler_anthropic.go`** (NEW 2026-08-21 — C1) | 740, 478, 455 (cites refreshed to 8f67bdf; was 697, 461, 447 @ fea5874) | Add `conversationKey string` field to `anthropicRequestContext` (struct at line 697); populate it at the Anthropic-path post-auth wiring site (the equivalent of `handler.go:401+`); pass it through `NewInternalHandler` at line 461 and into `HandleRequest`. Also cache `firstUserMessage` in the Anthropic-path equivalent of `initRequestContext`. The `firstUserMessage` extraction here uses the Anthropic-shape `messages[]` (the field walker is in `pkg/proxy/handler_anthropic.go` — adapt `credentiallb.ExtractFirstUserMessage` if the wire shape differs from OpenAI's chat-completions format). |
| `pkg/proxy/handler.go` | 635 | Call `h.ultimateHandler.Execute(r.Context(), w, r, rc.requestBody, rc.reqLog.Model, result.Hash, &rc.headersSent, &ultimateModelID, rc.conversationKey)`. |
| `cmd/main.go` | 47-107 (amended — #7; **Round 3c C3 sub-task 3 note**) | **DELETE the `credLB := credentiallb.NewEngine(...)` constructor and the rebind loop** per Dispatcher Ruling #2 (ModelsManager owns the engine; main.go only OBTAINS it). Replace with: `credLB := modelsMgr.Engine()` (single line; ModelsManager init in Phase 2 builds the engine and runs `RebindFromStore` for every enabled internal model). Pass `credLB` as the 7th arg to `proxy.NewHandler` at line 108. **Round 3c C3**: the engine reaches the race coordinator THROUGH this path — `main.go` → `NewHandler(…, credLB)` → `Handler.credEngine` → `newRaceCoordinatorWithEvents(engine, conversationKey, …)` at `handler.go:830`; main.go does NOT construct or reference the coordinator directly (no `newRaceCoordinatorWithEvents` hits in `cmd/main.go`). |
| `cmd/main.go` | 199-215 (shutdown — amended — #11) | **Shutdown sequence**: `srv.Shutdown(ctx)` FIRST (drains HTTP connections), THEN `credLB.Stop()` (stops engine janitor), THEN `dbStore.Close()` (closes DB). The defer for `credLB.Stop()` is registered BEFORE the defer for `dbStore.Close()` so LIFO fires them in the right order. Add an explicit `if credLB != nil { credLB.Stop() }` guard (nil-safe per W-3). |
| `pkg/proxy/handler_test.go` | (multiple call sites) | Pass `nil` as 7th arg in every `NewHandler` invocation. |
| `pkg/proxy/handler_integration_test.go` | 179, 338 | Pass `nil` as 7th arg. |
| `pkg/proxy/handler_anthropic_test.go` (NEW — C1) | (call sites) | Pass `nil` as 7th arg in every `NewHandler` invocation; pass `""` as `conversationKey` to `NewInternalHandler` for single-credential fast-path tests. |
| `pkg/ultimatemodel/handler_test.go` | 169, 196 | **#3 — REVISED + #8**: `mockModelsConfig` adds `ResolveInternalConfigWithAffinity` returning `(ResolvedCredential, bool)`. **Post-Phase-1 the `ModelConfig.CredentialID` field is GONE** — mocks/fixtures seed `Credentials: []models.CredentialRef{...}` using Phase 1's `TestRefs` fixture factory (e.g., `models.TestRefs(creds...)` — referenced by package; Phase 1 pins the factory signature in its plan). The new method returns the struct with `NewlyBound` settable per-test via a helper (e.g., `mockModelsConfig.SetNewlyBoundForTest(modelID, bool)`). |
| `pkg/ultimatemodel/handler_internal_test.go` | (mock usage) | Same — `mockModelsConfig.ResolveInternalConfigWithAffinity` returns the struct; `NewlyBound` settable per-test. |
| `pkg/proxy/handler_internal_test.go` (if exists) | (call sites) | Pass `nil`. |
| **`pkg/providers/interface.go`** (NEW 2026-08-25 — Round 3, R3-1) | ~168 (`ProviderError` methods region) | Add `RetryAfter time.Duration` field to `ProviderError`; add package-level `func IsRateLimitError(err error) bool` (HTTP 429 or rate-limit error-body code/type; OpenAI-compatible `{"error":{message,type,code}}` covers MiniMax — `models.ErrorTypeRateLimit`/`ErrorCodeRateLimit` at `models/errors.go:7,17` are the canonical strings). |
| **`pkg/providers/openai.go`** (NEW 2026-08-25 — Round 3, R3-1) | 217-268 (error construction), 592-606 (`parseRetryAfter` — currently DEAD CODE, zero callers) | Wire the dead `parseRetryAfter` (openai.go:593): on the 429 path (~255), parse the `Retry-After` response header into `ProviderError.RetryAfter`; classify rate-limit error-body code/type variants into the retryable+rate-limit bucket consumed by `IsRateLimitError`. |
| **`pkg/proxy/race_request.go`** (NEW 2026-08-25 — Round 3, R3-3) | 14-16 (modelType constants) | Add `modelTypeCredFailover upstreamModelType = "cred_failover"` alongside `modelTypeMain/Second/Fallback`. |
| **`pkg/proxy/race_coordinator.go`** (NEW 2026-08-25 — Round 3, R3-3; **AMENDED 2026-08-25 Round 3b — B1 + ruling (iii); AMENDED 2026-08-25 Round 3c — C1 hoisted gate + C3 wiring + W4 re-scope**) | `:79` (`spawnTriggerInfo` struct — **Round 3c C3**: gains `credentialID string`; today it carries trigger/errorMessage/failedRequest only), `:129` (`newRaceCoordinatorWithEvents` constructor — **Round 3c C3**: gains `engine` + `conversationKey` params; 8 params today, neither present; call site `handler.go:830`), `:189` (`spawn()` function — the Round-3 text's `spawnAttempt` is **PURGED**; **Round 3c C3**: switch at `~:196-218` gains `case modelTypeCredFailover: modelID = c.models[0]` — main/second/fallback only today; absent case ⇒ `modelID=""` silent misfire), `:252` (`manage()` function — the Round-3 text's `monitorLoop` is **PURGED**), `:338` (spawn-window check `c.winner == nil && len(c.requests) < len(c.models)` — **Round 3c C1**: the credFailover pre-checks are HOISTED ABOVE this gate; admission condition verbatim `gate := modelAttempts < len(models) \|\| credFailoverEligibleWithBudget()`; `modelTypeCredFailover` rows EXEMPT per B1 separate accounting; the window counts ONLY `modelType ∈ {main, second, fallback}`), `:350-364` (Case 1 pre-spawn branch — NEW pre-spawn check: internal model + `len(modelConfig.Credentials) > 1` + `IsRateLimitError(latestReq.GetError())` + retry budget remaining ⇒ `engine.ExcludeAndReselect(...)` Round-3b signature with `ReselectMode` return + spawn a **same-model** attempt with model type `modelTypeCredFailover` (canonical Go identifier; purges `modelTypeCredentialFailover`) INSTEAD of model fallback; **Round 3c W3**: per-request `soonestExpiryAttempted` flag + `continue` enforces SoonestExpiry single-shot), `:364-395` (Case-2 idle path — **Round 3c W4 RE-SCOPE of ruling (iii)**: spawns second/fallback UNCHANGED (at idle-spawn time NO error exists to classify); the stalled attempt's eventual 429 is adjudicated by Case 1, now reachable via the C1-hoisted gate), `:399-405` (Case-2 spawn loop for `modelTypeSecond` + `modelTypeFallback`), `:420-421` (all-failed check `len(c.requests) >= len(c.models)` — `modelTypeCredFailover` rows EXEMPT per B1 separate accounting; all-failed condition = "all fallback models tried OR credential budget exhausted with no candidate remaining"; terminal `:860-868` MUST NOT fire while an untried fallback model or credential remains) | NEW pre-spawn branch in Case 1: `credID, mode := engine.ExcludeAndReselect(modelID, conversationKey, failedCredID, retryAfter)` — `mode == ReselectHealthy` ⇒ spawn same-model `modelTypeCredFailover` carrying credential override INSTEAD of model fallback; `mode == ReselectSoonestExpiry` ⇒ spawn same-model ONCE with soonest-expiring credential override (single-attempt; on re-429 fall through to model-fallback); `mode == ReselectNone` ⇒ fall through to existing `modelTypeFallback` unchanged. `requestContext.triedCredentialIDs`/`failoverAttempts` mutated ONLY from `manage()` under `c.mu` (mandatory code comment at field declarations + every mutation site). `spawn()` extended to accept a `credentialOverride` field on `upstreamRequest` so `modelTypeCredFailover` rows resolve the SPECIFIC reselected credential (not a fresh `GetOrSelect`); Budget exhausted or no healthy credential ⇒ existing `modelTypeFallback` path unchanged (precedence is per-attempt, never skips the fallback entirely). |
| **`pkg/proxy/handler_helpers.go`** (NEW 2026-08-25 — Round 3, R3-5; **AMENDED 2026-08-25 Round 3b — mutation-discipline comment**) | 23-108 (`requestContext` struct; cite exact @ 8f67bdf) | Add per-request failover accounting: `triedCredentialIDs map[string]bool` (or slice) + `failoverAttempts int` — **request context, NOT engine** (R3-5). Cleared in `reset()`. **Round 3b — mandatory code comment** at both field declarations: `// mutated only from manage() under c.mu — race-free by construction` (B1 review #4 pin). |
| **`pkg/proxy/race_executor.go`** (NEW 2026-08-25 — Round 3, R3-2/R3-3; **AMENDED 2026-08-25 Round 3b — `streamResult` call/def disambiguation**) | 100-138 (resolution seam — cites exact @ 8f67bdf), 365 (non-stream provider call), 436 (stream provider call); **Round 3b cite disambiguation**: `streamResult` **call** site at `:852` (post-`WaitForWinner`) / **definition** at `:960`; `handleNonStreamResult` **call** site at `:879` / **definition** at `:1327` | Thread the credential override + budget fields through `upstreamRequest`/`executeRequest` so a `modelTypeCredFailover` attempt resolves the SPECIFIC reselected credential (not a fresh `GetOrSelect`); provider error path returns the raw `ProviderError` up to the coordinator (already does — ensure `RetryAfter` survives wrapping). |
| **`pkg/ultimatemodel/handler_internal.go`** (NEW 2026-08-25 — Round 3, R3-3; **AMENDED 2026-08-25 Round 3b — review #4 guard fix**) | 36-49 (`executeInternal`; cites exact @ 8f67bdf) | Equivalent hook: on rate-limited provider error from the internal execution (pre-first-byte — initial-call `*ProviderError` type assertion per Round 3b review #4; `headersSent` is NOT a usable marker because `*headersSent = true` is pre-set at `ultimatemodel/handler.go:608-609` and SSE heartbeat starts at `:643` BEFORE `executeInternal` at `:649-655`), call `engine.ExcludeAndReselect(...)` (Round-3b signature with `ReselectMode` return, phase2 Task 12) and retry ONCE with the reselected credential when budget allows (`ReselectHealthy` / `ReselectSoonestExpiry`); `ReselectNone` ⇒ propagate; else propagate the existing failure shape. Must NOT conflict with the thinking-sink invariant (`internal_handler.go:72-80` pattern) or the bare-JSON capture fallback. |
| **`pkg/proxy/internal_handler.go` + `handler_anthropic.go`** (NEW 2026-08-25 — Round 3, R3-3; **AMENDED 2026-08-25 Round 3b — review #4 cite fix + leader ruling (i)**) | 109-111 (`HandleRequest` / `ResolveInternalConfig` seam @ 8f67bdf), `handler_anthropic.go:348/455/478` (Anthropic chain — internal-handler construction at `:478`), **Round 3b cite fix**: the `/v1/messages` first-byte header write is at **`handler_anthropic.go:648-654`** (NOT `adapter_anthropic.go:170-176` which is `SetStreamHeaders`); **Round 3b leader ruling (i)**: the **`handler_anthropic.go:297-345` anthropic-passthrough internal branch** (internal model + anthropic-provider credential → `doAnthropicRequest` passthrough) joins the failover family — swap single-cred read (`modelConfig.CredentialID` / `GetCredential` / `ResolveAPIKey` / `arc.credentialAPIKey`) to `ResolveInternalConfigWithAffinity(modelConfig.ID, conversationKey)` (per #3) + add the rate-limit hook (**Round 3c W1**: classification = `arc.lastStatusCode == 429` OR body `type` substring `rate_limit` — the branch produces NO `*ProviderError`; Retry-After via NEW additive `arc.retryAfter time.Duration` captured where `arc.lastStatusCode` is set at `:423`; pre-write guard: `arc.headersSent` at `:588`/`:654`/`:1065`); passthrough transport via `doAnthropicRequest` is **UNCHANGED** | `/v1/messages` internal hook: same rate-limit → `ExcludeAndReselect` → single retry pattern as ultimate-internal, implemented at the `InternalHandler` seam so both the race-internal reuse and the Anthropic path inherit it; guarded by the same first-byte constraint. **Anthropic-passthrough internal branch (ruling (i))**: `grep -n "ResolveInternalConfigWithAffinity" pkg/proxy/handler_anthropic.go` returns ≥2 sites (internal-handler construction + the new passthrough-branch site at `:297-345`). |

## Tasks

> **AMENDED 2026-08-21**: Task rows updated in place to reflect A-1 (post-auth wiring), A-2 (multimodal hashing — no fallback), W-1 (3-return `GetOrSelect`), W-2 (empty-key no binding), W-3 (constructor-only injection), C1 (`/v1/messages` internal path), C2 (empty-key `NewlyBound=false`), #3 (struct `ResolvedCredential` + trailing `ok` — NOT a 6-tuple), #7 (main.go only OBTAINS the engine from `modelsMgr.Engine()` — ModelsManager owns engine construction + rebind), #8 (mock guidance uses Phase 1 `TestRefs` fixture; `CredentialID` field is GONE post-Phase-1), #11 (shutdown order `srv.Shutdown` → `credLB.Stop` → `dbStore.Close`). Task table structure is preserved; only task text changed where the contract changed.

| # | Task | Depends On | Acceptance |
|---|---|---|---|
| 1 | Add `firstUserMessage string` AND `conversationKey string` to `requestContext` (`pkg/proxy/handler_helpers.go:23-108`); clear both in `reset()`. | None | Both fields visible in struct; reset zeroes both; `go build ./...` passes. |
| 2 | In `initRequestContext` (`pkg/proxy/handler_functions.go:23-131`; cite refreshed to 8f67bdf — was 23-126 @ fea5874), AFTER `rc.resolvedModel` and `rc.originalMessages` are populated, cache ONLY `rc.firstUserMessage = credentiallb.ExtractFirstUserMessage(rc.originalMessages)` (cheap map walk; multimodal `[]interface{}` content is canonical-JSON hashed per A-2 — no fallback). **Do NOT compute `rc.conversationKey` here** — `rc.tokenID` is unset at this line. Import `pkg/credentiallb`. | Task 1 | `rc.firstUserMessage` is set when `resolvedModel != nil && resolvedModel.Internal`; multimodal `[]interface{}` content produces a deterministic canonical-JSON hash (precedent `hash_cache.go:172-186`); `""` for external/no-content. `go vet ./pkg/proxy/...` clean. |
| 2a | **NEW (A-1 post-auth wiring)** In `pkg/proxy/handler.go:401+`, AFTER `rc.tokenID` is set from `auth.AuthToken`, compute `rc.conversationKey = credentiallb.ComputeConversationKey(rc.resolvedModel.ID, rc.tokenID, rc.firstUserMessage)`. Anonymous requests (`tokenID == ""`) degrade to unsalted key. | Task 2 | `rc.conversationKey == ComputeConversationKey(modelID, tokenID, firstUserMessage)` for internal models; `""` for external/unknown. The key is NEVER computed in `initRequestContext`. |
| 3 | Add `credEngine *credentiallb.Engine` to `Handler` struct (the unexported field name `credEngine` per #16a — convention stated once, applies throughout); extend `NewHandler` signature with the 7th param `credEngine *credentiallb.Engine`. **W-3**: `Config` struct is unchanged — NO `Config.CredEngine` field (constructor-only injection is the law). | None | Compile-clean signature; every existing call site updated to pass `nil`. `Config` struct compiles clean. |
| 4 | In `executeInternalRequest` (`pkg/proxy/race_executor.go:100-138`), replace `ResolveInternalConfig(req.modelID)` with `ResolveInternalConfigWithAffinity(req.modelID, req.conversationKey)` at both call sites (secondary path line 112 and primary fallback line 137). Thread `conversationKey` AND `req.resolved` (the `ResolvedCredential` struct) through `upstreamRequest` and `executeRequest` (line 73). **#3 — REVISED**: `ResolveInternalConfigWithAffinity` returns `(ResolvedCredential, bool)` — NOT a 6-tuple. `req.resolved` carries `Provider / APIKey / BaseURL / InternalModel / CredentialID / NewlyBound`; `req.resolvedOK` is the trailing bool. The race coordinator reads `req.resolved.NewlyBound` (struct field). **C2 — REVISED**: when `conversationKey == ""`, the engine returns `NewlyBound=false` (no binding was stored) → caller does NOT publish `model_credential_selected` for empty-key picks. | Tasks 2, 2a, 3 | Compile-clean; `cfg.ModelsConfig.ResolveInternalConfigWithAffinity` is the only call inside `executeInternalRequest` (legacy `ResolveInternalConfig` is no longer called from this file). `req.resolved` and `req.resolvedOK` propagate correctly. |
| 5 | Augment the `[DEBUG] Race attempt …` log line at `pkg/proxy/race_executor.go:151` to include `credential_id=%s`. | Task 4 | Log line includes the resolved credential ID. |
| 6 | **W-1 — REVISED 2026-08-21 (reviewer pass):** In `executeRequest` (`pkg/proxy/race_executor.go:73-91`), publish `model_credential_selected` event AFTER the engine call returns and ONLY when `req.resolved.NewlyBound == true` AND `req.resolvedOK == true`. The per-`upstreamRequest` bool `bindingEmitted` is **REMOVED** — the engine's `NewlyBound` flag is the sole signal (the bool heuristic was unimplementable as a first-binding detector). **C2 — REVISED**: empty-key fresh-pick requests do **NOT** emit the event (the engine returned `NewlyBound=false` because no binding was stored; `newlyBound ⇔ a binding was stored on this call`). | Task 4 | Event fires exactly once per first binding (per `(modelID, conversationKey)` pair); pinned hits do NOT republish; verified via `EventBus.Subscribe` test sink in unit test (Phase 5). **Empty-key requests emit ZERO events** — verified by `TestHandler_EngineEventPublish_EmptyKey_NeverFires` (Phase 5; renamed from the prior `_FiresPerRequest` per C2). |
| 7 | Add `getEffectivePrimaryCredentialID(modelCfg *models.ModelConfig) string` helper in `pkg/ultimatemodel/handler.go` (free function or method); replace lines 625-635 (was 287-293 @ fea5874) `modelCfg.CredentialID != ""` check with the helper. | None | For `len(modelCfg.Credentials) == 0` the helper returns `""`; for `len == 1` returns `Credentials[0].CredentialID`; existing provider probe semantics preserved. |
| 8 | Extend `Handler.executeInternal` and `Handler.Execute` signatures with `conversationKey string`; thread through from `pkg/proxy/handler.go:635` (`rc.conversationKey`, post-auth; cite refreshed to 8f67bdf — was 627 @ fea5874). | Tasks 2, 2a, 4 | `Handler.Execute(...)` new signature has `conversationKey` as the 9th positional arg; `pkg/proxy/handler.go:635` passes `rc.conversationKey`. |
| 9 | Replace `h.modelsMgr.ResolveInternalConfig(modelCfg.ID)` at `pkg/ultimatemodel/handler_internal.go:46` with `h.modelsMgr.ResolveInternalConfigWithAffinity(modelCfg.ID, conversationKey)`. **#3 — REVISED**: the return is `(ResolvedCredential, bool)` — discard `NewlyBound` (event publish lives in `pkg/proxy/race_executor.go:executeRequest`, single source of truth; ultimate-internal does NOT publish). The ultimate-internal call site reads the struct fields directly (`.Provider`, `.APIKey`, `.BaseURL`, `.InternalModel`). Add `conversationKey` parameter. | Task 8 | Compile-clean; single call site uses the new method; no event publish added in this file. |
| 10 | **REVISED 2026-08-21 (reviewer pass — #7):** In `cmd/main.go`, obtain the engine from the store-side accessor: `credLB := modelsMgr.Engine()` (the local variable name `credLB` per #16a). **DELETE** the prior text implying `credLB := credentiallb.NewEngine(...)` — main.go does NOT construct the engine and does NOT call `RebindFromStore` (Dispatcher Ruling #2: ModelsManager owns the engine; main.go only OBTAINS it). The rebind happens inside `ModelsManager` init (Phase 2's job). If `modelsMgr.Engine()` returns `nil` (legacy / tests), the constructor's nil-safe contract kicks in (no panic; legacy `ResolveInternalConfig` is used). **AMENDED 2026-08-25 (Round 3g — Phase-2 Review Rulings — graceful-shutdown wiring + optional fast-path polish):** (a) **Graceful-shutdown teardown ordering pinned** — `cmd/main.go` currently has only `defer dbStore.Close()` at `:52`. Phase 3 adds the engine construction (this row, Task 11 subscription, Task 12 wiring) AND the teardown ordering, which is **`credLB.Stop()` (engine janitor stop) → `ModelsManager.Close()` → `dbStore.Close()`** (closes DB). Concretely: register `defer dbStore.Close()` first (existing at `:52`), then `defer modelsMgr.Close()` next (NEW — ModelsManager owns event-bus subscriptions and any per-instance cleanup; must run after the engine stops referencing modelState but before the DB closes), then `defer credLB.Stop()` BEFORE the `srv.Shutdown` call so LIFO fires them in the correct order — `srv.Shutdown` returns → `credLB.Stop()` → `modelsMgr.Close()` → `dbStore.Close()`. Add explicit nil-safe guards (`if credLB != nil { ... }`; `if modelsMgr != nil { ... }`). If `modelsMgr.Close()` does not exist yet, declare it on `*ModelsManager` (idempotent no-op when no event-bus subscriptions / no per-instance state — nil-safe). Phase 5 Task 13-side integration test asserts the order end-to-end. (b) **OPTIONAL ride-along polish (permitted, not required)** — the fast-path double-RLock at `pkg/credentiallb/engine.go:~213/230` (outer `e.mu.RLock()` + per-model `st.mu.RLock()`) MAY be collapsed to a single per-model RLock — **permitted to ride with Phase 3, not required, no behavioral change**. The double-RLock is correct today (the per-model lock is a subset of the outer lock's coverage; the outer RLock gates only the no-write fast paths, per the Round 3f pinned invariant); collapsing is a readability/micro-perf change. **Benchmark-gated if touched** — run `go test -bench=. -benchmem ./pkg/credentiallb/` before and after; do not regress fast-path allocations or contention. If touched, add a small comment at the collapse site explaining the single-RLock discipline and reference this round. | None (Phase 2 already ships the package) | On startup, `INFO: credentiallb: bound N models` log line comes from `ModelsManager` init (not main.go); `credLB` is non-nil when the DB-backed `modelsConfig` is `*database.Store` (the production path); nil-safe when `modelsConfig` is `*models.ModelsConfig` (legacy / unit-test path). Phase 5 acceptance: graceful-shutdown ordering is observable in `go test -race` (`srv.Shutdown` returns before `credLB.Stop()` runs, which runs before `modelsMgr.Close()` runs, which runs before `dbStore.Close()`); optional fast-path polish, if landed, shows non-regressing benchmark numbers. |
| 11 | Subscribe `bus` in `cmd/main.go` to `EventCredentialsChanged` events with a handler that calls `credLB.OnModelChanged(modelID, newRefs)`. **E-2** (filter-survivors): `OnModelChanged` preserves bindings whose credential still appears in `newRefs`; drops only orphans. **#7 — REVISED**: this handler is registered only if `credLB != nil` (nil-safe; legacy / tests skip the subscription). | Task 10, Phase 1 publish side | Subscription handler present on the production path; on a `ModelsManager.UpdateModel(...)` test, the handler runs (Phase 5 verifies via unit test); surviving bindings are preserved across routine weight nudges. |
| 12 | Pass `credLB` as the 7th positional arg to `proxy.NewHandler` at `cmd/main.go:108` (now `proxy.NewHandler(proxyConfig, bus, reqStore, bufferStore, tokenStore, usageCounter, credLB)`). | Task 10 | `cmd/main.go` compiles; `proxy.NewHandler` receives the engine obtained from `modelsMgr.Engine()`. |
| 13 | **REVISED 2026-08-21 (reviewer pass — #11), AMENDED 2026-08-25 (Round 3g — Phase-2 Review Rulings — ModelsManager.Close inserted in teardown order):** Shutdown ordering is load-bearing. The sequence is `srv.Shutdown(ctx)` FIRST (drains in-flight HTTP), THEN `credLB.Stop()` (stops engine janitor + per-model goroutines), THEN `modelsMgr.Close()` (NEW 2026-08-25 Round 3g — tears down event-bus subscriptions and any per-instance state; must run AFTER the engine stops referencing modelState but BEFORE the DB closes), THEN `dbStore.Close()` (closes DB). Implementation: register `defer dbStore.Close()` first (existing), then `defer modelsMgr.Close()` (NEW — added between `dbStore.Close` and `credLB.Stop` in registration order so the LIFO firing sequence is `credLB.Stop → modelsMgr.Close → dbStore.Close`), then `defer credLB.Stop()` BEFORE the `srv.Shutdown` call so LIFO fires them in the correct order — `srv.Shutdown` returns → `credLB.Stop()` → `modelsMgr.Close()` → `dbStore.Close()`. Add explicit nil-safe guards (`if credLB != nil { ... }`; `if modelsMgr != nil { ... }`). **E-1**: the janitor acquires the outer RLock and per-model write locks one at a time; reads proceed concurrently during sweeps. **Round 3g cross-ref:** see Task 10 row for the ModelsManager.Close() contract (idempotent no-op when no event-bus subscriptions / no per-instance state; nil-safe). | Task 10, Round 3g amendment | Janitor goroutine exits cleanly on SIGINT/SIGTERM (verified by no leak in `go test -race` after shutdown; Phase 5 adds an integration test that asserts the full order — `srv.Shutdown` returns before `credLB.Stop()` runs, which runs before `modelsMgr.Close()` runs, which runs before `dbStore.Close()`). |
| 14 | Update every test fixture that constructs `proxy.NewHandler` or `ultimatemodel.NewHandler` to match the new signatures (pass `nil` engine; update mocks to satisfy `ModelsConfigInterface`). **E-3**: single-credential fast path is byte-identical to today with NO binding map writes; assertable via `Engine.Stats()` or internal map size (Phase 5). | Tasks 3, 8 | `go build ./...` and `go vet ./...` clean across all packages; `go test ./pkg/proxy/... ./pkg/ultimatemodel/... ./pkg/store/database/... ./pkg/ui/...` all pass with no behavior changes (single-credential fast path unchanged; `Engine.Stats().Bindings(modelID) == 0` after N single-cred requests — Phase 5 asserts). |
| 15 | **NEW (E-4 recommended)** Add `Engine.Stats() StatsSnapshot` accessor and janitor panic-recovery: in `pkg/credentiallb/engine.go`, expose `Stats() (perModelHits, perModelMisses, perModelBindings)` and wrap the janitor sweep in `defer recover()` that logs and continues. Per-model RNG (or `math/rand/v2`) — shared `rand.Rand` + mutex serializes first-selections across all models; per-model RNG removes contention. Non-blocking recommendations. | Phase 2 (engine exists) | `Stats()` returns sane numbers under load; janitor survives an injected panic in one model's sweep (other models continue to be swept). |
| 16 | **NEW 2026-08-21 — C1 (reviewer pass):** Wire the engine on the Anthropic `/v1/messages` internal path. Add `conversationKey string` to `anthropicRequestContext` (`pkg/proxy/handler_anthropic.go:740`; was 697 @ fea5874); compute the Anthropic-path key POST-AUTH (mirror the chat-completions wiring site per A-1; the Anthropic path's auth-resolution site is inside `HandleAnthropicMessages`); extend `InternalHandler.HandleRequest` (`pkg/proxy/internal_handler.go:109`) to accept `conversationKey`; swap `internal_handler.go:111` from `ResolveInternalConfig` (5-tuple) to `ResolveInternalConfigWithAffinity` (struct + `ok bool` per #3). Pass `arc.conversationKey` from `handler_anthropic.go:478` (the `NewInternalHandler` construction) through to the seam. (All cites refreshed to 8f67bdf — fea5874-era: 697/67/69/461.) **C2**: the resolved `ResolvedCredential.NewlyBound == false` for empty-key picks → no event published on the Anthropic path. | Tasks 4, 11 (Scope) | Compile-clean; `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0 (only `ResolveInternalConfigWithAffinity` remains); Anthropic-shaped `/v1/messages` requests with the same `(tokenID, firstUserMessage)` land on the same upstream credential (Phase 5 verifies). |
| 17 | **NEW 2026-08-25 (Round 3 — R3-1), AMENDED 2026-08-25 (Round 3b — review #6), AMENDED 2026-08-25 (Round 3c — W2 `ProviderError.ErrorType`/`ErrorCode`): rate-limit classifier in `pkg/providers`.** Add `RetryAfter time.Duration` to `ProviderError` (`providers/interface.go` ~168) and `func IsRateLimitError(err error) bool` (package-level): true when the error wraps a `*ProviderError` whose HTTP status is 429 OR whose error-body code/type matches the rate-limit set per the **Round-3b pinned classifier vocabulary table**: **type-match = substring `rate_limit`** (covers both `models.ErrorTypeRateLimit="rate_limit"` and `pkg/proxy/translator.ErrorTypeRateLimit="rate_limit_error"`); **code-match = equality** on the rate-limit code constants (sources `models/errors.go:7,17` and `pkg/proxy/translator/errors.go:13,70,129`); HTTP status match = `429`. **(amended Round 3c — W2)** `ProviderError` additively gains **`ErrorType string`** and **`ErrorCode string`** fields (`providers/interface.go:155-161` — the struct currently lacks them; the vocabulary table reads `ProviderError.ErrorType`/`ProviderError.ErrorCode` post-extension instead of re-parsing raw bodies at the classifier), populated in `handleError` (`openai.go:234-246`) from the EXISTING unmarshal into the anonymous local `struct { Error struct { Message, Type, Code string } }` — the Type/Code values are already decoded there and then discarded; the extension merely assigns them to the returned `*ProviderError`. This makes the 503-body matrix row (Task 30 row (g)) implementable: a 503 whose body carries a rate-limit code/type now surfaces through `ProviderError.ErrorType`/`ErrorCode` where `IsRateLimitError` can read it. Classifier returns true if **any** of (type-substring, code-equality, status-429) matches. The classifier table is ONE table pinned in the plan (not scattered across the code); the implementation reads it once and routes accordingly. Wire the existing DEAD `parseRetryAfter` (`openai.go:593` — zero callers today, conflict-free): the 429 construction path (`openai.go` ~255 inside the 217-268 error region) parses the `Retry-After` header into `ProviderError.RetryAfter` (**Round 3b amend — explicit absent-`Retry-After` handling**: absent header or unparseable value (`""`, `"abc"`, malformed RFC1123) ⇒ `RetryAfter=0`; callers apply the 60s default per R3-4 / Task 11 in phase2 — `RetryAfter=0` is NEVER a "no cooldown" signal, it is a "use default" signal). The classifier also adds a matrix row: **503 status with rate-limit body code/type** ⇒ `true` (status ≠ 429 but the body explicitly identifies a rate-limit; per the contract's `translator` body-code check at `technical-analysis.md:858-872`). **Round 3b amend — explicit OUT OF SCOPE**: HTTP-200-with-embedded-rate-limit-error (mid-stream SSE `data:` payloads) is OUT OF SCOPE for R3-1; would require mid-stream parsing and is future work. **HTTP 200 with no rate-limit body is not classified.** No behavior change for non-429 errors; `IsRetryable` untouched. | None | `go build ./pkg/providers/...` green; `grep -n "parseRetryAfter" pkg/providers/openai.go` shows ≥1 non-definition caller (dead code wired); classifier unit matrix green (Phase 5 Task 30): 429 status (true), `models.ErrorTypeRateLimit` substring match (true), `pkg/proxy/translator.ErrorTypeRateLimit` substring match (true), code-equality on rate-limit code (true), MiniMax-style OpenAI-compatible envelope with rate-limit code (true), 503 + rate-limit body (true), 500 non-rate-limit body (false), 401/403 auth errors (false), 200 OK (false — out of scope per Round 3b), absent `Retry-After` header (⇒ `RetryAfter=0`, NOT panic, NOT 0-cooldown default; caller applies 60s) |
| 18 | **NEW 2026-08-25 (Round 3 — R3-3), AMENDED 2026-08-25 (Round 3b — B1 + leader ruling (iii) + canonical naming), AMENDED 2026-08-25 (Round 3c — C1 hoisted admission gate + C2 consumer mapping + C3 wiring sub-tasks + W3 single-shot flag + W4 ruling (iii) re-scope + S4 GetRequestStatuses): coordinator interception branch — credential failover BEFORE model switching.** In `race_coordinator.go` `manage()` (function at `:252`; the Round-3 task text used the wrong name `monitorLoop` which does NOT exist at HEAD — **PURGED**) Case 1 (~`:350-364`, where `latestReq.GetError() != nil` currently sets `shouldSpawn` for `modelTypeFallback` — `spawn()` (function at `:189`; the Round-3 text used `spawnAttempt` which does NOT exist — **PURGED**) picks `c.models[1]` at `~:215`): insert a pre-spawn check — (a) the failed request's model is internal (recheck via `rc.modelList` loop precedent at `handler.go:290` / `cfg.ModelsConfig.GetModel`); (b) `len(modelConfig.Credentials) > 1`; (c) `IsRateLimitError(latestReq.GetError())` (Task 17 classifier); (d) **Case-2 idle path — (amended Round 3c — W4 RE-SCOPE of leader ruling (iii); the Round-3b wording is SUPERSEDED, audit trail below)**: the Round-3b text required the Case-2 idle path (`manage()` `:364-395`, spawning `modelTypeSecond` + `modelTypeFallback` together at `:399-405`) to "ALSO run the rate-limit classification before spawning" — **wrong as stated: at idle-spawn time NO error exists to classify** (the Case-2 trigger is idle-stall of an in-flight attempt, not a returned error; `latestReq.GetError()` is nil for a stalled-but-unfinished request). Correct semantics: **Case 2 spawns `modelTypeSecond`/`modelTypeFallback` exactly as today — UNCHANGED**; the stalled attempt's eventual 429 is adjudicated by **Case 1** (which is now actually reachable in the multi-attempt environment the idle-spawn created — the C1-hoisted gate is exactly what makes that adjudication possible). Ordering guarantee preserved: when the stalled main later 429s, credential failover still precedes any FURTHER model spawn (Phase 5 Task 37, REWRITTEN Round 3c, asserts this ordering). **Audit trail:** Round-3b ruling (iii) literal text ("idle path runs classification before spawning") is marked SUPERSEDED by this Round 3c re-scope; the ruling's INTENT (uniform precedence — a rate-limited credential is always adjudicated before more models are spawned) is preserved via the Case-1 path; (e) **Case-1 latest-only inspection = accepted v1 behavior** (the review flagged that Case 1 reads `c.requests[len-1]` only; an earlier attempt's 429 is never adjudicated — documented as accepted v1, not adjudicated in spawn order — `modelTypeSecond` re-keys failover to the primary credential since secondary attempts share `c.models[0]`'s modelID but resolve the primary credential via `race_executor.go:112`; specified behavior, not a defect); (f) retry budget remaining (Task 19). ALL conditions (a)-(f) true ⇒ **`credID, mode := engine.ExcludeAndReselect(modelID, conversationKey, failedCredID, retryAfter)`** (Round-3b signature — see phase2 Task 12) — when `mode == ReselectHealthy`, spawn a **same-model** attempt with the NEW model type `modelTypeCredFailover` (Round-3b canonical Go identifier — the Round-3 text had three variants: `credential_failover` (F.3 tree), `modelTypeCredentialFailover` (F.3 rationale), `modelTypeCredFailover` (Round-3 Task 18); **canonical: `modelTypeCredFailover`** — purges the two others; **`model_credential_failover` stays as the EVENT name**; **`triggerRateLimit` is the spawn-trigger constant** name; `triggerMainError` continues to identify the Case-1 trigger) carrying the reselected credential override — INSTEAD of model fallback; when `mode == ReselectSoonestExpiry`, same-model spawn ONCE with the soonest-expiring credential override (**amended Round 3c — W3 single-shot enforcement**: a per-request `soonestExpiryAttempted bool` flag on the coordinator request state (set when the SoonestExpiry spawn fires) + `continue` in the manage loop — once set, a subsequent 429 can NEVER produce a second SoonestExpiry spawn; the next 429 routes to model-fallback sequentially per §F.4 — single-attempt-then-fall-through is structurally enforced, not convention); when `mode == ReselectNone`, fall through to existing `modelTypeFallback` spawn unchanged (caller routes directly to model-fallback). **(amended Round 3c — C2) consumer logic FOLLOWS phase2 Task 12's unified mode mapping verbatim**: `ReselectNone` → model-fallback fall-through; `ReselectSoonestExpiry` → single attempt then fall-through; `ReselectHealthy` (including the B2 no-op return `(currentBinding.credentialID, ReselectHealthy)`) → proceed with the returned credential. Phase2 semantics win; the contract layer is synced by the sibling worker. **B1 (spawn-window gate exemption) — (amended Round 3c — C1 CRITICAL: HOIST the credFailover pre-checks ABOVE the `:338` window gate; the Round-3b exemption-only form is UNREACHABLE in the core scenario):** the Round-3b text placed the Case-1 pre-spawn check behind the spawn-window gate at `manage()` `:338` (`if c.winner == nil && len(c.requests) < len(c.models)`), with `modelTypeCredFailover` rows merely EXEMPT from the count. In the core scenario (3 credentials / 1 model / no fallback chain) the window evaluates `1 < 1` = **false from the FIRST failure** → the Case-1 branch never runs → **zero failovers → the request dead-loops to the stream deadline**. That is exactly the user's quote scenario, so the exemption alone is dead code there. **Fix (pinned):** the credFailover pre-checks (rate-limit classification + internal-model + `len(Credentials) > 1` + budget remaining + `winner == nil`) are evaluated ABOVE / short-circuiting the window gate; the gate itself becomes, verbatim: `gate := modelAttempts < len(models) \|\| credFailoverEligibleWithBudget()` — where `modelAttempts` is the Round-3b separate accounting (counts ONLY `modelType ∈ {main, second, fallback}` requests) and `credFailoverEligibleWithBudget()` is the conjunction of the hoisted pre-checks (a)-(c)+(f) above. When the gate is true via the credFailover arm, the Case-1 `ExcludeAndReselect` branch runs even at `modelAttempts >= len(models)`; the spawn-window check at `:338` and the all-failed check at `:420-421` (`len(c.requests) >= len(c.models)` on the separate accounting) still do NOT see `modelTypeCredFailover` rows — **ONE mechanism pinned: separate accounting** (an internal counter `credFailoverAttempts` increments per `modelTypeCredFailover` spawn; the alternative — extending the cap by `len(modelCfg.Credentials)−1` — remains **REJECTED**; **equivalence note (Round 3c)**: the hoisted-gate form is SEMANTICALLY EQUIVALENT to the rejected cap-extension (`cap += len(credentials)−1` admits the same spawn set); cap-extension is rejected as an IMPLEMENTATION choice — it mutates len-dependent invariants (`len(c.models)` is load-bearing in the all-failed check and the terminal condition) — NOT as a semantic difference). **Regression guard:** Phase 5 Task 32 scenario (a) (3-cred/no-chain FULL chain) is the pinned regression test for EXACTLY this dead-loop bug — it fails if the pre-checks ever sink back below the window gate. All-failed condition (amended): "all fallback models tried OR credential budget exhausted with no candidate remaining"; the terminal `:860-868` "All models rate limited" shape MUST NOT fire while an untried fallback model or credential remains; after credential exhaustion, fall through to the existing `modelTypeFallback` spawn (`c.models[1]`) when it exists. The existing terminal shape `:860-868` itself is UNCHANGED (R3-4). **C3 — (amended Round 3c — CRITICAL) three wiring sub-tasks for currently-referenced-but-NONEXISTENT code (verified @ 8f67bdf; without them the Task-18 branch cannot compile or silently misfires):** (1) **`spawnTriggerInfo` gains `credentialID string`** — the struct at `race_coordinator.go:79` currently carries only trigger/errorMessage/failedRequest; the failed credential ID must ride it from the Case-1 classification to `spawn()`/the executor so the `modelTypeCredFailover` attempt resolves the SPECIFIC reselected credential (not a fresh `GetOrSelect`); (2) **(amended Round 3j — Phase-3 Review C1 root-cause; the Round-3c pin below is SUPERSEDED, kept visible)** `spawn()` switch gains `case modelTypeCredFailover:` resolving the model from the TRIGGER — **old pin, (SUPERSEDED Round 3j):** "the switch at `race_coordinator.go:~196-218` (handles main/second/fallback only; an absent case leaves `modelID == ""` and the spawn silently misfires — wrong model key, resolution failure) gains `case modelTypeCredFailover: modelID = c.models[0]`" — **WRONG** when a FALLBACK row (`c.models[1]`) is the one that 429'd in a multi-model chain: the implemented arm (`race_coordinator.go:269-280`, its comment "c.models[0] keeps the row on the SAME model that just 429'd") spawns the failover on the MAIN model instead of the model whose credential was rate-limited. **Correct pin (Round 3j):** (i) **`spawnTriggerInfo` gains a `modelID` field** (alongside the existing `credentialID` from sub-task (1)), populated from `latestReq.modelID` at the Case-1 spawn site — the failed attempt's OWN model, not a positional `c.models` index; (ii) **the `case modelTypeCredFailover:` arm uses `triggerInfo.modelID`** (falling back to `c.models[0]` ONLY if empty, defensively) — replacing the `modelID = c.models[0]` pin; (iii) **`resolveSpecificInternalCredential` belt-and-braces**: the executor-side check (`race_executor.go:173`, B3-specific credential resolution) verifies the reselected credential is a member of THAT model's `Credentials` — the defense layer catching any trigger-population bug. **Audit trail (Round 3j):** Round-3c C3(2) pinned `modelID = c.models[0]` → implemented faithfully at 675556f → Phase-3 review caught the multi-model defect (fallback-row 429 spawns on the wrong model) → corrected in Round 3j to trigger-carried modelID; (3) **`newRaceCoordinatorWithEvents` gains `engine` + `conversationKey` params** — the constructor at `race_coordinator.go:129` currently takes 8 params with NO engine/conversationKey; both are added (constructor-only injection per W-3 — **NO `Config.CredEngine`**), wired from `cmd/main.go` (engine via `modelsMgr.Engine()`, threaded through `proxy.NewHandler`) through the call site at `handler.go:830`. **S4 — (amended Round 3c) `GetRequestStatuses` credFailover-row handling folded INTO this task** (was a ride-along note): the coordinator's status snapshot (consumed by the UI request inspector) must render `modelTypeCredFailover` rows with their credential identity — a `credentialID` field on the per-request status row (from sub-task (1)) so operators can see WHICH credential each failover attempt used; non-credFailover rows leave it empty. **`requestContext.triedCredentialIDs` / `failoverAttempts` (Task 19)** are mutated ONLY from the `manage()` loop body — single-goroutine, serialized by the coordinator mutex (`c.mu`); the task's mandated code comment MUST be present at both field declarations and at every mutation site: `// mutated only from manage() under c.mu — race-free by construction`. | 17, Phase 2 Tasks 11-13 | **Round-3b gate-exemption tests (amended acceptance)**: (a) **3-credential single-model rescue** (`len(c.models) == 1`, no fallback chain, 3 creds on one model) — get the FULL failover chain (2 `modelTypeCredFailover` respawns for the user's core scenario — the EXACT scenario the user's quote described); (b) **2-entry model list rescue** — both credentials tried, then `c.models[1]` IS tried (no silent termination); (c) **Case-2 idle ordering — (amended Round 3c — W4; supersedes the Round-3b "classification before spawn" acceptance)**: idle-stalled main ⇒ Case-2 spawns second/fallback UNCHANGED; when the stalled main LATER 429s, credential failover (Case-1, now reachable via the C1-hoisted gate) precedes any FURTHER model spawn (Phase 5 Task 37 REWRITTEN asserts this ordering); (d) `grep -n "modelTypeCredFailover" pkg/proxy/race_request.go pkg/proxy/race_coordinator.go` returns the constant + the Case-1 spawn ONLY (no hits in `:338` window check or `:420-421` all-failed check); (e) `grep -n "monitorLoop\|spawnAttempt" pkg/proxy/` returns ZERO hits (the Round-3 task text's wrong names are purged); (f) `grep -n "modelTypeCredentialFailover" pkg/proxy/` returns ZERO hits (canonical is `modelTypeCredFailover`); (g) `grep -n "triggerRateLimit\|triggerMainError" pkg/proxy/race_coordinator.go` returns both trigger constants; (h) `grep -n "// mutated only from manage" pkg/proxy/` returns the mandated comment at both `requestContext` field declarations and the `manage()` mutation sites. **Round 3c additions (amended acceptance):** (i) **C1 hoisted-gate grep**: `grep -n "credFailoverEligibleWithBudget" pkg/proxy/race_coordinator.go` returns the helper definition + its use in the hoisted admission gate (`gate := modelAttempts < len(models) \|\| credFailoverEligibleWithBudget()`), evaluated BEFORE the plain-window short-circuit **(Round 3j re-baseline: the identifier grep is the gate; the full `gate := modelAttempts < len(models) || credFailoverEligibleWithBudget()` expression grep is ADVISORY — gofmt line-wrapping may split it)**; (j) **C3 wiring greps (amended Round 3j)**: `grep -n "credentialID" pkg/proxy/race_coordinator.go` shows `spawnTriggerInfo.credentialID` + the `case modelTypeCredFailover:` arm resolving `modelID` from `triggerInfo.modelID` (the `modelID = c.models[0]` population is SUPERSEDED Round 3j); `grep -n "modelID" pkg/proxy/race_coordinator.go` shows the `spawnTriggerInfo.modelID` field + its population from `latestReq.modelID` at the Case-1 spawn site (Round 3j addition); `grep -n "newRaceCoordinatorWithEvents" pkg/proxy/handler.go cmd/main.go pkg/proxy/race_coordinator.go` shows the widened constructor signature (engine + conversationKey) and the `handler.go:830` call site passing both; (k) **W3 single-shot**: `grep -n "soonestExpiryAttempted" pkg/proxy/race_coordinator.go` returns the flag declaration + the set-once check; the all-cooling-then-re-429 path provably routes to model-fallback (Phase 5 Tasks 31/36); (l) **S4**: `GetRequestStatuses` output includes `credentialID` on `modelTypeCredFailover` rows (Phase 5 Task 45 asserts the structure: exactly `len(creds)−1` credFailover rows + 0-or-1 fallback row after exhaustion); (m) **Round 3j — 2-model fallback-429 regression** (pinned as Phase 5 Task 32 scenario (c)): modelList [M1, M2]; M1's attempt succeeds partially then the FALLBACK row M2 (multi-cred) 429s; the credFailover respawn must stay on M2 (M2's other credential), NOT jump to M1 — assert via `GetRequestStatuses` row model identity + event payload `model_id`. Single-cred model 429 still spawns `modelTypeFallback` byte-identical to today (Phase 5 Task 29 regression guard; **Phase 5 Task 32 scenario (a) is the pinned C1 dead-loop regression guard**). |
| 19 | **NEW 2026-08-25 (Round 3 — R3-5): retry-budget plumbing in `requestContext`/`upstreamRequest`.** Per-request accounting lives in the REQUEST context (R3-5 — engine stays per-credential-state only): `requestContext` gains `triedCredentialIDs` + `failoverAttempts` (`handler_helpers.go`, cleared in `reset()`); `upstreamRequest` gains the failover fields (reselected `credentialID` override + `retryAfter` carried from `ProviderError`); the coordinator increments `failoverAttempts` per `modelTypeCredFailover` spawn and records the tried credential. **Budget rule: each remaining credential at most once per request; total failover attempts ≤ len(credentials)−1.** Budget check is part of Task 18's pre-spawn condition (e). No loops are possible: the tried-set makes re-selection of a tried credential impossible (`ExcludeAndReselect` exclusion + tried-set intersection). | 18 | Unit: 3-credential model with all-failing 429s terminates after exactly 2 failover attempts + 1 fallback-or-terminal path; `failoverAttempts` never exceeds `len(credentials)−1`; `reset()` clears the state (pool reuse safe). |
| 20 | **NEW 2026-08-25 (Round 3 — R3-8): `model_credential_failover` event + `[LB-FAILOVER]` logs.** Publish via `events.Bus` on every successful failover spawn: payload `{model_id, from_credential_id, to_credential_id, reason: "rate_limit", retry_after_ms, cooldown_ms, attempt_index}` (mirror the `race_secondary_model_used` publish shape at `race_executor.go:121-129`). Log prefix `[LB-FAILOVER]` at INFO for successful failovers and WARN for all-cooling soonest-expiry picks + budget exhaustion. `EngineStats.Failovers`/`Cooldowns` surface through the existing `Engine.Stats()` (Phase 2 Task 14) — no new accessor. | 18, 19 | `grep -rn "model_credential_failover" pkg/` returns the publish site(s) with the full payload; `grep -rn "LB-FAILOVER" pkg/` returns INFO + WARN sites; Phase 5 Task 34 asserts payload + Stats counters. |
| 21 | **NEW 2026-08-25 (Round 3 — R3-3), AMENDED 2026-08-25 (Round 3b — review #4 + leader ruling (i) corollary): ultimate-internal hook.** In `pkg/ultimatemodel/handler_internal.go` (36-49): on a rate-limited provider error from the internal execution BEFORE any client write, apply the same pattern — classifier check (`IsRateLimitError`, Task 17), `len(Credentials) > 1`, budget (Task 19), `engine.ExcludeAndReselect(...)` (Round-3b signature with `ReselectMode` return, phase2 Task 12), single retry with the reselected credential (`ReselectHealthy` / `ReselectSoonestExpiry`); `ReselectNone` ⇒ propagate (existing failure shape). **Round 3b guard correction (review #4)** — the cited `headersSent` marker is UNUSABLE: `Execute` sets `*headersSent = true` at `ultimatemodel/handler.go:608-609` and starts the SSE heartbeat (`startSSEHeartbeat(w, heartbeatCtx)` at `:643`, writes `: heartbeat\n\n` during execution) BEFORE `executeInternal` (`:649-655`). Correct ultimate guard: retry only when the returned error is the **initial provider-call `*ProviderError`** (type assertion via `errors.As(err, &providerErr)` where `providerErr *providers.ProviderError`); structurally sound because initial-call errors return before any content write (`handler_internal.go:155-157` non-stream, `:253-255` stream) and mid-stream failures surface as SSE error events. The guard is the type assertion, NOT `headersSent`. **Must not conflict with post-fea5874 code in this area**: the thinking-sink invariant (`internal_handler.go:72-80` capture-only discipline), the bare-JSON capture fallback, and `extractNonStreamContent` — the retry re-enters the internal execution BEFORE any capture/wire side effects (first-byte guard, Task 23). | 17-19, Phase 2 | **Round 3b grep-gates (amended acceptance)**: `grep -n "ExcludeAndReselect" pkg/ultimatemodel/handler_internal.go` returns the hook; `grep -n "headersSent" pkg/ultimatemodel/handler_internal.go` returns ZERO hits as a guard (the marker is `*ProviderError` per review #4); `grep -rn "ExcludeAndReselect" pkg/ultimatemodel/` returns the hook only; thinking-capture tests still green (no double-sink on retry — the retry path constructs a fresh execution context); Phase 5 covers E2E. |
| 22 | **NEW 2026-08-25 (Round 3 — R3-3), AMENDED 2026-08-25 (Round 3b — review #4 + leader ruling (i)), AMENDED 2026-08-25 (Round 3c — W1 passthrough classification premise corrected): `/v1/messages` internal hook + anthropic-passthrough internal branch IN failover scope.** Implement the equivalent failover at the `InternalHandler` seam (`pkg/proxy/internal_handler.go:109-111` — `HandleRequest` / `ResolveInternalConfig` call) so the Anthropic path (`handler_anthropic.go:348 → :455 → :478` chain) inherits same-provider failover: rate-limited internal provider error + multi-cred + budget + no Anthropic bytes written ⇒ **`engine.ExcludeAndReselect(...)` (Round-3b signature with `ReselectMode` return, phase2 Task 12) + single retry** inside `HandleRequest` (or a thin wrapper the Anthropic path calls); `ReselectNone` ⇒ propagate; `ReselectSoonestExpiry` ⇒ single-attempt retry then propagate; once the Anthropic response write begins (first-byte marker = the SSE header write at **`handler_anthropic.go:648-654`** — sets `Content-Type: text/event-stream` + `Cache-Control: no-cache, no-transform` + `Connection: keep-alive` + `X-Accel-Buffering: no` + `WriteHeader(http.StatusOK)` + sets `arc.headersSent = true` + writes `: connected\n\n` — **NOT** `adapter_anthropic.go:170-176` which is the unrelated `SetStreamHeaders` helper, the review's conflated cite is **FIXED**), the error propagates (R3-6). **Round 3b leader ruling (i)** — extend coverage to the **`handler_anthropic.go:297-345` anthropic-passthrough internal branch** (internal model + anthropic-provider credential) for C1 consistency with the internal-handler hook: this branch currently bypasses `doAnthropicInternalRequest` entirely and reads `modelConfig.CredentialID` / `GetCredential` / `cred.ResolveAPIKey()` / sets `arc.credentialAPIKey` to drive `doAnthropicRequest` (passthrough transport — UNCHANGED per the dispatch ruling). The branch swaps: (1) `modelConfig.CredentialID` read → `ResolveInternalConfigWithAffinity(modelConfig.ID, conversationKey)` returning `ResolvedCredential` (per #3); (2) `arc.credentialAPIKey = resolved.APIKey`; (3) `cred` read replaced by the affinity-resolved credential; (4) the rate-limit hook added — **(amended Round 3c — W1: the passthrough branch produces NO `*ProviderError`; the Task-21 type-assertion guard does NOT apply here)**: `doAnthropicRequest` never constructs a `*ProviderError` and never reads `resp.Header` (Retry-After is discarded today), so the classification spec for this branch is: **`arc.lastStatusCode == 429` OR the response body's `type` field contains the substring `rate_limit`** (`arc.lastStatusCode` exists today — set at `handler_anthropic.go:423`, consumed at `:272-275`); **Retry-After is captured via a NEW additive `arc.retryAfter time.Duration` field**, populated at the same site `arc.lastStatusCode` is set (`:423`) by reading the response header there (one-line additive change inside `doAnthropicRequest`'s status-recording path) — plus multi-cred + budget + **no Anthropic bytes written** (pre-write guard: `arc.headersSent` at `:588`/`:654`/`:1065`) ⇒ `ExcludeAndReselect` (passing `arc.retryAfter`; 0 ⇒ engine applies the 60s default) + single retry with the reselected credential; passthrough transport via `doAnthropicRequest` is **UNCHANGED** (the ruling preserves the wire shape). | 17-19, Phase 2 | **Round 3b grep-gates (amended acceptance)**: `grep -n "ExcludeAndReselect\|IsRateLimitError" pkg/proxy/internal_handler.go` returns the hook; `grep -n "ResolveInternalConfigWithAffinity" pkg/proxy/handler_anthropic.go` returns **≥2 sites** (the internal-handler construction site at `:478` + the new passthrough-branch site at `:297-345` per ruling (i)) — proves ruling (i) coverage is in place; `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0 (C1 grep-guard — only `ResolveInternalConfigWithAffinity` remains); `grep -n "ResolveInternalConfig(" pkg/proxy/handler_anthropic.go` returns 0 in the `/v1/messages` internal path (the Anthropic file uses `HandleAnthropicMessages` + `doAnthropicInternalRequest` + the passthrough branch via `doAnthropicRequest`, NOT legacy `ResolveInternalConfig` directly); `grep -n "adapter_anthropic.go:170-176" pkg/proxy/internal_handler.go pkg/proxy/handler_anthropic.go pkg/proxy/race_coordinator.go` returns ZERO hits as a first-byte cite (the cite is `handler_anthropic.go:648-654`); `TestHandler_AnthropicMessages_RateLimitFailover` (Phase 5) passes — covering BOTH the regular `/v1/messages` internal hook AND the new passthrough-branch hook. |
| 23 | **NEW 2026-08-25 (Round 3 — R3-6), AMENDED 2026-08-25 (Round 3b — review #4 purge): explicit streaming-constraint guard — failover ONLY before first client byte.** Document + enforce per path with **REAL guards only** (the Round-3 task text referenced nonexistent state; **PURGED in place** per Round 3b review #4): (a) **race-internal** — guard = **`c.winner == nil`** (winner requires `req.IsCompleted() && req.GetError() == nil` per `:299`; `WaitForWinner` is the synchronization point). The invariant wording is **"no CONTENT flushes pre-winner"** (NOT "nothing flushes pre-winner" — the framing headers + `": connected\n\n"` ARE written at `handler.go:783-797` BEFORE `coordinator.Start()` at `:831`, and `rc.headersSent = true` is set at `:795`; these carry framing, not model content, so the failover guard is `c.winner == nil`, not `headersSent`). The Round-3 references to **`winner.chunksFlushedToClient`** and **`latestReq.bytesFlushedToClient()`** are **DELETED** — neither field nor method exists at HEAD (`grep -rn "chunksFlushedToClient\|bytesFlushedToClient" pkg/` returns ZERO hits); the Round-3 `latestReq.status != statusStreaming` reference is **DELETED** — it is vacuous (failed attempts are `statusFailed`, not streaming). The actual flush points run only post-`WaitForWinner`: `streamResult` call `:852` / definition `:960` and `handleNonStreamResult` call `:879` / definition `:1327`. The interception site (Task 18) is pre-winner by construction — the guard `c.winner == nil` at the spawn-block asserts a future refactor cannot flush early. (b) **ultimate-internal** — guard = **initial-call `*ProviderError` type assertion** (per Round 3b review #4; the Round-3 `headersSent` marker is UNUSABLE because `*headersSent = true` is set at `ultimatemodel/handler.go:608-609` BEFORE `executeInternal` at `:649-655`, AND the SSE heartbeat starts at `:643` BEFORE `executeInternal`); initial-call errors return before any content write (`handler_internal.go:155-157` non-stream, `:253-255` stream). (c) **`/v1/messages` internal** — guard = `arc.headersSent == false` at the hook site (per the pre-write rule); the SSE header write at **`handler_anthropic.go:648-654`** is the first-byte marker (NOT `adapter_anthropic.go:170-176` — that is `SetStreamHeaders`, an unrelated helper; the review's conflated cite is **FIXED**). Once ANY first-byte marker has fired, rate-limit errors PROPAGATE (no respawn) — documented in code comments at all three hooks. | 18, 21, 22 | **Round 3b acceptance (amended)**: each hook site carries the real guard + comment; Phase 5 Task 33 (`streaming-started-no-failover`) asserts an error after first byte propagates with zero respawns; **grep-gates (amended)**: `grep -rn "chunksFlushedToClient\|bytesFlushedToClient" pkg/` returns ZERO hits (Round-3 invented state purged); `grep -n "headersSent" pkg/ultimatemodel/handler_internal.go` returns ZERO hits as a guard (the guard is `*ProviderError`); `grep -n "adapter_anthropic.go:170-176" pkg/` returns ZERO hits as a first-byte cite (cite is `handler_anthropic.go:648-654`); `grep -rn "first byte\|firstByte" pkg/proxy/race_coordinator.go pkg/ultimatemodel/handler_internal.go pkg/proxy/internal_handler.go pkg/proxy/handler_anthropic.go` shows the guard comments at every hook site |
| 24 | **NEW 2026-08-25 (Round 3 — R3-7), AMENDED 2026-08-25 (Round 3c — W6 four-path happy-path acceptance): explicit no-change notes — race-external + ultimate-external.** Document (code comments + this plan) that credential failover does NOT touch: (a) the race-external env path — `race_executor.go:279-300` (`cfg.UpstreamCredentialID` resolution) stays UNCHANGED: a 429 there follows the existing model-racing behavior, no `ExcludeAndReselect`, no cooldown (the env credential is not model-owned); (b) ultimate-external passthrough — provider detection only (`PrimaryCredentialID()` shim, Task 7); client auth passes through, no credential switching. Scope = **(amended Round 3c — W6) the FOUR LB'd internal paths** (race-internal, ultimate-internal, `/v1/messages` internal, **anthropic-passthrough internal branch** — the "three paths" wording superseded by the Round-3b ruling (i) scope expansion; this row now says FOUR). | 18-22 | `grep -n "ExcludeAndReselect\|IsRateLimitError\|modelTypeCredFailover" pkg/proxy/race_executor.go` shows hits ONLY in the internal-path regions (100-138 / provider-call plumbing), ZERO in the 279-300 external region; ultimate-external region of `pkg/ultimatemodel/handler.go` shows no failover references; Phase 5 E2E confirms external-path behavior byte-identical. **(amended Round 3c — W6) failover happy path × all FOUR LB'd paths is part of THIS task's acceptance**: a 429-on-credential-A → succeeds-on-credential-B failover is demonstrated on EACH of race-internal, ultimate-internal, `/v1/messages` internal, and the anthropic-passthrough internal branch (Phase 5 Tasks 23, 21-hook coverage, 19-extension, and 38 respectively; the four-path matrix is asserted in Phase 5 Exit Criterion #13) — a path missing its happy-path failover test fails this task even if its grep-gates pass. |

## Coupling

> **AMENDED 2026-08-21**: coupling entries updated to reflect the post-auth wiring site (A-1) and the engine's 3-return `GetOrSelect` (W-1). The structural couplings (which phase owns which contract surface) are unchanged; only the contract surface itself shifted.

- **Tight with Phase 1 (DB/store)**: Phase 3 calls `ModelsManager.ResolveInternalConfigWithAffinity` and reads `ModelConfig.Credentials []CredentialRef`; if Phase 1 changes those, Phase 3 must re-align. Specifically:
  - `pkg/store/database/store.go:1316-1350` — `ResolveInternalConfig` (legacy) is still called by the engine's single-credential fast path inside `ResolveInternalConfigWithAffinity`. Phase 3 does NOT call it directly; Phase 2/1 owns that boundary.
  - `pkg/store/database/store.go` — `ResolveInternalConfigWithAffinity` returns `(ResolvedCredential, bool)`; the `ResolvedCredential.NewlyBound` struct field carries the W-1 signal to the race coordinator (per Round-2 #3 — struct form; supersedes the '6th return or side-channel' phrasing).
  - `pkg/models/config.go:80-111` — `ModelConfig.Credentials` field must exist when Phase 3 reads it.
- **Tight with Phase 2 (engine)**: Phase 3 imports `pkg/credentiallb` for `Engine`, `ComputeConversationKey`, `ExtractFirstUserMessage`, `ErrNoCredentials`, `EventCredentialsChanged`. **W-1**: `Engine.GetOrSelect` now returns `(credentialID, newlyBound, err)` — Phase 3 must thread `newlyBound` upstream to the event-publish site. **W-2**: empty-key requests return `(credentialID, true, nil)` (fresh pick, no binding stored). Any signature drift between Phase 2 and Phase 3 breaks Phase 3's import lines. The contract is frozen in `technical-analysis.md §API Contract` (lines 1349-1373 changelog + amended contract) — drift requires a sibling-level reconciliation.
- **Loose with Phase 4 (UI/API)**: Phase 4 mutates the JSON payload that `pkg/ui/server.go` sends and that the frontend posts; Phase 3 does not touch the UI server. The seam is the JSON wire shape of the `models.credentials` array (per Phase 4 contract). **M-1**: the DB column `credential_id` is kept as a derived shadow (written by Phase 1's migration); Phase 4's UI/API surface still drops `credential_id` from the JSON wire — the shadow is invisible to the UI.
- **Loose with Phase 5 (tests)**: Phase 5 will add multi-credential handler integration tests and engine unit tests. Phase 3 must leave `executeInternalRequest`'s call site clean (no extra logging or behavior) so Phase 5's request-counting assertions are stable. **W-1 + E-3**: Phase 5's event test must use `Engine.Stats()` or an event-sink count to verify `model_credential_selected` fires exactly once per first binding (not per request).
- **Independent of the existing `pkg/ultimatemodel/hash_cache.go`** (decision A — we do NOT reuse `HashMessages`; the engine owns the conversation key format via `pkg/credentiallb/key.go`).

### Coupling Matrix

| | Phase 1 | Phase 2 | Phase 3 | Phase 4 | Phase 5 |
|---|---|---|---|---|---|
| Phase 1 | — | tight (store writes Engine refs) | tight (ModelsManager API consumed here) | tight (DTO shape) | loose (backfill tests) |
| Phase 2 | tight | — | tight (Engine API consumed here) | independent | tight (engine unit tests) |
| Phase 3 | tight | tight | — | loose (DTO shape) | loose (integration tests) |
| Phase 4 | tight | independent | loose | — | tight (UI snapshot tests if any) |
| Phase 5 | loose | tight | loose | tight | — |

### Coupling — Round 3 additions (2026-08-25, rate-limit failover)

| | Task 17 (classifier) | Task 18 (interception) | Task 19 (budget) | Tasks 21/22 (ultimate + anthropic hooks) | Task 20 (event/logs) |
|---|---|---|---|---|---|
| Task 17 | — | tight (sole classifier consumer) | loose | tight (hooks call `IsRateLimitError`) | loose (reason field) |
| Task 18 | tight | — | tight (budget is a spawn precondition) | loose (same pattern, different path) | tight (event fires per spawn) |
| Task 19 | loose | tight | — | tight (hooks check the same budget) | tight (attempt_index) |
| Tasks 21/22 | tight | loose | tight | — (21↔22 loose: independent paths, shared pattern) | tight |
| Task 20 | loose | tight | tight | tight | — |

- **Tight with Phase 2 (Round 3):** Tasks 18/21/22 consume `Engine.ExcludeAndReselect` (phase2
  Task 12) and the cooldown semantics (phase2 Tasks 11/13); Task 20 reads `EngineStats.Failovers/
  Cooldowns` (phase2 Task 14). Signature drift breaks compile — contract wins
  (`technical-analysis.md` Round 3).
- **Tight with Phase 5 (Round 3):** Tasks 17-24's acceptance assertions are formalized as Phase 5
  Tasks 28-34 (precedence, budget, streaming guard, event payload).
- **Loose with Phase 4:** failover is runtime-only; no UI editor surface changes (phase4 Round 3
  note).

## Risks

> **AMENDED 2026-08-21**: risks updated to reflect W-1 (signature drift risk upgraded), W-2 (empty-key silent no-binding), A-1 (post-auth wiring site risk), A-2 (no multimodal fallback — must hash canonical JSON), W-3 (constructor-only; no second injection path), E-1 (janitor outer RLock), E-2 (filter-survivors invalidation).

| # | Risk | Impact | Likelihood | Mitigation |
|---|---|---|---|---|
| 1 | `credentiallb` package signature drift between Phase 2 and Phase 3 implementation — `GetOrSelect` returns the wrong shape (missing `newlyBound`) OR `ComputeConversationKey` is the 2-arg variant (no tokenID) | High — breaks compile AND silent event-flood (event fires per-request instead of per-first-binding) | Low (contract is frozen in `technical-analysis.md §API Contract` lines 1349-1373 changelog + amended §API Contract) | Phase 3 imports `pkg/credentiallb` exactly as documented. If drift surfaces in code review, the dispatcher reconciles via plan-overview update — Phase 3 worker does NOT modify `pkg/credentiallb` internals. |
| 2 | Forgetting to update a `NewHandler` call site in a non-obvious test file (e.g., a new test added since the contract was written) | Medium — `go build` fails in CI | Medium | Task 14 includes a grep-driven checklist: `grep -rn "proxy.NewHandler\|ultimatemodel.NewHandler" pkg/ cmd/ test/` before merge. Build + test gates catch the rest. |
| 3 | `req.conversationKey` field on `upstreamRequest` (the race-side struct) needs plumbing through `executeRequest`; missing it falls back to `""` which per W-2 silently disables affinity (no binding stored, fresh pick per request) | High — all multi-cred models behave like single-cred (defeats the feature) | Low | Add a unit test in Phase 5 that asserts `req.conversationKey == rc.conversationKey` after the engine call. Phase 3 task 4 includes a compile-time check: `req.conversationKey` is required (no zero-value fallback in production paths). W-2 empty-key is the EXPECTED behavior for no-first-user-message requests, not a bug — but a missing plumbing produces the same observable. |
| 4 | **A-1** — implementing the post-auth wiring site at the wrong place (e.g., back in `initRequestContext` where `rc.tokenID` is unset) results in `rc.conversationKey == ComputeConversationKey(modelID, "", firstUserMessage)` — anonymous unsalted key for ALL requests. Templated-agent skew returns. | High — silent LB defeat (the exact failure class A-1 was meant to prevent) | Low (contract is explicit on `:401+` site) | Code review checklist: `grep -n "ComputeConversationKey" pkg/proxy/handler.go` returns EXACTLY ONE call site at `handler.go:401+`, NONE in `handler_functions.go`. Phase 5 affinity test (two tokens, same message → independent bindings) catches regression. |
| 5 | `initRequestContext` extracting `firstUserMessage` for every request adds measurable CPU cost on hot path | Low (one canonical-JSON walk per request) | Low | Benchmark `pkg/proxy/handler_functions.go:23-131` (cite refreshed to 8f67bdf) before/after with `go test -bench BenchmarkInitRequestContext` (added in Phase 5); expected delta <5µs per request. Multimodal canonical-JSON hashing is the worst case (~20µs at 10KB content); document in commit message if delta exceeds 50µs. |
| 6 | **`cmd/main.go` shutdown ordering — REVISED 2026-08-21 (reviewer pass — #11)** — if the sequence is `srv.Shutdown(ctx)` → `credLB.Stop()` → `dbStore.Close()` is violated (e.g., `credLB.Stop()` runs before in-flight requests have drained), the engine is unreachable mid-request and `ResolveInternalConfigWithAffinity` returns `ok=false` → 500s. | Medium | Low | Order: `srv.Shutdown(ctx)` first (drains HTTP connections), THEN `credLB.Stop()` (stops engine janitor), THEN `dbStore.Close()` (closes DB). The defers are registered in reverse order (defer `dbStore.Close()` first, then defer `credLB.Stop()` BEFORE the shutdown signal handler so LIFO fires them in the right order). Phase 5 shutdown integration test asserts the order end-to-end. |
| 7 | The `getEffectivePrimaryCredentialID` shim is missed by the ultimate-external provider probe path, breaking ultimate-external for models that only have `Credentials` populated | Medium | Low | The shim is a free function in `pkg/ultimatemodel/handler.go` and is the single point of truth; unit test in Phase 5 verifies both `Credentials == nil` and `len(Credentials) == 1` cases. |
| 8 | **W-1 — REVISED 2026-08-21 (reviewer pass):** `model_credential_selected` event flood: implementer falls back to per-request publish because the `NewlyBound` plumbing is hard, instead of removing the bool heuristic | Medium — event bus saturation, SSE feed noise, observability collapse | Medium | Task 6 mandates removal of the per-request `bindingEmitted` bool; the engine's `NewlyBound` is the SOLE signal. Code review: `grep -n "bindingEmitted" pkg/proxy/` returns ZERO hits in committed code. Phase 5 unit test `TestHandler_EngineEventPublish_OncePerFirstBinding` asserts event count == 1 for 5 affinity-pinned requests (NOT 5). |
| 8a | **C2 — NEW 2026-08-21 (reviewer pass):** implementer incorrectly publishes `model_credential_selected` for empty-key fresh-pick requests (the prior task text implied `newlyBound=true` on empty-key picks — WRONG per C2). The contract pins `newlyBound ⇔ a binding was stored on this call`; empty-key picks store NO binding → `NewlyBound=false` → event NOT published. | Medium — empty-key workloads (templated agents, system-only messages) flood the SSE feed; observability collapse | Medium | Task 6 explicitly states empty-key fresh-pick requests do NOT emit the event. Phase 5 test `TestHandler_EngineEventPublish_EmptyKey_NeverFires` (renamed from prior `_FiresPerRequest`) asserts 0 events for 5 empty-key requests. **Contract wins**: any prior text implying `newlyBound=true` on empty-key picks is superseded by C2. |
| 9 | The new 7th-arg signature change ripples through every downstream consumer (`cmd/main.go` is the only production caller, but tests are many) | Low — mechanical update | Medium | Task 14 is a dedicated sweep-and-update task with a grep-driven checklist. Build + test gates enforce zero stragglers. |
| 10 | **W-3 — REVISED 2026-08-21 (reviewer pass — #16a):** implementer adds `Config.CredEngine` field back as a second injection path ("defensive convenience") | Medium — two ways to disagree (constructor vs. config); testing gap when only one is set | Low | Code review: `grep -n "CredEngine" pkg/proxy/handler.go` returns ONLY the unexported `Handler.credEngine` (lowercase) field; no `Config.CredEngine`. Phase 2 ships constructor-only; Phase 3 honors that. The naming convention (`Handler.credEngine` field vs. `credLB` local in main.go) is stated once and applies throughout — no mixing. |
| 11 | **W-2** — empty `conversationKey` requests silently disable affinity; if a deployment has many empty-first-message requests, LB distribution looks broken (every request re-rolls) | Low — by design (W-2 spec); no functional break | Low | DEBUG log at the post-auth wiring site (`[LB] empty conversationKey for modelID=%s`) surfaces the rate; Phase 5 verifies the empty-key path explicitly. Documented in engine doc.go and Phase 5 README. **C2 — REVISED**: empty-key requests do NOT publish `model_credential_selected` (no binding stored → `NewlyBound=false`). |
| 12 | **E-1** — janitor outer-write-lock regression (the original contradiction) re-introduced if implementer follows phase2's older task text | High — every sweep stalls all reads for its full duration | Low | Code review: `grep -n "engine.mu.Lock\|e.mu.Lock" pkg/credentiallb/engine.go` in the janitor path returns ONLY `RLock` on the outer lock; per-model locks may be write locks. Phase 5 race-detector test catches if a janitor sweep races with a request. |
| 13 | **E-2** — `OnModelChanged` clear-all regression (flushing all bindings on weight nudge) re-introduced | Medium — operator nudging weight 1→2 loses affinity for every active conversation | Low | Code review: `grep -n "OnModelChanged" pkg/credentiallb/engine.go` shows filter-survivor logic, not clear-all. Phase 5 test asserts bindings are preserved across weight nudge. |
| 14 | **C1 — NEW 2026-08-21 (reviewer pass):** Anthropic `/v1/messages` internal path is silently skipped (implementer only wires the chat-completions path) | High — Claude Code (the primary client endpoint) loses LB; `Engine.Stats()` shows the chat path receiving all the load | Medium | Task 16 mandates the swap at `pkg/proxy/internal_handler.go:111` (was 69 @ fea5874); Exit criterion grep-guard `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0. Phase 5 has a handler-level integration test (`TestHandler_AnthropicMessagesAffinity`) and an E2E scenario (`test_mock_credential_lb.sh` Test 8 — `/v1/messages` affinity). |
| 15 | **#7 — NEW 2026-08-21 (reviewer pass):** implementer re-introduces `credLB := credentiallb.NewEngine(...)` + a rebind loop in `cmd/main.go`, contradicting Dispatcher Ruling #2 (ModelsManager owns the engine; main.go only OBTAINS it). Result: two engine instances (one in main.go, one in ModelsManager) and inconsistent state. | High — silent state divergence; one engine sees bindings the other doesn't; observability collapses | Low | Code review: `grep -n "credentiallb.NewEngine" cmd/main.go` returns ZERO matches. `grep -n "RebindFromStore" cmd/main.go` returns ZERO matches. Phase 2 ships the engine accessor (`ModelsManager.Engine()`); main.go consumes it as `credLB := modelsMgr.Engine()`. |
| 16 | **#8 — NEW 2026-08-21 (reviewer pass):** mock guidance (line 102 in the prior text) still says seed `CredentialID` on the mock `ModelConfig`. Post-Phase-1 the `ModelConfig.CredentialID` field is GONE — `Credentials []models.CredentialRef` is the single source of truth. If mocks seed the old field, the test code compiles only because Go is permissive on unexported names, but the runtime behavior silently skips the LB path. | Medium — tests pass for the wrong reason; engine path is untested | Medium | Files-table row pins `mockModelsConfig` to seed `Credentials: []models.CredentialRef{...}` using Phase 1's `TestRefs` fixture factory (referenced by package — `models.TestRefs(creds...)`). Phase 5 tests use the factory. |
| 17 | **Round 3 (R3-3)** — interception branch mis-orders precedence: implementer spawns model fallback first (or both), defeating same-provider failover | High — the user-facing requirement (same-provider HA before model switch) silently absent | Medium | Task 18 pins the pre-spawn condition order (credential-first, fallback only on budget-exhaustion/no-healthy-credential); Phase 5 Task 28 asserts 429-on-multi-cred spawns ZERO model fallbacks before credential exhaustion; exit grep-gate #14 |
| 18 | **Round 3 (R3-5)** — budget accounting leaks into the engine (per-request state in engine maps) — memory growth + cross-request contamination | Medium — wrong-credential exclusion, unbounded map growth | Low | Task 19 pins accounting to `requestContext` (`triedCredentialIDs`, `failoverAttempts`), cleared in `reset()`; Phase 2 explicitly excludes per-request tracking; Phase 5 Task 32 |
| 19 | **Round 3 (R3-6)** — a failover respawn fires AFTER first byte (streaming started) — duplicated/corrupt client output | High — visible wire corruption | Low (race path inherently safe; hooks are pre-write) | Task 23 pins first-byte markers per path (race: post-winner only, `handler.go:960/1327`; ultimate: first ResponseWriter write; anthropic: SSE header at `adapter_anthropic.go:170-176`) + guard conditions at every hook; Phase 5 Task 33 |
| 20 | **Round 3 (R3-7)** — implementer "helpfully" adds failover to the race-external env path or ultimate-external passthrough | Medium — env-credential semantics change; scope creep against pinned ruling | Medium | Task 24 documents the no-change scope; exit grep-gate #16 asserts zero failover symbols in `race_executor.go:279-300` region + ultimate-external region |

## Exit Criterion

> **Round 3j re-baseline (2026-08-25 — Phase-3 Review C1 root-cause, W5):** the Phase-3 exit-grep
> pins are re-baselined with three gate SEMANTICS; this preamble carries the rule — individual
> gates keep their wording unless directly contradicted:
> 1. **Code-only semantics** — a grep gate counts hits in CODE only. Matches inside comments
>    (e.g. the mandated `// mutated only from manage()` comment, doc comments, gate-documenting
>    prose) do NOT count toward or against a gate unless the gate explicitly targets comments —
>    the (h)-style comment-presence gates DO target comments and are kept as-is (intentional).
> 2. **Line-wrap-proof patterns** — gates pin patterns that survive gofmt line-wrapping: grep for
>    the identifier (e.g. `credFailoverEligibleWithBudget`), NOT the full gate expression
>    (`gate := modelAttempts < len(models) || ...` wraps). Multi-token expression greps are
>    ADVISORY; identifier greps are the gate.
> 3. **Strict-zero purge gates** — the wrong-symbol purge gates (`monitorLoop|spawnAttempt`,
>    `modelTypeCredentialFailover`, `chunksFlushedToClient|bytesFlushedToClient`, the old
>    `(string, bool)` signature shape) pin ZERO hits STRICTLY — no expected-hit-count looseness;
>    any hit (code or comment, per the gate's definition) fails the gate.
> Pre-Round-3j "N/N passing" readings that counted comment hits, or relied on wrap-vulnerable
> expression greps, are superseded by this preamble.

Phase 3 is complete when:
1. `go build ./...` and `go vet ./...` pass with zero warnings.
2. `go test ./pkg/proxy/... ./pkg/ultimatemodel/... ./pkg/store/database/... ./pkg/ui/...` all pass with `go test -race` — no behavior regression for single-credential models (byte-identical to pre-Phase-3 baseline; verified by snapshot test or wire-level equality in Phase 5).
3. `cmd/main.go` starts cleanly, logs `[INFO] credentiallb: bound N models`, and serves `/v1/chat/completions` with single-credential models producing identical responses to today (manual smoke test).
4. **W-1 + C2 — REVISED 2026-08-21 (reviewer pass):** The `model_credential_selected` event appears exactly once per unique `(modelID, conversationKey)` pair (verified by `req.resolved.NewlyBound == true` propagation end-to-end per #3 — struct field, NOT 6th tuple element) in an event-sink unit test (Phase 5). **Empty-key requests (W-2 + C2) emit ZERO events** — the engine returns `NewlyBound=false` because no binding was stored; the event publish is gated on `NewlyBound=true`.
5. `getEffectivePrimaryCredentialID` helper is in place; ultimate-external path still resolves the provider from the primary credential.
6. The shutdown path logs `[INFO] credentiallb: janitor stopped` and exits cleanly under SIGINT (verified by Phase 5 integration test that asserts `srv.Shutdown(ctx)` returns BEFORE `credLB.Stop()` runs, which runs BEFORE `dbStore.Close()` — the LIFO defer order is correct per #11).
7. **A-1** — `grep -n "ComputeConversationKey" pkg/proxy/handler.go` returns EXACTLY ONE call site at `handler.go:401+` (the post-auth wiring site), and `grep -n "ComputeConversationKey" pkg/proxy/handler_functions.go` returns ZERO hits (NOT in `initRequestContext`). The token-salted key is never computed pre-auth.
8. **E-3** — `Engine.Stats().Bindings(modelID) == 0` after N single-credential requests (no map writes on the fast path); asserted by Phase 5 test.
9. **W-3** — `grep -n "CredEngine" pkg/proxy/handler.go` returns ONLY the unexported `Handler.credEngine` field — no `Config.CredEngine` second injection path.
10. **C1 — NEW 2026-08-21 (reviewer pass):** `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns ZERO matches in this file (only `ResolveInternalConfigWithAffinity` remains after the swap at line 69). `grep -n "ResolveInternalConfig(" pkg/proxy/handler_anthropic.go` returns ZERO matches in the `/v1/messages` internal path (the Anthropic file routes through `doAnthropicInternalRequest` → `NewInternalHandler` → `HandleRequest` — none call `ResolveInternalConfig` directly). Anthropic-shaped `/v1/messages` requests with the same `(tokenID, firstUserMessage)` land on the same upstream credential (Phase 5's `TestHandler_AnthropicMessagesAffinity` + E2E Test 8 assert this end-to-end).
11. **#7 — NEW 2026-08-21 (reviewer pass):** `grep -n "credentiallb.NewEngine" cmd/main.go` returns ZERO matches; `grep -n "RebindFromStore" cmd/main.go` returns ZERO matches. Main.go obtains the engine via `credLB := modelsMgr.Engine()` (single line). ModelsManager init (Phase 2) owns engine construction + rebind.
12. **#8 — NEW 2026-08-21 (reviewer pass):** `grep -n "CredentialID" pkg/ultimatemodel/handler_test.go` returns ZERO matches on `ModelConfig.CredentialID` seed lines (only `getEffectivePrimaryCredentialID` helper references and `TestModelRequest.CredentialID` may remain). Mocks seed `Credentials: []models.CredentialRef{...}` via Phase 1's `TestRefs` factory.
13. **Round 3 (R3-1)** — `grep -n "IsRateLimitError" pkg/providers/` returns the definition and its openai.go wiring; `grep -n "parseRetryAfter" pkg/providers/openai.go` shows ≥1 NON-definition hit (the dead parser is wired); classifier unit matrix green.
14. **Round 3 (R3-2/R3-3)** — `grep -n "modelTypeCredFailover" pkg/proxy/` returns the `race_request.go` constant + the `race_coordinator.go` Case 1 spawn; `grep -rn "ExcludeAndReselect" pkg/proxy/ pkg/ultimatemodel/` returns ≥3 hook call sites (race-internal coordinator, ultimate-internal, `/v1/messages` internal).
15. **Round 3 (R3-8)** — `grep -rn "model_credential_failover" pkg/` returns the publish site(s) with payload keys `{model_id, from_credential_id, to_credential_id, reason, retry_after_ms, cooldown_ms, attempt_index}`; `grep -rn "LB-FAILOVER" pkg/` returns the INFO/WARN log sites.
16. **Round 3 (R3-7 scope guard)** — `grep -n "ExcludeAndReselect\|IsRateLimitError\|modelTypeCredFailover" pkg/proxy/race_executor.go` shows ZERO hits in the external-path region (279-300; hits allowed only in the internal resolution/plumbing regions); the ultimate-external region of `pkg/ultimatemodel/handler.go` shows no failover references.
17. **Round 3b (B1) — wrong-symbol + cite regression grep-gates** — `grep -rn "monitorLoop\|spawnAttempt\|modelTypeCredentialFailover" pkg/` returns ZERO hits; `grep -rn "chunksFlushedToClient\|bytesFlushedToClient" pkg/` returns ZERO hits; `grep -rn "adapter_anthropic.go:170-176" pkg/` returns ZERO hits as a first-byte cite; `grep -n "manage()\|spawn()" pkg/proxy/race_coordinator.go` returns both function symbols; `grep -n "modelTypeCredFailover" pkg/proxy/race_request.go pkg/proxy/race_coordinator.go` returns the canonical constant + the Case-1 spawn ONLY (NO hits in the `:338` window check or the `:420-421` all-failed check — B1 separate accounting verified); `grep -n "triggerRateLimit\|triggerMainError" pkg/proxy/race_coordinator.go` returns both trigger constants; `grep -n "// mutated only from manage" pkg/proxy/` returns the mandated single-goroutine-mutation comment at both field declarations + mutation sites; `grep -n "ResolveInternalConfigWithAffinity" pkg/proxy/handler_anthropic.go` returns **≥2 sites** (internal-handler construction + the new passthrough-branch site per leader ruling (i)); `grep -n "headersSent" pkg/ultimatemodel/handler_internal.go` returns ZERO hits as a guard (the guard is `*ProviderError` per review #4); the `/v1/messages` first-byte cite is `handler_anthropic.go:648-654` (NOT `adapter_anthropic.go:170-176`).
18. **Round 3b (B2/B3) — mode-aware signature** — `grep -n "ReselectMode\|ReselectHealthy\|ReselectSoonestExpiry\|ReselectNone" pkg/credentiallb/engine.go pkg/proxy/race_coordinator.go pkg/ultimatemodel/handler_internal.go pkg/proxy/internal_handler.go pkg/proxy/handler_anthropic.go` returns the enum + ≥4 call sites (one per hook — race coordinator Case 1 + ultimate-internal + `/v1/messages` internal + the new passthrough-branch site per leader ruling (i)); the Round-3 `(credentialID, ok)` signature is GONE **(Round 3j: strict-zero purge — the old `(string, bool)` / `(credentialID, ok)` signature shape admits ZERO hits, code or comment, no expected-hit-count looseness)**.

---

## Amendment Changelog (2026-08-21 — leader rulings)

| Ruling | Section(s) amended | What changed |
|---|---|---|
| **A-1** | Objective; Context; Scope #3; Files (`handler.go:401+`, `handler_functions.go`); Tasks #2, #2a; Coupling; Risks #4 | Key formula is now `sha256(modelID + "|" + tokenID + "|" + firstUserMessage)`. The key is **NOT** computed in `initRequestContext` (where `rc.tokenID` is unset at `handler.go:352`; cite refreshed to 8f67bdf) — it is computed at the **NEW post-auth wiring site** (`handler.go:401+`), after `rc.tokenID` is populated. Anonymous/unauthenticated requests degrade to unsalted key (graceful). Task #2a added to make the wiring site explicit. |
| **A-1-wiring** | Context; Files (`handler.go:401+`); Tasks #2, #2a; Risks #4 | `initRequestContext` caches ONLY `rc.firstUserMessage` (cheap map walk). The token-salted key is computed POST-AUTH. The original contract's "compute in `initRequestContext`" was unimplementable; this amendment corrects it. |
| **A-2** | Context; Scope (logging #8); Tasks #2, #15 | All requests get affinity — no multimodal no-affinity fallback. `ExtractFirstUserMessage` returns canonical-JSON hash of multimodal `[]interface{}` content (precedent `hash_cache.go:172-186`); deterministic canonical hash, same conversation pins to same credential. |
| **W-1** | Context; Objective; Scope #6, #9; Files (`race_executor.go`, `handler.go:executeRequest`); Tasks #4, #6; Coupling; Risks #1, #8 | `GetOrSelect` returns `(credentialID, newlyBound, err)`. `ResolveInternalConfigWithAffinity` propagates `newlyBound` upstream (6th return or side-channel). **[SUPERSEDED by Round-2 #3: struct `ResolvedCredential` field `NewlyBound` — see Round-2 appendix.]** `model_credential_selected` event fires ONLY when `newlyBound=true`. The per-request bool `bindingEmitted` heuristic is REMOVED — it was unimplementable as a first-binding detector (only detected first-selection-within-a-request, not first-binding-ever). |
| **W-2** | Context; Scope #8 (logging); Tasks #4, #6; Risks #11 | Empty `conversationKey == ""` ⇒ NO binding stored, fresh weighted pick per request. The ""-as-own-bucket reading is REMOVED. Phase 3 logs at DEBUG when key is empty; does NOT crash; does NOT skip the engine call. Fresh picks emit `model_credential_selected` on `newlyBound=true`. |
| **W-3** | Context; Files (`handler.go` Config); Tasks #3; Risks #10 | `Config.CredEngine` field is **REMOVED** from the contract. Constructor-only injection is the law. The 7th `NewHandler` constructor arg is the SOLE injection path; `main.go` passes `modelsMgr.Engine()`. |
| **E-1** | Tasks #13; Risks #12 | Janitor outer lock is `RLock` (reads proceed concurrently); per-model write locks taken one at a time. The original "outer write lock" self-contradiction is resolved. |
| **E-2** | Tasks #11; Risks #13 | `OnModelChanged` filters bindings (preserve survivors; drop orphans) — does NOT clear-all. Routine weight nudges preserve cache affinity. |
| **E-3** | Tasks #14; Exit #8; Risks (coupling note) | Single-credential fast path: NO binding map writes. Byte-identical to today. Assertable via `Engine.Stats()` or internal map size (Phase 5). |
| **E-4 (recommended)** | Tasks #15; Risks (new #5 perf note) | Per-model RNG (or `math/rand/v2`); `Engine.Stats()` accessor; janitor panic-recovery (`defer recover()` + continue). Non-blocking recommendations. |
| **M-1** (cross-ref to Phase 1) | Coupling (Phase 1 note) | DB column `credential_id` is kept as a derived shadow (written by Phase 1's migration). Phase 4's UI/API surface still drops `credential_id` from the JSON wire — the shadow is invisible to the UI. |

---

## Round 2 — Reviewer Punch-List (2026-08-21)

> **REVISED 2026-08-21 (reviewer pass):** The seven items below are
> the surgical amendments from the reviewer punch-list. Each row ties
> to a specific section above that now carries a
> `> **REVISED 2026-08-21 (reviewer pass — …):**` blockquote. **Contract
> wins**: the pins below are downstream law — Task 4/6/9/10/13/16, the
> Files-table rows for `race_executor.go` / `internal_handler.go` /
> `handler_anthropic.go` / `cmd/main.go`, the Exit-criteria grep-guards,
> and the operator-facing surfaces MUST mirror the shapes pinned here.

| # | Item | Section(s) updated | One-line summary |
|---|------|---------------------|------------------|
| **C1** | `/v1/messages` internal path in scope | Objective; Context (contract names); Scope #11 (NEW); Files (`pkg/proxy/internal_handler.go`, `pkg/proxy/handler_anthropic.go`); Tasks #16 (NEW); Risks #14; Exit Criterion #10 | Anthropic `/v1/messages` is the **PRIMARY client endpoint** (Claude Code). Verified chain: `cmd/main.go:139` (route) → `handler_anthropic.go:348` (`doAnthropicInternalRequest` call) → `handler_anthropic.go:455` (function def) → `handler_anthropic.go:478` (`NewInternalHandler` construction) → `pkg/proxy/internal_handler.go:109` (`HandleRequest` signature) → `internal_handler.go:111` (5-tuple `ResolveInternalConfig` seam to swap) — cites refreshed to 8f67bdf (Round 3); fea5874-era: 340/447/461/67/69. Thread `anthropicRequestContext.conversationKey` (NEW field, struct at `handler_anthropic.go:740`; was 697 @ fea5874) → `HandleRequest` extra arg → `ResolveInternalConfigWithAffinity`. Key computation follows A-1: compute AFTER the Anthropic request is authenticated at the equivalent of `handler.go:401+` (the Anthropic path's auth-resolution site inside `HandleAnthropicMessages`). Grep-guard: `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0. |
| **C2** | Empty-key `NewlyBound=false` invariant pinned | Context (W-2 + C2 line); Scope #9 (Event publish — was: "Empty `conversationKey` STILL trigger the event on `newlyBound=true`" → NOW: empty-key picks NEVER emit the event); Tasks #4, #6 (acceptance: "Empty-key requests emit ZERO events"); Risks #8a (NEW); Exit Criterion #4 | `conversationKey == ""` ⇒ fresh weighted pick per call, **NO binding stored**, engine returns `NewlyBound = false`. The caller does **NOT** publish `model_credential_selected` (the binding-store event). Phase 3 owns a DEBUG log at the post-auth wiring site for empty-key fresh-pick observability (Scope #8 — that IS the canonical C2 observability mechanism). **Contract wins**: prior text implying `newlyBound=true` on empty-key picks is SUPERSEDED. |
| **#3** | `ResolveInternalConfigWithAffinity` return shape pinned (struct form) | Context (contract names); Scope #4 (return shape), #9 (single source of truth for event publish); Files (`race_executor.go` 100-138, `handler_internal.go` 36-49, `handler.go:executeRequest`); Tasks #4, #6, #9; Coupling | **Struct `ResolvedCredential` + trailing `ok bool`** (NOT a 6-tuple). `ResolvedCredential{Provider, APIKey, BaseURL, InternalModel, CredentialID, NewlyBound}` + trailing `ok`. The race coordinator reads `req.resolved.NewlyBound` (struct field) and `req.resolvedOK` (bool) — NOT a 6-tuple element. Mock guidance: `mockModelsConfig.ResolveInternalConfigWithAffinity` returns the struct; `NewlyBound` settable per-test via a helper (e.g., `mockModelsConfig.SetNewlyBoundForTest(modelID, bool)`). |
| **#7** | `cmd/main.go` does NOT construct the engine | Scope #7 (cmd/main.go block); Files (`cmd/main.go` 47-107, 199-215); Tasks #10, #11, #12; Risks #15 (NEW); Exit Criterion #11 | Dispatcher Ruling #2 (ModelsManager owns the engine; main.go only OBTAINS it). DELETE the `credLB := credentiallb.NewEngine(...)` constructor and the rebind loop. Replace with: `credLB := modelsMgr.Engine()` (single line). Rebind happens inside ModelsManager init (Phase 2's job). Grep-guards: `grep -n "credentiallb.NewEngine" cmd/main.go` returns 0; `grep -n "RebindFromStore" cmd/main.go` returns 0. |
| **#8** | Mock guidance stale: post-Phase-1 `CredentialID` field is GONE | Files (`pkg/ultimatemodel/handler_test.go`, `pkg/ultimatemodel/handler_internal_test.go`); Risks #16 (NEW); Exit Criterion #12 | `mockModelsConfig.AddInternalModel` (and any seeded test fixture) must NOT seed `ModelConfig.CredentialID` — that field is GONE post-Phase-1. Mocks/fixtures seed `Credentials: []models.CredentialRef{...}` using Phase 1's `TestRefs` fixture factory (referenced by package — `models.TestRefs(creds...)`; Phase 1 pins the factory signature in its plan). The new method returns the struct with `NewlyBound` settable per-test. |
| **#11** | Shutdown sequence pinned: `srv.Shutdown` → `credLB.Stop` → `dbStore.Close` | Scope #7 (cmd/main.go shutdown text); Files (`cmd/main.go` 199-215); Task #13; Risks #6; Exit Criterion #6 | Order matters. Implementation: `defer dbStore.Close()` registered first (existing), then `defer credLB.Stop()` BEFORE the shutdown signal handler — LIFO fires them in the correct order: `srv.Shutdown(ctx)` returns → `credLB.Stop()` → `dbStore.Close()`. Explicit `if credLB != nil { ... }` guards (nil-safe). Phase 5 shutdown integration test asserts the order. |
| **#16a (cross-item)** | Naming convention `Handler.credEngine` (field) vs. `credLB` (main.go local) | Context (Task #3 field name); Files (`cmd/main.go` 47-107); Tasks #3, #10; Risks #10 | Convention stated once: `Handler.credEngine *credentiallb.Engine` (unexported, lowercase `c`); `credLB := modelsMgr.Engine()` (local in main.go, after the #7 fix). Applies throughout; no mixing. |

---

## Round 3 — Rate-Limit Failover (2026-08-25)

> **AMENDED 2026-08-25 (Round 3):** this file gains (a) the rate-limit credential-failover wiring
> tasks (R3-1..R3-8 translated; contract in `decisions.md` + `technical-analysis.md` "Round 3"
> sections by the sibling worker — cited, not duplicated; **contract wins on any conflict**), and
> (b) base-drift cite corrections for the fea5874 → 8f67bdf rebase (onto master 396fd12; 11
> intervening commits on `pkg/proxy/` + `pkg/ultimatemodel/`). Settled rulings (A-1, M-1, C1, C2,
> W-1..W-3, E-1..E-4, #3, #10, #13) are NOT re-litigated — Round 3 adds behavior on top (rebind
> refreshes `boundAt` per #10; cooldown never touches it).

### Round 3 changelog — failover tasks (NEW)

| Task | Ruling | Summary |
|------|--------|---------|
| 17 | R3-1 | Classifier in `pkg/providers`: `IsRateLimitError` + `ProviderError.RetryAfter`; wires the DEAD `parseRetryAfter` (`openai.go:593`, zero callers — conflict-free) into the 429 path (~255) |
| 18 | R3-3 | `race_coordinator.go` `manage()` Case 1 (`:350-364`) pre-spawn branch (Round-3 text used the wrong names `monitorLoop` / `spawnAttempt` — **PURGED Round 3b**): internal + multi-cred + rate-limited + no client bytes + budget ⇒ `ExcludeAndReselect(...)` (Round-3b signature with `ReselectMode` return) + `modelTypeCredFailover` (canonical Go identifier; `modelTypeCredentialFailover` variant **PURGED Round 3b**) same-model spawn INSTEAD of `modelTypeFallback` (`c.models[1]` at `~:215`); `modelTypeCredFailover` rows EXEMPT from the `:338` window count + `:420-421` all-failed accounting per B1; terminal shape at `:860-868` UNCHANGED |
| 19 | R3-5 | Retry budget in `requestContext`/`upstreamRequest` (`triedCredentialIDs`, `failoverAttempts`; ≤ len(credentials)−1; each remaining credential once; cleared in `reset()`) — request context, NOT engine |
| 20 | R3-8 | `model_credential_failover` event `{model_id, from_credential_id, to_credential_id, reason:"rate_limit", retry_after_ms, cooldown_ms, attempt_index}` + `[LB-FAILOVER]` INFO/WARN logs + `EngineStats.Failovers/Cooldowns` surfacing |
| 21 | R3-3 | Ultimate-internal hook (`handler_internal.go:36-49`) — same pattern; must not conflict with thinking-sink invariant (`internal_handler.go:72-80`) / bare-JSON capture fallback |
| 22 | R3-3 | `/v1/messages` internal hook at the `InternalHandler` seam (`internal_handler.go:109-111`; Anthropic chain 348→455→478) |
| 23 | R3-6 | Streaming-constraint guard: failover only pre-first-byte; first-byte markers pinned per path (race: post-winner `handler.go:960/1327`; ultimate: first ResponseWriter write; anthropic: SSE header `adapter_anthropic.go:170-176`) |
| 24 | R3-7 | Explicit no-change notes: race-external (`race_executor.go:279-300`) + ultimate-external UNCHANGED; scope = 3 LB'd internal paths only |

Files table += 8 Round-3 rows (`providers/interface.go`, `providers/openai.go`, `race_request.go`,
`race_coordinator.go`, `handler_helpers.go`, `race_executor.go`, `ultimatemodel/handler_internal.go`,
`internal_handler.go`+`handler_anthropic.go`); Coupling += Round-3 task matrix; Risks += #17-#20;
Exit Criterion += #13-#16 (grep-gates for classifier wiring, hook sites, event payload, scope guard).

### Round 3 changelog — cite corrections (fea5874 @ old → 8f67bdf @ new; all verified against the working tree)

| Site | Old cite | New cite | Where in this file |
|---|---|---|---|
| doAnthropicInternalRequest call | `handler_anthropic.go:340` | `handler_anthropic.go:348` | header, Context chain, Round-2 C1 row |
| doAnthropicInternalRequest func def | `handler_anthropic.go:447` | `handler_anthropic.go:455` | header, Context chain, Objective, Round-2 C1 row |
| NewInternalHandler construction | `handler_anthropic.go:461` | `handler_anthropic.go:478` | header, Context chain, Scope #11, Task 16, Round-2 C1 row |
| anthropicRequestContext struct | `handler_anthropic.go:697` | `handler_anthropic.go:740` | Context chain, Scope #11, Task 16, Round-2 C1 row |
| CredentialID probe (phase1 cite) | `handler_anthropic.go:294` | `handler_anthropic.go:302-303` | (corrected in phase1-plan.md; listed here for the drift table) |
| NewInternalHandler | `internal_handler.go:38` | `internal_handler.go:48` | Scope #11, Files table |
| HandleRequest signature | `internal_handler.go:67` | `internal_handler.go:109` | header, Context chain, Scope #11, Task 16, Files table, Round-2 C1 row |
| ResolveInternalConfig seam | `internal_handler.go:69` | `internal_handler.go:111` | header, Context chain, Scope #11, Task 16, Risk 14, Round-2 C1 row |
| initRequestContext call | `handler.go:353` | `handler.go:352` | Context bullet, Scope #3, Round-1 changelog A-1 |
| initRequestContext span | `handler_functions.go:23-126` | `handler_functions.go:23-131` | Context bullet, Files table |
| ultimate Execute call site | `handler.go:627` | `handler.go:635` | Scope #6, Files table (2 rows), Task 8 |
| ultimate-external provider probe (D3) | `ultimatemodel/handler.go:287-293` | `ultimatemodel/handler.go:625-635` (**MAJOR +337** — probe relocated into `Execute`) | Context bullet, Scope #5, Files table, Task 7 |
| Verified exact (unchanged) | — | `handler.go:401` (tokenID), `race_executor.go:73/112/137/151`, `ultimatemodel/handler_internal.go:36/46`, `cmd/main.go:139`, `handler_helpers.go:23-108`, `handler_internal.go:29-44` (interleaved doc) | kept as-is |

**New code since fea5874 that Round-3 wiring must not conflict with** (verified present @ 8f67bdf):
`SetThinkingSink` + thinking-sink invariant (`internal_handler.go:72-80`); bare-JSON capture
fallback + D3 probe relocation (`ultimatemodel/handler.go`); `extractNonStreamContent`
(`handler_helpers.go:258`); SSE `Cache-Control: no-cache, no-transform` (`adapter_anthropic.go:170-176` — **Round 3b amend**: this is the unrelated `SetStreamHeaders` helper; the `/v1/messages` first-byte header write is at `handler_anthropic.go:648-654` per Task 22/23 fixes).

---

## Round 3b — Architect Review Amendments (2026-08-25)

> **AMENDED 2026-08-25 (Round 3b):** this phase file is amended in place per the architect
> stress-test (`architecture-review-round3.md`) + 3 leader rulings. **R3-1..R3-8 semantics
> STAND.** This phase's structural fix is **B1 (spawn-window gate exemption)**; the other
> amendments (B2/B3, B3 corollary) live in phase2 and are CONSUMED here via the
> Round-3b `ExcludeAndReselect` signature. The Round-3 invented-guard-state (`chunksFlushedToClient` /
> `bytesFlushedToClient` / `headersSent` as ultimate marker) is **PURGED** in Task 23 + Task 21
> + Task 22. The Round-3 cite conflation (`adapter_anthropic.go:170-176` as the first-byte
> header write) is **FIXED** to `handler_anthropic.go:648-654`. The Round-3 wrong symbols
> (`monitorLoop` / `spawnAttempt` / `modelTypeCredentialFailover`) are **PURGED**. The
> **`handler_anthropic.go:297-345` anthropic-passthrough internal branch** is pulled INTO
> failover scope per leader ruling (i). Contract (`decisions.md` + `technical-analysis.md`
> Round 3 sections by the sibling contract worker) wins on any conflict.

| # | Amendment | What changed in this file |
|---|-----------|---------------------------|
| **B1 (Review #1)** | Spawn-window gate exemption + separate accounting for `modelTypeCredFailover` attempts; the user's core scenario (3 creds on one model, no fallback chain) gets the FULL failover chain | **Task 18 (AMENDED in place)**: B1 separate-accounting mechanism pinned (internal counter `credFailoverAttempts` increments per `modelTypeCredFailover` spawn; the `:338` window check and the `:420-421` all-failed check do NOT see `modelTypeCredFailover` rows; the cap-extension alternative is REJECTED-FOR-CLARITY); all-failed condition amended to "all fallback models tried OR credential budget exhausted with no candidate remaining"; terminal `:860-868` MUST NOT fire while an untried fallback model or credential remains; after credential exhaustion, fall through to existing `modelTypeFallback` (`c.models[1]`) when it exists; **acceptance criterion += both rescue scenarios** (a) 3-cred single-model full chain (b) 2-entry model list fall-through to `c.models[1]` after credential exhaustion. **Files row `race_coordinator.go`** rewritten: function names corrected (`spawn()` at `:189`, `manage()` at `:252` — Round-3 text's `spawnAttempt` / `monitorLoop` are PURGED). **Risk #16a (NEW)** in plan-overview captures the residual risk. |
| **B2 (Review #2)** | `ExcludeAndReselect` rebind-only-if-current-cred-matches precondition (idempotent under concurrent same-conversation 429s) | **Task 18 (AMENDED in place)**: consumes the Round-3b `ExcludeAndReselect(modelID, conversationKey, excludedCredID, retryAfter) (credentialID, mode ReselectMode)` signature from phase2 Task 12 — when `mode == ReselectHealthy` proceed; `mode == ReselectSoonestExpiry` proceed ONCE with soonest-expiring credential; `mode == ReselectNone` fall through to model-fallback unchanged. (The precondition itself is phase2's engine-side implementation; phase3 consumes the mode-aware return shape.) |
| **B3 (Review #3)** | `ExcludeAndReselect` mode-aware return `(credentialID, mode ReselectMode)` with `mode ∈ {ReselectHealthy, ReselectSoonestExpiry, ReselectNone}` | **Task 18 (AMENDED in place)**: per-mode branching at the spawn site — see B2 row above for the mode-handling detail. **Plan-overview Scope + Risks #16b** carry the cross-file change. |
| **Review #4 (real guards)** | Replace invented/nonexistent guard state with real ones | **Task 21 (AMENDED in place)**: ultimate-internal guard corrected to **initial-call `*ProviderError` type assertion** (`errors.As(err, &providerErr)` where `providerErr *providers.ProviderError`); the Round-3 `headersSent` marker is UNUSABLE because `*headersSent = true` is pre-set at `ultimatemodel/handler.go:608-609` and SSE heartbeat starts at `:643` BEFORE `executeInternal` at `:649-655`; acceptance criterion += grep-gate `grep -n "headersSent" pkg/ultimatemodel/handler_internal.go` returns ZERO hits as a guard. **Task 22 (AMENDED in place)**: `/v1/messages` first-byte cite FIXED to **`handler_anthropic.go:648-654`** (NOT `adapter_anthropic.go:170-176` which is `SetStreamHeaders`); `streamResult` call `:852` / def `:960` disambiguated in Files table row. **Task 23 (AMENDED in place)**: race-path guard pinned to `c.winner == nil` with corrected wording "no CONTENT flushes pre-winner (framing headers + `: connected\n\n` precede `coordinator.Start()` per `handler.go:783-797` — carry no model content)"; PURGE `chunksFlushedToClient` / `bytesFlushedToClient` references (vacuous — neither field nor method exists at HEAD; `grep -rn "chunksFlushedToClient\|bytesFlushedToClient" pkg/` returns ZERO hits); PURGE `latestReq.status != statusStreaming` reference (vacuous — failed attempts are `statusFailed`). **Files table rows** updated accordingly. **Plan-overview Risk #16c** captures the residual drift risk. |
| **Review #4 (tried-set mutation discipline)** | `requestContext.triedCredentialIDs`/`failoverAttempts` mutated ONLY from `manage()` loop (single-goroutine, serialized by `c.mu`) + mandatory code comment | **Task 18 (AMENDED in place)**: mandated code comment text `// mutated only from manage() under c.mu — race-free by construction` at both field declarations and every mutation site; acceptance criterion += grep-gate `grep -n "// mutated only from manage" pkg/proxy/` returns the comment at both `requestContext` field declarations and the `manage()` mutation sites. **Files row `handler_helpers.go`** amended. |
| **Review #6 (Task 17 classifier)** | ONE pinned classifier vocabulary table; absent-`Retry-After` ⇒ `RetryAfter=0` ⇒ caller applies 60s default; HTTP-200-with-embedded-error OUT OF SCOPE | **Task 17 (AMENDED in place)**: vocabulary table pinned — type-match = substring `rate_limit` (covers `models.ErrorTypeRateLimit` + `pkg/proxy/translator.ErrorTypeRateLimit`); code-match = equality on rate-limit code constants; sources `models/errors.go:7,17` + `pkg/proxy/translator/errors.go:13,70,129`; 503 + rate-limit body ⇒ `true`; HTTP 200 with rate-limit body OUT OF SCOPE (future work); absent `Retry-After` / malformed ⇒ `RetryAfter=0` (NEVER a "no cooldown" signal; caller applies 60s default); acceptance criterion += substring match + code equality + 503 row + 200-out-of-scope + absent-header. |
| **Leader ruling (i) — anthropic-passthrough internal branch IN failover scope** | `handler_anthropic.go:297-345` anthropic-passthrough internal branch (internal model + anthropic-provider credential → `doAnthropicRequest` passthrough) joins the failover family for C1 consistency | **Task 22 (AMENDED in place)**: the branch swaps its single-cred read (`modelConfig.CredentialID` / `GetCredential` / `cred.ResolveAPIKey()` / `arc.credentialAPIKey`) to `ResolveInternalConfigWithAffinity(modelConfig.ID, conversationKey)` returning `ResolvedCredential` (per #3); the rate-limit hook added — initial-call error (`*ProviderError` type assertion mirroring Task 21's guard fix) + multi-cred + budget + no Anthropic bytes written (pre-write guard: `arc.headersSent` at `:588`/`:654`/`:1065`) ⇒ `ExcludeAndReselect` + single retry; **passthrough transport via `doAnthropicRequest` is UNCHANGED** (the ruling preserves the wire shape — only the credential read + the rate-limit hook are added); **acceptance criterion += grep-gate `grep -n "ResolveInternalConfigWithAffinity" pkg/proxy/handler_anthropic.go` returns ≥2 sites** (internal-handler construction + the new passthrough-branch site at `:297-345`). **Plan-overview Risk #16 (Round 3b resolution note)** + Scope In-Scope. |
| **Leader ruling (iii) — Case-2 idle classification** | Case-2 idle path (`race_coordinator.go:364-395`) ALSO runs rate-limit classification before spawning — uniform precedence on every spawn path | **Task 18 (AMENDED in place)**: Case-2 idle path at `manage()` `:364-395` runs `IsRateLimitError(latestReq.GetError())` check before spawning `modelTypeSecond` + `modelTypeFallback` together at `:399-405`; if the stalled request has a rate-limit error pending, the engine's `ExcludeAndReselect` is consulted before spawning the parallel — uniform precedence on EVERY spawn path (leader ruling supersedes the review's record-as-non-goal suggestion); also documents Case-1 latest-only inspection (`c.requests[len-1]`) = accepted v1 behavior; `modelTypeSecond` 429 re-keys failover to the primary credential (secondary resolves primary cred via `race_executor.go:112`) = specified behavior. |
| **Canonical naming (Round 3 consolidation)** | Pin ONE canonical Go identifier for the new model-type constant | **Task 18 (AMENDED in place)**: **canonical: `modelTypeCredFailover`** (purges `credential_failover` from F.3 tree and `modelTypeCredentialFailover` from F.3 rationale); `model_credential_failover` stays as the EVENT name; `triggerRateLimit` is the spawn-trigger constant name; `triggerMainError` continues to identify the Case-1 trigger; acceptance criterion += grep-gate `grep -n "modelTypeCredentialFailover" pkg/proxy/` returns ZERO hits (canonical is `modelTypeCredFailover`). |

**Files table — Rows amended (Round 3b):** the 4 rows for `race_coordinator.go`, `handler_helpers.go`, `race_executor.go`, `ultimatemodel/handler_internal.go`, and `internal_handler.go + handler_anthropic.go` — all gain `(AMENDED 2026-08-25 Round 3b — …)` markers with the corrected function names, cites, and guards. Tasks 17, 18, 21, 22, 23 are amended in place; the `(amended Round 3b)` markers appear at the top of each affected row in the established style.

**Wrong-symbol purges performed (Round 3b):**
- `monitorLoop` (Round-3 text for the coordinator loop) → `manage()` at `:252`
- `spawnAttempt` (Round-3 text for the spawn function) → `spawn()` at `:189`
- `chunksFlushedToClient` (Round-3 text, invented) → **DELETED** (no such field at HEAD)
- `bytesFlushedToClient()` (Round-3 text, invented) → **DELETED** (no such method at HEAD)
- `modelTypeCredentialFailover` (Round-3 text variant) → canonical `modelTypeCredFailover`

**Cite fixes performed (Round 3b):**
- `/v1/messages` first-byte header write: `adapter_anthropic.go:170-176` → **`handler_anthropic.go:648-654`** (Task 22 + Task 23)
- `streamResult` call `:852` / def `:960` — disambiguated in Files row
- `handleNonStreamResult` call `:879` / def `:1327` — disambiguated in Files row
- `headersSent` as ultimate-internal guard → replaced with `*ProviderError` type assertion (Task 21)
- `latestReq.status != statusStreaming` → **DELETED** (vacuous; failed attempts are `statusFailed`)

**Scope expansion (Round 3b — leader ruling (i)):** the **`handler_anthropic.go:297-345` anthropic-passthrough internal branch** (internal model + anthropic-provider credential) is now in failover scope; the branch's single-cred read swaps to `ResolveInternalConfigWithAffinity` + adds the rate-limit hook. Passthrough transport via `doAnthropicRequest` is UNCHANGED.

---

## Round 3c — Reviewer Findings (2026-08-25)

> **AMENDED 2026-08-25 (Round 3c — Reviewer Findings):** reviewer verdict APPROVED-WITH-NOTES,
> gated on one mandatory pass (3 critical, 10 warnings, 6 suggestions — all pin-level; leader
> pre-ruled every decision point, including a RE-SCOPE of Round-3b ruling (iii)). Round 3 +
> Round 3b semantics STAND except where explicitly superseded below. Round 3b changelog rows
> above are preserved verbatim as audit trail; supersessions are called out HERE. Contract
> (`decisions.md` + `technical-analysis.md`, sibling worker) wins on any residual conflict.

### Round 3c — item → task mapping (this file)

| Item | Severity | What changed | Task(s) / section(s) |
|------|----------|--------------|----------------------|
| **C1** | Critical | **Hoisted admission gate**: the credFailover pre-checks (rate-limit + internal + multi-cred + budget + `winner==nil`) are hoisted ABOVE the `:338` window gate — the Round-3b exemption-only form was UNREACHABLE in the core scenario (3 creds / 1 model / no chain: `1 < 1` false from first failure → zero failovers → dead-loop to deadline). Admission condition pinned verbatim: `gate := modelAttempts < len(models) \|\| credFailoverEligibleWithBudget()` (`modelAttempts` = Round-3b separate accounting, `modelType ∈ {main, second, fallback}` only). Equivalence note added: hoisted form ≡ rejected cap-extension (`cap += len(credentials)−1`) — cap-extension rejected as IMPLEMENTATION (mutates len-dependent invariants), not semantics. Phase 5 Task 32 scenario (a) pinned as the regression guard for exactly this bug. | Task 18 (B1 paragraph rewritten in place); Files row `race_coordinator.go` (`:338` cell) |
| **C2** | Critical | Consumer logic pinned to phase2 Task 12's unified mode mapping (phase2 semantics WIN; contract synced by sibling): `ReselectNone` → model-fallback fall-through; `ReselectSoonestExpiry` → single attempt then fall-through; `ReselectHealthy` (incl. B2 no-op) → proceed with returned credential. | Task 18 (mode-branching paragraph amended in place) |
| **C3** | Critical | Three wiring sub-tasks enumerated for currently-referenced-but-nonexistent code: (1) `spawnTriggerInfo.credentialID string` (`race_coordinator.go:79` — trigger/errorMessage/failedRequest only today); (2) `case modelTypeCredFailover: modelID = c.models[0]` in the `spawn()` switch (`:~196-218` — main/second/fallback only today; absent case ⇒ `modelID=""` silent misfire); (3) `engine` + `conversationKey` params on `newRaceCoordinatorWithEvents` (`:129` — 8 params today, neither present), wired `cmd/main.go` → `NewHandler` → `handler.go:830`; constructor-only injection per W-3, NO `Config.CredEngine`. Files-table rows for `race_coordinator.go`, `handler.go` (NEW `:830` row), `cmd/main.go` updated. | Task 18 (C3 block); Files rows `race_coordinator.go` / `handler.go` / `cmd/main.go` |
| **W1** | Warning | Passthrough-branch classification premise corrected: the branch produces NO `*ProviderError` and `doAnthropicRequest` discards `resp.Header` today — classify via `arc.lastStatusCode == 429` OR body `type` substring `rate_limit`; Retry-After captured via NEW additive `arc.retryAfter time.Duration` at the `arc.lastStatusCode` set-site (`:423`). | Task 22 (swap item (4)); Files row `internal_handler.go + handler_anthropic.go` (ruling (i) cell) |
| **W2** | Warning | `ProviderError` additively gains `ErrorType`/`ErrorCode` (`providers/interface.go:155-161`), populated in `handleError` from the existing unmarshal (`openai.go:238-246` anonymous local `{Error:{Message,Type,Code}}` — values decoded then discarded today); makes the 503-body matrix row implementable; vocabulary table reads `ProviderError.ErrorType/ErrorCode` post-extension. | Task 17 |
| **W3** | Warning | SoonestExpiry single-shot structurally enforced: per-request `soonestExpiryAttempted` flag + `continue` — no double-spawn; the next 429 routes to model-fallback sequentially (per §F.4). | Task 18 (mode-branching paragraph); Files row `race_coordinator.go` (`:350-364` cell) |
| **W4** | Warning | **Ruling (iii) RE-SCOPED**: Round-3b's "Case 2 classifies rate-limit before spawning" was wrong as stated — at idle-spawn time NO error exists to classify. Case 2 spawns second/fallback UNCHANGED; the stalled attempt's eventual 429 is adjudicated by Case 1 (now reachable with C1 fixed — the multi-attempt environment post-idle-spawn is exactly where the hoisted gate matters). Phase 5 Task 37 REWRITTEN to assert that ordering. Round-3b wording marked SUPERSEDED with an in-task audit-trail note. | Task 18 (item (d) rewritten in place + acceptance (c)); Files row `race_coordinator.go` (`:364-395` cell) |
| **W6** | Warning | "Failover happy path × all FOUR LB'd paths" (race-internal, ultimate-internal, `/v1/messages` internal, anthropic-passthrough) folded into the acceptance criteria; "three LB'd paths" wording superseded. | Task 24 |
| **S4** | Suggestion | `GetRequestStatuses` credFailover-row handling (per-row `credentialID`, operator-visible) folded INTO Task 18 (was a ride-along note); Phase 5 control test (non-rate-limit error → straight-to-model-fallback, no credFailover spawn) is Phase 5 Task 46. | Task 18 (S4 block + acceptance (l)); Phase 5 Task 46 |

**Not touched by Round 3c in this file:** Tasks 1-16 (Round-1/2 wiring — no Round 3c items land there); Task 19 (budget plumbing — unchanged by C1; the hoisted gate consults the same budget check); Task 20 (event/logs — payload unchanged); Task 21 (ultimate-internal guard stays `*ProviderError` — that path DOES produce `*ProviderError`; W1 applies only to the passthrough branch); Task 23 (streaming guards — W5 lands in Phase 5 Task 33, which owns the test-side assertion); the Round 3b changelog appendix (preserved verbatim; supersessions called out above). Phase 3 gains NO new numbered tasks this round — every Round 3c item here is an in-place amendment of Tasks 17/18/22/24 + Files rows (the dispatcher's next-free slot 25 remains unused).

---

## Round 3g — Phase-2 Review Rulings (2026-08-25)

> **AMENDED 2026-08-25 (Round 3g):** leader-ruled micro plan-sync from the
> Phase-2 external review. Surgical append-only amendment to Task 10 area +
> Task 13. No restructuring, no new task numbering; existing Round-1/2/3
> semantics UNCHANGED where not explicitly superseded.

| Round | What changed | Task(s) / section(s) |
|-------|--------------|----------------------|
| **Round 3g** | Phase-2 Review Rulings: Task 10 area — `ModelsManager.Close()` wired into graceful shutdown (order: `credLB.Stop()` → `ModelsManager.Close()` → `dbStore.Close()`); optional fast-path single-RLock polish (`engine.go:213/230`) may ride with Phase 3 (benchmark-gated, no behavioral change) | Task 10 area (engine construction in main) + Task 13 (shutdown ordering — `modelsMgr.Close()` inserted between `credLB.Stop` and `dbStore.Close`) |

---

## Round 3j — Phase-3 Review C1 root-cause (2026-08-25)

> **AMENDED 2026-08-25 (Round 3j):** leader-ruled plan correction from the Phase-3 review
> (C1 root-cause, 3 items across the plan family; W3 lands in `technical-analysis.md`).
> In-place amendments with audit-trail markers; the superseded Round-3c pin is kept visible.
> No restructuring, no new task numbering; prior round rows preserved verbatim.

| Round | What changed | Task(s) / section(s) |
|-------|--------------|----------------------|
| **Round 3j** | Phase-3 Review C1 root-cause: Task 18 C3(2) corrected — case modelTypeCredFailover uses triggerInfo.modelID (populated from latestReq.modelID at Case-1), NOT c.models[0] (wrong model on fallback-row 429 in multi-model chains); spawnTriggerInfo gains modelID; resolveSpecificInternalCredential belt-and-braces; 2-model fallback-429 regression test added to acceptance; exit-grep gates re-baselined (code-only semantics, line-wrap-proof patterns, purge gates strict-zero) | Task 18 + Exit Criterion preamble |
| **Round 3j (addendum)** | REACHABILITY recorded, superseding the fix-coder's "latent bug" reading (disproven by verify pass @ 3d1236d): Case-1 credFailover is PRODUCTION-REACHABLE for fallback-row 429s in multi-model chains — hoisted gate passes on the credEligible arm at modelAttempts >= len(models) (race_coordinator.go:452-454) and spawnedCredFailover short-circuits the B1 all-failed check (:508, :595) ⇒ under the superseded c.models[0] pin the wrong-model spawn was LIVE, billing-grade; the 3d1236d fix + two-layer regression test (Task 18 gate (m) + Phase 5 Task 32 scenario (c)) are LOAD-BEARING. B1-ordering rationale corrected + stale line-refs cleaned in 7d64ece | Task 18 (C3(2) + acceptance) |
