# Phase 5: Tests & Verification

> **AMENDED 2026-08-21 (leader rulings):** this phase is amended per architect council rulings A-1 (token-salted affinity tests), A-2 (multimodal flips to affinity assertions — no fallback), W-1 (event test uses `newlyBound` signal), W-2 (empty-key no-binding test), E-3 (fast-path no map writes), E-4 (janitor panic-recovery + `Engine.Stats()`), M-1-test (PG-gated 028 transaction test **MANDATORY**). The amended API Contract in `technical-analysis.md` (lines 1349-1373 changelog + §API Contract) is authoritative. The single most consequential addition is the **leader-mandated templated-first-message distribution test** — the existing distribution test with 100 unique first messages CANNOT detect templated-agent skew. See "Amendment Changelog" at the end of this file for the ruling-by-ruling diff.

## Objective
Verify the entire feature end-to-end with three layers: (a) unit tests for the engine's distribution (BOTH unique-first-message AND templated-first-message), affinity (token-salted keys), TTL, weight-invalidation, race-detector properties, and the W-2 empty-key no-binding invariant; (b) handler integration tests that prove multi-credential models distribute by weight, single-credential models are byte-identical to today with NO binding map writes (E-3), and the engine wiring survives the request lifecycle; (c) an E2E shell script (`test/test_mock_credential_lb.sh`) that exercises two mock upstreams, asserts distribution + affinity + the templated-first-message scenario in-process, and serves as the rollout smoke test. All tests must pass under `go test -race`.

## Context
Phase 1 delivered the schema + store (M-1: migration 028 = ADD + backfill + same-statement derived shadow write to `credential_id`; NO DROP COLUMN; 029 deferred behind release-note deprecation window). Phase 2 delivered `pkg/credentiallb.Engine` with the amended contract from `technical-analysis.md §API Contract` (lines 1349-1373 changelog + amended contract). Phase 3 wired the engine into the request path with the post-auth token-salted conversation key (A-1). Phase 4 added the UI/API surface. Phase 5 is the verification gate. The test plan mirrors the structure already established in `decisions.md §E` (engine unit, handler integration, migration, E2E), with extensions: (1) a templated-first-message distribution test that the original plan's 100-unique-messages test cannot detect; (2) a `test_helpers.go` extension so handler integration tests can drive two distinct upstreams against one model; (3) a manual curl checklist (in-repo, not a separate doc) for the rollout reviewer.

The existing house pattern is:
- **Go unit/integration tests**: `pkg/.../*_test.go`, run via `go test ./...`. Uses `database/sql` + `modernc.org/sqlite` for in-memory DBs (see `pkg/proxy/handler_integration_test.go:30-71` for the `setupIntegrationDB` pattern).
- **Handler integration tests**: `pkg/proxy/handler_integration_test.go` — uses `httptest.NewServer` for upstreams and `models.NewModelsConfig()` for fast config setup.
- **E2E shell scripts**: `test/test_mock_*.sh` — `bash` + `curl` + `go run` for the proxy. The reference is `test/test_mock_ultimate_model.sh` (565 lines): two upstreams on ports 4001/4002, proxy on 4322, `clean_ports` from `test_mock_clean_ports.sh`, `assert_contains` helper, `hard_timeout` trap.
- **Frontend tests**: `pnpm test` (vitest + @testing-library/preact, per `pkg/ui/frontend/package.json`). House pattern is light — most coverage is on the Go side.
- **Mock LLMs**: `test/mock_llm_openai.go` is a real HTTP server that simulates an OpenAI-compatible upstream; supports message-keyed responses for sticky tests.

This phase adds NO production code (with one exception: a tiny helper added to `pkg/proxy/test_helpers.go` to enable multi-upstream handler integration tests). All other work is test files.

## Scope

### In Scope
1. **Create** engine unit tests `pkg/credentiallb/*_test.go` — mirror `decisions.md §E` tests 1–7 PLUS the **leader-mandated templated-first-message distribution test** and the **W-2 empty-key no-binding test** (per the architect's amendment). Fixed RNG seed for determinism. Each test is small (<200 lines) and named per the contract:
   - `TestEngine_Distribution` — N=10k with unique first-user messages; ±5% tolerance.
   - **`TestEngine_Distribution_TemplatedFirstMessage` (NEW, leader-mandated)** — N=10k requests sharing the SAME first-user message but DIFFERENT `tokenID`s (one per request). Asserts hits distribute across credentials per weights. **This test detects the templated-agent skew failure class that A-1 was introduced to prevent.** Without it, the existing 100-unique-messages test passes while the actual production workload silently pins to one credential.
   - `TestEngine_Affinity` — same `(tokenID, firstUserMessage)` pair → same credential across N calls; `newlyBound=true` on first call, `false` on subsequent.
   - `TestEngine_Affinity_TokenSalted` (NEW, A-1) — same first-user message but DIFFERENT `tokenID`s → DIFFERENT bindings (independent selections per principal). Two distinct tokens sending the same message land on potentially DIFFERENT credentials; the same token sending the same message lands on the SAME credential.
   - `TestEngine_Affinity_Anonymous` (NEW, A-1 graceful degradation) — empty `tokenID` + same first-user message → same credential (the unsalted key binds them).
   - **`TestEngine_EmptyKey_NoBinding` (NEW, W-2)** — two requests with `conversationKey == ""` → MAY pick DIFFERENT credentials (fresh weighted pick per request). The engine's internal binding map size for that model stays 0 (no binding stored). Per C2: empty-key picks return `newlyBound=false` (no binding stored) — the engine NEVER emits, and callers MUST NOT publish `model_credential_selected` for empty-key picks (zero events; DEBUG log is the observability, phase3 Scope #8). (**REVISED 2026-08-21 — reviewer pass C2**; supersedes the prior "event fires per request" reading.)
   - `TestEngine_TTLExpiry` — bind, advance time past TTL, next call returns fresh credential.
   - `TestEngine_WeightChangeInvalidation` — `OnModelChanged` with new weights; **E-2** filter-survivors: bindings whose credential survives are preserved; only orphans dropped.
   - `TestEngine_RaceDetector` — concurrent `GetOrSelect` for distinct + overlapping keys; pass `go test -race`.
   - `TestEngine_NoCredentials` — returns `ErrNoCredentials`; `newlyBound=false`.
   - `TestEngine_SingleCredentialFastPath` — **E-3** asserts the binding map size for that model is **ZERO** after N requests (`Engine.Stats().Bindings(modelID) == 0`); `newlyBound=false` on every call (no binding write).
   - **`TestEngine_ExtractFirstUserMessage_Multimodal` (NEW, A-2)** — multimodal `[]interface{}` content produces a deterministic canonical-JSON hash; the same content yields the same hash; the same content with a different ordering yields a different hash (canonicalization discipline). Pinned to the same credential across N calls.
   - **`TestEngine_JanitorPanicRecovery` (NEW, E-4 recommended)** — inject a panic into one model's sweep path; assert the janitor logs the panic, continues to sweep other models, and does NOT exit the goroutine. Assertable via `Engine.Stats()` showing the panicking model's bindings dropped (other models untouched).
   - **`TestEngine_Stats` (NEW, E-4 recommended)** — `Engine.Stats()` returns sane per-model hits/misses/bindings under load.
   - Run all with `go test -race`.
2. **Create** engine integration tests that exercise the full lifecycle (start, bind, `OnModelChanged` filter-survivors, `OnCredentialDeleted` cross-model drop, `Engine.Stats()` observability, `Stop`) — `pkg/credentiallb/integration_test.go`.
3. **Modify** `pkg/proxy/test_helpers.go` (additive only):
   - Add a helper `newUpstreamPair(t *testing.T) (*httptest.Server, *httptest.Server)` that returns two upstream servers bound to free ports, each capable of returning OpenAI-style JSON. Used by handler integration tests.
   - Add `recordedRequestCount(server *httptest.Server) int` helper that reads from an atomic counter on the upstream.
   - Do NOT change the existing `testProvider` struct semantics (production tests rely on it).
4. **Create** `pkg/proxy/handler_integration_test.go` additions (do not edit existing tests):
   - `TestHandler_MultiCredentialDistribution` — model with `credentials: [{A,1,0},{B,2,1}]`; 100 requests with unique first-user messages; assert hits on A in [20,55] and B in [45,80] (weights 1:2 → expected 33/67 with ±15% tolerance for N=100; Phase 5 documents the looser tolerance vs. the engine's ±5% at N=10k).
   - **`TestHandler_MultiCredentialDistribution_TemplatedFirstMessage` (NEW, leader-mandated)** — same model; N requests sharing the SAME first-user message but DIFFERENT `tokenID`s (via `httptest.NewRequest` with distinct `Authorization: Bearer <token>` headers mapped to distinct token rows in the auth store). Asserts hits distribute per weights across both upstreams. Catches the A-1 regression at the handler layer.
   - `TestHandler_MultiCredentialAffinity` — same model; 5 requests with the same `(tokenID, first-user message)` pair; assert all 5 land on the same upstream; the `model_credential_selected` event fires exactly once (verified via Phase 5 test #8 / `EventBus.Subscribe` test sink).
   - **`TestHandler_MultiCredentialAffinity_TokenSalted` (NEW, A-1)** — same model; TWO distinct tokens each send 5 requests with the SAME first-user message. The two tokens may bind to DIFFERENT upstreams; each token's own 5 requests all land on the same upstream. This is the regression catcher for A-1: if the wiring site slips back to `initRequestContext` (no tokenID), the two tokens would pin to the same credential.
   - `TestHandler_MultiCredentialAffinity_Anonymous` (NEW, A-1 graceful degradation) — same model; 5 requests with NO auth header + same first-user message; all 5 land on the same upstream (unsalted key).
   - **`TestHandler_EmptyConversationKey_NoBinding` (NEW, W-2 + REVISED 2026-08-21 (reviewer pass — C2))** — model with multi-credentials; 10 requests with EMPTY first-user message (e.g., system-only messages). Each request MAY hit a different upstream; the engine's binding map size for that model stays 0 (verified via `Engine.Stats().Bindings(modelID)` after the requests); **`model_credential_selected` does NOT fire for any of the 10 requests** (per C2: `NewlyBound=false` because no binding was stored on each call; event publish is gated on `NewlyBound=true`).
   - `TestHandler_MultimodalAffinity` (AMENDED, A-2 — was a fallback assertion) — model with multi-credentials; 5 requests with multimodal content (text + image_url array) producing the SAME canonical-JSON hash; all 5 land on the same upstream. Deterministic canonical-JSON pinning (precedent `hash_cache.go:172-186`).
   - `TestHandler_SingleCredentialBackwardCompat` — model with one credential; send 10 requests; assert wire-level request bytes (model field, body shape) match the pre-feature baseline (compare against a recorded golden file or use `httputil.DumpRequestOut` to a per-test buffer; verify against the same upstream's recorded body for a single-credential path that pre-dates this feature). **E-3**: also assert `Engine.Stats().Bindings(modelID) == 0` after the 10 requests (NO map writes on the fast path).
   - `TestHandler_UltimateMultiCredentialAffinity` — same model, ultimate path; first two requests use the primary credential (with LB), then force ultimate model trigger (via `X-Force-Ultimate-Model: true` header per `pkg/ultimatemodel/handler.go:68`), assert the ultimate model's credential selection is independent (separate bucket, per `decisions.md §D`).
    - `TestHandler_AnthropicMessagesAffinity` (NEW 2026-08-21 — C1, leader-ruled) — drives a request through the `/v1/messages` route (`cmd/main.go:139` → `pkg/proxy/handler_anthropic.go:340` → `:447` → `:461` → `pkg/proxy/internal_handler.go:67/69`) with Anthropic-shape JSON body. Two scenarios: (a) **affinity** — same `(tokenID, firstUserMessage)` pair → same upstream credential across N requests; (b) **distribution** — N=100 requests with unique first-user messages distribute across the two upstreams per weights (using `newUpstreamPair` from Task 4). The test uses the **DB-backed `setupIntegrationDB`** substrate (per #4 — see Task 12) because the Anthropic path runs against `NewInternalHandler` which calls `ModelsManager.ResolveInternalConfigWithAffinity`, and only the DB-backed `ModelsManager` owns an engine. The single-credential fast path (`len(Credentials) == 1`) is byte-identical to today (Phase 3 E-3); the test asserts both shapes. **C1 grep-guard**: after the test, `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0.
5. **Create** `pkg/store/database/store_test.go` additions:
   - **SQLite migration tests**: `TestMigration028_BackfillSQLite`, `TestMigration028_EmptyCredential`, `TestMigration028_MultiCredential`. **M-1 amendment**: each test must additionally assert that `PRAGMA table_info(models)` STILL includes `credential_id` (the column is NOT dropped; it is kept as a derived shadow). Backfill: `credentials_json='[{"credential_id":"X","weight":1,"position":0}]'` AND `credential_id='X'` (the shadow).
   - **`TestMigration028_PGGated_TransactionCommit` (UPGRADED — MANDATORY, M-1-test)** — previously documented as "manual" or "best-effort" per `decisions.md §E`; this is the repo's FIRST file-level transactional migration (PG migration 024 uses `BEGIN` only inside PL/pgSQL, NOT at the file level). The test MUST be bundled with SQLite + pgx v5 against a real Postgres instance (testcontainers or CI-side), prove that the transaction commits on both dialects, and prove the same-statement derived shadow write (`credential_id = credentials[0].credential_id`) succeeds alongside the JSON backfill in ONE transaction. **Without this test passing, migration 028 is NOT approved.** If no Postgres container is available in CI, the test MUST be gated behind a build tag (`//go:build pg_integration`) and documented as a manual runbook for the rollout reviewer; either way, it is MANDATORY pre-merge.
   - **Postgres coverage is no longer "best-effort manual"** — see above. The PG-gated transaction test gates Phase 1's exit (and therefore Phase 5's exit).
6. **Modify** `pkg/store/database/store_test.go` (engine invalidation coverage, since Phase 1 wires `ModelsManager.AddModel`/`UpdateModel`/`RemoveModel`/`RemoveCredential` to the engine via `OnModelChanged`/`OnCredentialDeleted`):
   - `TestModelsManager_EngineOnModelChanged` — create model with two credentials, call `AddModel`, then `UpdateModel` with one credential removed, assert `ModelsManager.Engine().OnModelChanged` was called (verified via a stub engine if Phase 1 wired an interface, or via the binding map state).
   - `TestModelsManager_EngineOnCredentialDeleted` — delete a credential that's referenced, assert the engine's bindings for that credential are dropped (verified via the binding map state after `RemoveCredential` returns).
   - These tests rely on the Phase 1 `ModelsManager.Engine()` accessor.
7. **Create** `test/test_mock_credential_lb.sh` (~400 lines, modeled on `test_mock_ultimate_model.sh`):
   - Start TWO mock upstreams on ports 4001 and 4002 (`go run test/mock_llm_openai.go -port=4001` and `-port=4002`). Each mock records its hit count via an in-process counter (extend `mock_llm_openai.go` with a `--hit-count-file` flag that writes to a temp file on each request — minimal additive change to the existing mock).
   - Configure the proxy on port 4322 via env overrides (same pattern as `test_mock_ultimate_model.sh:118-127`).
   - Configure the auth store with TWO distinct API tokens (token-A, token-B), each with permission to use the test model. Used by Test 7 (token-salted affinity).
   - Create a model with `credentials: [{4001_cred, 1, 0}, {4002_cred, 2, 1}]` via `POST /fe/api/models`.
   - **Test 1 (Affinity, token-salted)** — token-A sends 5 chat completions with the same first user message ("What is 2+2?"). Assert all 5 hit the same mock (single upstream in the result set). Verify via the hit-count file.
   - **Test 2 (Distribution, unique first messages)** — 100 chat completions with unique first user messages (UUIDs as the message content). Assert hit counts: 4001 in [25, 50], 4002 in [50, 75] (weights 1:2 → 33/67 expected).
   - **`Test 2b (Distribution, templated first message — leader-mandated, NEW)`** — token-A, token-B, ..., token-N each send ONE chat completion with the IDENTICAL first user message ("What is the weather today?"). N=200 token rotations. Assert hit counts: 4001 in [25, 100], 4002 in [100, 175] (weights 1:2 → 33/67 expected with ±15% tolerance for N=200). **This catches the A-1 regression at the E2E layer**: if the post-auth wiring site slips back to `initRequestContext`, all 200 requests share one unsalted key and pin to one upstream.
   - **`Test 2c (Distribution, anonymous, no auth header — NEW)`** — N=100 anonymous requests (no `Authorization` header) sharing the same first user message. Assert hits distribute across both upstreams per weights. Catches the A-1 graceful-degradation regression.
   - **`Test 2d (Empty first message — W-2 + REVISED 2026-08-21 (reviewer pass — C2))`** — N=100 requests with empty `messages[]` or no first-user-message field. Assert hits distribute across both upstreams (each request gets a fresh pick; no binding stored). **Per C2**: assert that ZERO `model_credential_selected` events are emitted across the 100 requests (engine returns `NewlyBound=false`; event publish gated on `NewlyBound=true`). The proxy's stderr / event sink count must be 0 — NOT 100. This is the E2E counterpart of `TestHandler_EngineEventPublish_EmptyKey_NeverFires`.
   - **`Test 2e (Multimodal — A-2, NEW)`** — N=10 requests with the same multimodal content (text + `image_url` array) producing identical canonical-JSON hash. Assert all 10 hit the same mock (affinity preserved for multimodal — no fallback).
   - **Test 3 (Validation, mixed-provider)** — POST to `/fe/api/models/validate` with two credentials of different providers. Assert HTTP 400 + error message contains "provider mismatch" or equivalent.
   - **Test 4 (Single-credential backward compat + E-3 fast-path)** — create a model with one credential, send 10 requests, assert all 10 hit the single mock upstream. Grep proxy stderr for `Engine.Stats().Bindings == 0` log line after the test (proves NO map writes on the single-cred fast path).
   - **Test 5 (Engine event-bus propagation, only-on-first-binding — W-1)** — log the engine's INFO line "model=X→cred=Y bound" and grep for it after the affinity test (Test 1). If the line appears once per unique conversation (not per request), the test passes. Full verification is in `pkg/proxy/handler_test.go` via `EventBus.Subscribe` test sink.
   - **Test 6 (Single request → single publish — W-1)** — send 5 affinity requests, assert the `model_credential_selected` event fires exactly once per unique conversation (verified via the SSE feed or by counting log lines; SSE feed is more robust — see #11 below).
   - **Test 7 (Token-salted affinity independence — A-1)** — token-A sends 5 requests with message M; token-B sends 5 requests with message M; assert token-A's 5 requests all hit the SAME upstream, token-B's 5 requests all hit the SAME upstream, and the two tokens MAY bind to DIFFERENT upstreams. (This is the A-1 wiring-site regression catcher at the E2E layer.)
   - Per `decisions.md §E`: **restart persistence is deferred to v2** — no restart scenario in this E2E. The contract explicitly says restart breaks affinity for any conversation whose binding's TTL would have outlived the restart (acceptable per the 24h TTL × short-restart math).
8. **Create** `test/manual_curl_checklist.md` (in-repo, not a separate doc) — a short checklist of curl commands the rollout reviewer can run against a live deployment. Mirror the structure of `decisions.md §E test 5` (curl examples). Sections: distribution, affinity, single-credential regression, validate endpoint, UI smoke. **Skip if the house pattern doesn't use a checklist** — verify by `grep -l "manual_curl\|smoke" test/ docs/` (none expected).
9. **Modify** `test/mock_llm_openai.go` (minimal additive change): add a `--hit-count-file <path>` flag. On every request, atomically increment and write the count to the file. Used by `test_mock_credential_lb.sh` to verify distribution without parsing log output. Default behavior unchanged.
10. **Create** `pkg/proxy/race_executor_test.go` additions (if not already covered):
    - `TestExecuteInternalRequest_SelectedCredentialIDInLog` — capture stderr, run a single internal request through `executeInternalRequest`, assert the augmented log line `[DEBUG] Race attempt … credential_id=…` contains the expected credential ID.
11. **Modify** `pkg/proxy/handler_test.go` (additions):
    - **`TestHandler_EngineEventPublish_OncePerFirstBinding` (AMENDED, W-1)** — subscribe a test sink to `model_credential_selected`, send 5 affinity-pinned requests with the same `(tokenID, firstUserMessage)` pair; assert exactly 1 event fired. The engine's `newlyBound` return drives the publish; if the bool heuristic leaks back in (per-request publish), this test fails with count == 5. Test name reflects the actual semantic ("first binding" = `newlyBound=true`).
    - **`TestHandler_EngineEventPublish_EmptyKey_NeverFires` (RENAMED 2026-08-21 (reviewer pass — C2); was `_FiresPerRequest`)** — send 5 requests with empty first-user-message (engine returns `NewlyBound=false` each time per C2 — `newlyBound ⇔ a binding was stored on this call`; no binding is stored for empty keys); **assert 0 events fired** (the event publish is gated on `NewlyBound=true`; empty-key picks never publish).
    - `TestHandler_NilEngineFastPath` — pass `nil` as the 7th arg to `NewHandler`, send requests, assert no panic, requests succeed (legacy path).
12. **Update** the rollout verification docs (existing `README.md` or `.agents/` docs): add a short "Rollout Verification" subsection that lists `go test -race ./pkg/credentiallb/... ./pkg/proxy/... ./pkg/ultimatemodel/... ./pkg/store/database/... ./pkg/ui/...` plus `./test/test_mock_credential_lb.sh` as the merge gate. Skip if no docs to extend.

### Out of Scope
- **NOT writing** frontend unit tests for the new `MultiCredentialEditor` — Preact component testing requires `pnpm test` setup that's not house pattern for new components. The manual smoke test in Phase 4's exit criterion is the verification.
- **NOT testing** cross-instance affinity (Redis-backed) — deferred to v2.
- **NOT writing** benchmarks in this phase (`go test -bench`) — referenced in Phase 3 Risk #4 as "added in Phase 5" but the actual benchmark is optional; skip if not blocking merge.
- **NOT testing** the broken `config.updated` event payload at `pkg/config/config.go:518` (per `decisions.md §C "Tech-debt caveat"` — out of scope).
- **NOT testing** the engine's panic recovery from the janitor — flagged in `technical-analysis.md §Recommended Paydown` as optional.
- **NOT testing** the Postgres migration path via a container if one isn't already wired — document as "manual runbook" in the commit message, matching the existing pattern.

## Files

### Create
| File | Purpose | Approx lines |
|---|---|---|
| `pkg/credentiallb/engine_test.go` | Engine unit tests (distribution, **templated-first-message distribution**, affinity, **token-salted affinity**, **anonymous affinity**, **empty-key no-binding**, TTL, weight invalidation, race detector, no-credentials, single-credential fast path, **multimodal**, **janitor panic recovery**, **Stats()**) — mirrors `decisions.md §E` tests 1–7 PLUS the amendments. | ~700 |
| `pkg/credentiallb/key_test.go` | `ComputeConversationKey` (token-salted, anonymous unsalted, empty message) determinism + `ExtractFirstUserMessage` (multimodal canonical-JSON, empty, missing role) coverage. | ~120 |
| `pkg/credentiallb/integration_test.go` | Full lifecycle: start, bind, `OnModelChanged` filter-survivors, `OnCredentialDeleted` cross-model drop, `Engine.Stats()` observability, `Stop`. | ~140 |
| `test/test_mock_credential_lb.sh` | E2E script: two mock upstreams, distribution + **templated-first-message distribution** + **token-salted affinity** + **anonymous** + **empty-key** + **multimodal** + affinity + backward compat + validate scenarios. | ~500 |

### Modify
| File | Lines | What changes |
|---|---|---|
| `pkg/proxy/test_helpers.go` | (additive, end of file) | `newUpstreamPair(t *testing.T)`, `recordedRequestCount(*httptest.Server)`, `UpstreamCounters` struct. **NEW (A-1)**: `newAuthTokenPair(t)` returning two distinct tokens with permission to use the test model. ~70 lines added. |
| `pkg/proxy/handler_integration_test.go` | (additive, end of file) | **11** new test functions (NEW 2026-08-21 — C1): `TestHandler_MultiCredentialDistribution`, `TestHandler_MultiCredentialDistribution_TemplatedFirstMessage` (leader-mandated), `TestHandler_MultiCredentialAffinity`, `TestHandler_MultiCredentialAffinity_TokenSalted` (A-1), `TestHandler_MultiCredentialAffinity_Anonymous` (A-1), `TestHandler_EmptyConversationKey_NoBinding` (W-2 + C2), `TestHandler_MultimodalAffinity` (A-2 amended), `TestHandler_SingleCredentialBackwardCompat` (+ E-3 binding-map size assertion), `TestHandler_UltimateMultiCredentialAffinity`, **`TestHandler_AnthropicMessagesAffinity` (NEW 2026-08-21 — C1; uses DB-backed `setupIntegrationDB` substrate per #4)**, **`TestHandler_ShutdownOrder` (NEW 2026-08-21 — #11)**. **#4 substrate split**: distribution/affinity/empty-key tests use either the DB-backed `setupIntegrationDB` OR a test-local `ModelsConfigInterface` impl wrapping a directly-constructed `credentiallb.Engine`; single-credential fast-path tests keep using `models.NewModelsConfig()` (no engine needed; verifies E-3 byte-identity). ~550 lines added. |
| `pkg/proxy/handler_test.go` | (additive, end of file) | 3 new test functions: `TestHandler_EngineEventPublish_OncePerFirstBinding` (W-1), `TestHandler_EngineEventPublish_EmptyKey_NeverFires` (RENAMED 2026-08-21 — C2; was `_FiresPerRequest`), `TestHandler_NilEngineFastPath`. ~120 lines added. |
| `pkg/proxy/race_executor_test.go` | (additive) | `TestExecuteInternalRequest_SelectedCredentialIDInLog` — captures stderr and asserts the augmented log line. ~40 lines added. |
| `pkg/store/database/store_test.go` | (additive) | **`TestMigration028_BackfillSQLite` (AMENDED, M-1)** — assert `PRAGMA table_info(models)` STILL includes `credential_id` (derived shadow; NOT dropped). `TestMigration028_EmptyCredential`, `TestMigration028_MultiCredential`. **`TestMigration028_PGGated_TransactionCommit` (NEW, MANDATORY per M-1-test)** — pgx v5 against real Postgres + same-statement derived shadow write. `TestModelsManager_EngineOnModelChanged` (E-2 filter-survivors), `TestModelsManager_EngineOnCredentialDeleted`. ~300 lines added. |
| `pkg/ultimatemodel/handler_test.go` | (additive, mock) | `mockModelsConfig.ResolveInternalConfigWithAffinity` stub. ~10 lines added. |
| `pkg/ultimatemodel/handler_internal_test.go` | (additive, mock) | Same — `mockModelsConfig` extended. ~5 lines added. |
| `pkg/ui/handlers_models_test.go` | (additive) | Tests for: empty + internal → 400; mixed-provider → 400; weight=0 → 400; legacy `credential_id` silently ignored; valid multi-cred POST. ~120 lines added. |
| `test/mock_llm_openai.go` | (additive flag) | `--hit-count-file <path>` flag; atomic counter writes the count after each request. ~25 lines added. |
| `README.md` (optional) | (locate) | Add "Rollout Verification" subsection. |

## Tasks

| # | Task | Depends On | Acceptance |
|---|---|---|---|
| 1 | Implement `pkg/credentiallb/engine_test.go` with the contract tests from `decisions.md §E` PLUS the amendments: `TestEngine_Distribution`, **`TestEngine_Distribution_TemplatedFirstMessage` (leader-mandated)**, `TestEngine_Affinity`, **`TestEngine_Affinity_TokenSalted` (A-1)**, **`TestEngine_Affinity_Anonymous` (A-1)**, **`TestEngine_EmptyKey_NoBinding` (W-2)**, `TestEngine_TTLExpiry`, `TestEngine_WeightChangeInvalidation` (E-2 filter-survivors), `TestEngine_RaceDetector`, `TestEngine_NoCredentials`, `TestEngine_SingleCredentialFastPath` (E-3 binding-map size == 0), **`TestEngine_ExtractFirstUserMessage_Multimodal` (A-2)**, **`TestEngine_JanitorPanicRecovery` (E-4)**, **`TestEngine_Stats` (E-4)**. Fixed RNG seed (e.g., `42`) for determinism. | Phase 2 (engine exists) | `go test -race ./pkg/credentiallb/...` passes; **the templated-first-message distribution test FAILS if A-1 is reverted** (proves regression coverage). |
| 2 | Implement `pkg/credentiallb/key_test.go` covering: deterministic hash for identical inputs; different modelID, tokenID, or firstUserMessage yields different hash; **anonymous (empty tokenID) yields unsalted key different from any token-salted key**; multimodal content returns deterministic canonical-JSON hash; missing role returns `""`; empty messages returns `""`. | Phase 2 (key.go exists) | All branches covered. **The tokenID-different-yields-different-hash test is the A-1 wiring-site regression catcher at the unit-test layer.** |
| 3 | Implement `pkg/credentiallb/integration_test.go` covering the full lifecycle: bind, lazy expiry, janitor sweep (manual trigger via short TTL), `OnModelChanged` rebuild + **filter-survivors** (E-2), `OnCredentialDeleted` cross-model drop, `Stop` idempotency, `Engine.Stats()` observability. | Phase 2 | `go test -race` passes; janitor goroutine exit verified (no leak detector in house, but `runtime.NumGoroutine()` before/after matches); **filter-survivors test asserts bindings preserved across weight nudge** (E-2 regression catcher). |
| 4 | Extend `pkg/proxy/test_helpers.go` with `newUpstreamPair(t *testing.T) (*httptest.Server, *httptest.Server, *atomic.Int32, *atomic.Int32)` returning two servers + per-server hit counters. **NEW (A-1)**: `newAuthTokenPair(t *testing.T) (tokenA, tokenB string, cleanup func())` returning two distinct tokens mapped to separate rows in the auth store, both with permission to use the test model. | None | Helpers compile; existing tests unaffected. |
| 5 | Implement `TestHandler_MultiCredentialDistribution` AND **`TestHandler_MultiCredentialDistribution_TemplatedFirstMessage` (leader-mandated)** AND `TestHandler_MultiCredentialAffinity` AND **`TestHandler_MultiCredentialAffinity_TokenSalted` (A-1)** AND **`TestHandler_MultiCredentialAffinity_Anonymous` (A-1)** AND **`TestHandler_EmptyConversationKey_NoBinding` (W-2 + REVISED 2026-08-21 — C2)** in `pkg/proxy/handler_integration_test.go`. Use the `newUpstreamPair` and `newAuthTokenPair` helpers; configure the model via `models.NewModelsConfig()` (fast path; does NOT exercise the engine through the DB layer — that's covered in Task 12 below). **#4 — substrate pin (NEW 2026-08-21 reviewer pass):** the JSON-backed `models.NewModelsConfig()` does NOT own an engine (only the DB-backed `ModelsManager` owns one). For tests that need to verify engine behavior end-to-end (the multi-credential, affinity, templated, and empty-key scenarios), use the DB-backed `setupIntegrationDB` pattern OR a test-local `ModelsConfigInterface` implementation that wraps a directly-constructed `credentiallb.Engine`. Phase 5 ships BOTH substrates (per test class): distribution/affinity tests use the DB-backed substrate; the single-credential fast-path tests (E-3) keep using `models.NewModelsConfig()` because no engine is needed. **C1 — anthropic integration test (NEW 2026-08-21 reviewer pass):** `TestHandler_AnthropicMessagesAffinity` uses the DB-backed substrate (per #4) because `NewInternalHandler` calls `ModelsManager.ResolveInternalConfigWithAffinity`. | Tasks 4, 3 | 100 unique-message requests produce hits in [20,55] on A and [45,80] on B (loose, ±15%); **200 templated-first-message requests with rotating tokens produce hits in [25,100] on A and [100,175] on B (leader-mandated)**; 5 affinity requests all hit the same upstream; **two distinct tokens sending the same message MAY bind to different upstreams, each token's own 5 requests all hit the same upstream**; **5 empty-key requests MAY hit different upstreams; `Engine.Stats().Bindings(modelID) == 0`; `model_credential_selected` event fires ZERO times** (per C2 — no binding stored, `NewlyBound=false`, event gated on `NewlyBound=true`). |
| 6 | Implement `TestHandler_SingleCredentialBackwardCompat`: one model + one credential; send 10 requests; capture wire bytes via `httputil.DumpRequestOut`; assert the recorded body matches a snapshot captured pre-feature. The snapshot is generated by running the test against the un-modified engine path (record once, commit as test fixture). **E-3**: also assert `Engine.Stats().Bindings(modelID) == 0` after the 10 requests (no map writes on the fast path). | Task 4 | Wire bytes match the snapshot byte-for-byte; **binding map size == 0** (E-3). |
| 7 | Implement `TestHandler_UltimateMultiCredentialAffinity` AND **`TestHandler_MultimodalAffinity` (A-2 — was a fallback assertion, now an affinity assertion)** in `pkg/proxy/handler_integration_test.go`. | Tasks 4, 5 | Ultimate path independent of primary's pinned credential; **multimodal content with identical canonical-JSON hash pins to the same credential across N requests (deterministic canonical-JSON pinning)**. |
| 8 | Implement **`TestHandler_EngineEventPublish_OncePerFirstBinding` (AMENDED, W-1)** AND **`TestHandler_EngineEventPublish_EmptyKey_NeverFires` (RENAMED 2026-08-21 — C2; was `_FiresPerRequest`)** in `pkg/proxy/handler_test.go`: subscribe a test sink to `model_credential_selected` via `EventBus.Subscribe`. | Phase 3 (event publish implemented) | 5 affinity-pinned requests produce exactly 1 event (not 5); **5 empty-key requests produce 0 events** (per C2: engine returns `NewlyBound=false` because no binding is stored; event publish gated on `NewlyBound=true`). |
| 9 | Implement `TestHandler_NilEngineFastPath` in `pkg/proxy/handler_test.go`: pass `nil` as the 7th arg; send requests; assert no panic, requests succeed via legacy `ResolveInternalConfig`. | Phase 3 (nil-safe signature) | Test passes; `ResolveInternalConfigWithAffinity` returns the legacy tuple unchanged. |
| 10 | Implement `TestExecuteInternalRequest_SelectedCredentialIDInLog` in `pkg/proxy/race_executor_test.go`: capture stderr (via `log.SetOutput(buf)`), run one internal request, assert the augmented log line contains `credential_id=<id>`. | Phase 3 (log augmentation) | Test passes; log line format matches `decisions.md §C "Logging & Observability"` shape. |
| 11 | Implement **`TestMigration028_BackfillSQLite` (AMENDED, M-1 — assert `credential_id` column still present after migration)**, `TestMigration028_EmptyCredential`, `TestMigration028_MultiCredential`, AND **`TestMigration028_PGGated_TransactionCommit` (MANDATORY per M-1-test)** in `pkg/store/database/store_test.go`. Each SQLite test uses a fresh in-memory SQLite DB. The PG test bundles pgx v5 against a real Postgres instance (testcontainers or CI-side) and proves the file-level `BEGIN`/`COMMIT` transaction commits on both dialects with the same-statement derived shadow write. | Phase 1 (migration 028 exists) | All SQLite tests pass; **`TestMigration028_PGGated_TransactionCommit` MANDATORY pre-merge** (gated by `//go:build pg_integration` if no container in CI; manual runbook documented otherwise). |
| 12 | Implement `TestModelsManager_EngineOnModelChanged` (asserts E-2 filter-survivors — bindings whose credential survives are preserved across `UpdateModel`) AND `TestModelsManager_EngineOnCredentialDeleted` in `pkg/store/database/store_test.go`. These rely on `ModelsManager.Engine()` (Phase 1 accessor) and on the engine's binding map state (verified via `Engine.Stats()` per E-4 — preferred over reflection). | Phase 1 (engine accessor), Phase 2 (engine Stats accessor) | Both tests pass; binding map state matches expectations after each operation; **filter-survivors test catches E-2 regression**. |
| 13 | Extend `mockModelsConfig` in `pkg/ultimatemodel/handler_test.go` and `pkg/ultimatemodel/handler_internal_test.go` with `ResolveInternalConfigWithAffinity` (returns the same tuple as `ResolveInternalConfig`). | Phase 3 (interface change) | Existing tests in those files still pass. |
| 14 | Add UI handler tests in `pkg/ui/handlers_models_test.go` for the 5 contract scenarios (empty + internal → 400; mixed-provider → 400; weight=0 → 400; legacy field ignored; valid multi-cred POST → 201). | Phase 4 (validate endpoint changes) | All 5 tests pass. |
| 15 | Extend `test/mock_llm_openai.go` with `--hit-count-file <path>` flag (atomic counter, writes after each request). | None | Mock runs unchanged with no flag; with the flag, the file is updated after each request. |
| 16 | Implement `test/test_mock_credential_lb.sh`: **12 test scenarios** (Tests 1, 2, **2b templated-first-message**, **2c anonymous**, **2d empty-key (W-2 + C2: assert 0 events)**, **2e multimodal (A-2)**, 3 validation, 4 single-cred backward compat, **5 event-bus only-on-first (W-1)**, 6 single-event-per-binding, **7 token-salted affinity (A-1)**, **8 `/v1/messages` affinity (NEW 2026-08-21 — C1, Anthropic-shaped requests through the `/v1/messages` route; same `(tokenID, firstUserMessage)` -> same upstream credential; same conversation pins across both chat-completions AND `/v1/messages`)**). Modeled on `test_mock_ultimate_model.sh` structure (clean_ports, hard_timeout, assert helpers). | Tasks 14, 15 | Script exits 0; each test prints PASS/FAIL; overall summary at the end. **The script MUST be runnable locally without Postgres** (SQLite-backed store). **The full output MUST be pasted into the PR description per Task 22 / #14**. |
| 17 | Optional: write `test/manual_curl_checklist.md` if house pattern uses checklists; otherwise skip. **NEW (A-1)**: checklist should include curl examples for templated-first-message scenarios — same first message with two different `Authorization: Bearer` headers → independent bindings. | Task 16 | File exists OR confirmed skip. |
| 18 | Optional: update `README.md` "Rollout Verification" subsection with the merge-gate commands. | Tasks 1, 5, 6, 8, 11, 16 | `go test -race ./pkg/credentiallb/... ./pkg/proxy/... ./pkg/ultimatemodel/... ./pkg/store/database/... ./pkg/ui/...` plus `./test/test_mock_credential_lb.sh` documented as the merge gate; PG-gated 028 transaction test documented as a separate MANDATORY gate. |
| 19 | **NEW 2026-08-21 — C1 (reviewer pass):** Implement `TestHandler_AnthropicMessagesAffinity` (handler-level integration test for the `/v1/messages` route). Use the DB-backed `setupIntegrationDB` substrate (per #4) because `NewInternalHandler` calls `ModelsManager.ResolveInternalConfigWithAffinity`. Drive an Anthropic-shaped JSON body through `HandleAnthropicMessages` (`cmd/main.go:139`); assert (a) same `(tokenID, firstUserMessage)` pair lands on the same upstream credential across N requests, and (b) N=100 requests with unique first-user messages distribute per weights. The single-credential fast path (`len(Credentials) == 1`) is asserted byte-identical to today (Phase 3 E-3). **C1 grep-guard** runs after the test: `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0. | Tasks 4, 12, 16 | Both affinity and distribution assertions pass on the Anthropic path; C1 grep-guard passes; the test fails if the implementation slips back to `ResolveInternalConfig` in `internal_handler.go:69`. |
| 20 | **NEW 2026-08-21 — #11 (reviewer pass):** Implement `TestHandler_ShutdownOrder` in `pkg/proxy/handler_integration_test.go`. Construct a full proxy stack (DB-backed `setupIntegrationDB` + `credLB` + HTTP server); record call order via wrapped mocks; trigger SIGINT; assert the recorded order is `srv.Shutdown(ctx)` -> `credLB.Stop()` -> `dbStore.Close()` exactly (per Phase 3 #11). | Task 19 (substrate) | Recorded order matches the contract; `credLB.Stop()` runs AFTER `srv.Shutdown(ctx)` returns (no in-flight requests blocked); `dbStore.Close()` runs LAST. |
| 21 | **NEW 2026-08-21 — #4 (reviewer pass):** Provide a test-local `ModelsConfigInterface` helper for engine-integration tests that need a direct `credentiallb.Engine` without the full DB stack. Pattern: construct the engine (`credentiallb.NewEngine(...)`), implement the `ModelsConfigInterface` methods around it (or wrap `models.NewModelsConfig()` and inject the engine via a setter that Phase 2 adds), and use this helper in Tasks 1, 5, 19, 20 where DB setup is overkill. Phase 5 ships BOTH substrates (per test class) — see Task 5 acceptance for the per-test substrate decision. | Phase 2 (engine exists) | Helper compiles; existing tests using `models.NewModelsConfig()` for single-credential fast paths are unaffected; engine-integration tests use the helper and pass. |
| 22 | **NEW 2026-08-21 — #14 (reviewer pass):** The output paste from `test/test_mock_credential_lb.sh` (full PASS/FAIL summary + per-test hit counts) MUST be pasted into the PR description as a Phase 5 exit precondition. Promotion: this is a **merge gate**, not a nicety. Without the paste, the reviewer cannot verify the 11 scenarios passed locally. | Task 16 | PR description contains the script's output verbatim; reviewer checklist references the paste. |

## Coupling

- **Tight with Phase 1 (DB/store)**: Task 11 verifies migration 028; Task 12 verifies engine wiring through `ModelsManager`. If Phase 1's accessor name changes (`Engine()`), Task 12 must follow.
- **Tight with Phase 2 (engine)**: Tasks 1, 2, 3 verify the engine contract. If Phase 2 ships a test seam (e.g., `BindingCountForTest`), Task 12 uses it.
- **Tight with Phase 3 (handler wiring)**: Tasks 5, 6, 7, 8, 9, 10 verify the request-path wiring. They depend on Phase 3's exact log-line format and event name.
- **Loose with Phase 4 (UI/API)**: Task 14 verifies the API surface. Depends on Phase 4's `validateCredentials` returning the documented error strings.
- **Independent** of the existing `pkg/ultimatemodel/hash_cache.go` — Phase 5 does not retest HashMessages (the engine owns its own key per Decision A).

### Coupling Matrix

| | Phase 1 | Phase 2 | Phase 3 | Phase 4 | Phase 5 |
|---|---|---|---|---|---|
| Phase 1 | — | tight | tight | tight | tight (migration tests) |
| Phase 2 | tight | — | tight | independent | tight (engine tests) |
| Phase 3 | tight | tight | — | loose | tight (integration tests) |
| Phase 4 | tight | independent | loose | — | tight (UI handler tests) |
| Phase 5 | tight | tight | tight | tight | — |

## Risks

> **AMENDED 2026-08-21**: risks updated for the new templated-first-message test, A-1 token-salted tests, A-2 multimodal flip, W-1 only-on-first event test, W-2 empty-key test, E-3 binding-map size assertion, E-4 janitor panic-recovery + Stats(), M-1 PG-gated transaction test (MANDATORY).

| # | Risk | Impact | Likelihood | Mitigation |
|---|---|---|---|---|
| 1 | The fixed RNG seed for `TestEngine_Distribution` produces counts outside ±5% tolerance at N=10k on a particular Go version | Low — flaky test | Very Low (seed=42 is well-known stable) | If flaky, document the actual bounds in the test comment; never widen the tolerance beyond ±5% (the engine's correctness depends on it). |
| 2 | `TestHandler_MultiCredentialDistribution` at N=100 produces counts outside the loose ±15% tolerance due to RNG variance | Low — flaky test | Low | Document the looser tolerance for N=100 in the test comment; if flaky in CI, raise N to 200 (still fast) or accept a wider band. The ±5% at N=10k is the canonical guarantee; this test is a smoke test, not the canonical one. |
| 3 | **`TestEngine_Distribution_TemplatedFirstMessage` (NEW, leader-mandated)** at N=10k with rotating tokens produces counts outside ±5% tolerance due to RNG variance | Low — flaky test | Low | Same RNG seed = same tolerance. N=10k is large enough that ±5% is robust. Document tolerance in test comment. If flaky in CI, raise N to 20k (still fast). |
| 4 | The mock upstream on port 4001/4002 is held by another process during `test_mock_credential_lb.sh` (CI parallel test runs collide) | Medium — test fails to start | Medium | Use `clean_ports 4001 4002 4322` from `test/test_mock_clean_ports.sh` (already a house pattern); hard timeout trap at 75s. |
| 5 | The engine's binding map state is not observable from outside the package (no `Engine.Stats()` accessor), so Task 12 cannot run | Medium — gap in invalidation coverage | Low (E-4 mandates `Stats()`) | Phase 2 ships `Engine.Stats() StatsSnapshot` returning per-model hits/misses/bindings per E-4. If Phase 2 doesn't ship it, fall back to indirect verification: trigger `RemoveCredential`, then send a request to a model that referenced the deleted credential, assert the request fails with `failed to resolve internal config` (engine self-corrected). |
| 6 | `TestHandler_SingleCredentialBackwardCompat` produces a wire-bytes snapshot that drifts over time as other features land (e.g., Phase 3's augmented log line, future translator changes) | Low — false-positive failures | Medium | Snapshot is generated ONCE post-Phase-3 and committed as a fixture. The test asserts the request body to the upstream is byte-identical to the snapshot — log lines are excluded by capturing only the body sent upstream, not the request log. |
| 7 | `test/test_mock_credential_lb.sh` is not run in CI (house pattern runs only `go test ./...`, not `test/*.sh`) | Medium — gap in E2E coverage | High (matches existing house pattern; `test_mock_ultimate_model.sh` has the same gap) | Document the script in the README as a "manual smoke test" run before merge. Phase 5 commits a CI hook ONLY if the house pattern is updated to include `test/*.sh` — otherwise do not add a hook. **The 11 scenarios (including templated-first-message) MUST be documented as a manual runbook** — the A-1 regression is invisible to `go test ./...` without these scenarios. |
| 8 | **`TestMigration028_PGGated_TransactionCommit` (NEW, MANDATORY per M-1-test)** cannot run because no Postgres container is available in CI | High — gates Phase 1 exit; without it, migration 028 is not approved | Medium (matches existing pattern — `decisions.md §E` documents this explicitly) | Gate the test behind `//go:build pg_integration`; document a manual runbook in the commit message AND in the README's Rollout Verification section. The test MUST be runnable on demand (testcontainers or local docker) before any Phase 1 PR is merged. **The repo's first file-level transactional migration requires this gate.** |
| 9 | The augmented log line format (`credential_id=%s`) is parsed via a brittle string match in `TestExecuteInternalRequest_SelectedCredentialIDInLog` | Low — test fragility | Low | Parse with a regex (`regexp.MustCompile(\`credential_id=(\S+)\`)`) rather than a string contains; document the regex in a comment. |
| 10 | `pkg/ui/handlers_models_test.go` doesn't currently have a "valid multi-cred POST" test path — adding it requires building the full POST handler context (auth, models config, store), which may not exist as a test fixture yet | Medium — blocker for Task 14 | Low | Inspect the existing tests; if the fixture is missing, add a small `setupUITestServer(t *testing.T) *httptest.Server` helper (mirroring `pkg/proxy/handler_integration_test.go:30-71`). The implementation is mechanical. |
| 11 | **W-1 + C2 (REVISED 2026-08-21 (reviewer pass))** — `model_credential_selected` event test is misnamed/weakly specified (e.g., test asserts "exactly 1 event" but Phase 3's implementation accidentally publishes per-request because the bool heuristic leaked back in) — the test passes but production behavior is wrong | Medium — observability regression invisible to other tests | Low | Test name reflects semantic intent (`OncePerFirstBinding`); the assertion checks `eventCount == 1` for 5 affinity-pinned requests with the SAME `(tokenID, firstUserMessage)` pair. If Phase 3's per-request bool heuristic leaks back, event count becomes 5 and the test fails. **The empty-key variant test (`TestHandler_EngineEventPublish_EmptyKey_NeverFires` — RENAMED per C2) catches the OPPOSITE mistake** — over-aggressive dedup that emits the event on empty-key picks. Per C2, empty-key requests emit ZERO events (engine returns `NewlyBound=false`; no binding was stored). Together, the two tests pin the exact W-2 + W-1 + C-2 semantics. |
| 12 | **A-1** — `TestEngine_Distribution_TemplatedFirstMessage` rotates tokens via a stub token store, but the actual `handler.go:401+` site reads `rc.tokenID` from `auth.AuthToken` — if the test's stub doesn't plumb the tokenID through `rc.tokenID`, the test passes spuriously (no salt, single credential pins) | High — false-positive test, A-1 regression invisible | Low | Test uses the real `pkg/proxy/handler.go:401+` wiring path via `httptest.NewRequest` + `Authorization: Bearer` header → auth.AuthToken → `rc.tokenID`. NOT a stub. Verify with a unit-level assertion: `req.tokenID != ""` before the engine call. |
| 13 | **E-4** — `TestEngine_JanitorPanicRecovery` is flaky: the injected panic occasionally escapes the `defer recover()` if the engine's internal locking discipline changes | Low — test fragility | Low | The `defer recover()` is in the janitor sweep function (one place). Document the panic-recovery contract in `pkg/credentiallb/engine.go` (a comment near the janitor goroutine) so refactors don't accidentally remove it. Phase 5 test asserts at least one model continues to be swept after the panic. |

## Exit Criterion
Phase 5 is complete when:
1. `go test -race ./pkg/credentiallb/... ./pkg/proxy/... ./pkg/ultimatemodel/... ./pkg/store/database/... ./pkg/ui/...` passes with zero failures.
2. `go test -bench=.` (optional, see Risk #4 of Phase 3) shows the engine's `GetOrSelect` hot-path latency is <1µs at N=1000 bindings (informational; not a merge gate).
3. `test/test_mock_credential_lb.sh` exits 0 — **all 12 scenarios pass** (Tests 1, 2, **2b templated-first-message (A-1)**, **2c anonymous (A-1)**, **2d empty-key (W-2 + C2: 0 events)**, **2e multimodal (A-2)**, 3 validation, 4 single-cred backward compat + E-3, 5 event-bus only-on-first (W-1), 6 single-event-per-binding, 7 token-salted affinity (A-1), **8 `/v1/messages` affinity (C1)**). **#14 — NEW 2026-08-21 (reviewer pass):** the script's full output (PASS/FAIL summary + per-test hit counts) is pasted into the PR description as a Phase 5 exit precondition (NOT a nicety — it is a merge gate).
4. The migration tests in `pkg/store/database/store_test.go` confirm migration 028 atomicity (backfill + derived shadow write in one transaction) and that `credential_id` is **STILL PRESENT** in the schema as a derived shadow (M-1 amendment; the column is NOT dropped in 028).
5. **`TestMigration028_PGGated_TransactionCommit` passes against a real Postgres instance** (MANDATORY per M-1-test; gated by `//go:build pg_integration` if no container in CI; documented as a manual runbook otherwise). This gate is load-bearing for Phase 1's exit.
6. Manual smoke: an operator can `go run cmd/main.go` against a fresh DB, attach 2 credentials to a model via the UI (Phase 4), send 100 unique-message requests AND 100 templated-first-message requests with rotating tokens, and observe ~33/67 distribution in both cases by counting SSE events (`model_credential_selected`).
7. Rollout Verification section in the README (if present) lists the merge gate commands, including the PG-gated 028 transaction test.
8. **C2 — REVISED 2026-08-21 (reviewer pass):** `TestHandler_EngineEventPublish_EmptyKey_NeverFires` (renamed from `_FiresPerRequest`) passes: 5 empty-key requests produce ZERO `model_credential_selected` events. The engine returns `NewlyBound=false` because no binding is stored; event publish is gated on `NewlyBound=true`. Together with `TestHandler_EngineEventPublish_OncePerFirstBinding` (5 affinity-pinned requests -> exactly 1 event), the two tests pin the exact W-1 + W-2 + C-2 semantics.
9. **C1 — NEW 2026-08-21 (reviewer pass):** `TestHandler_AnthropicMessagesAffinity` passes (both affinity and distribution scenarios); the C1 grep-guard `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0 after the test. The `/v1/messages` route ships with engine-backed credential resolution.
10. **#4 — NEW 2026-08-21 (reviewer pass):** Engine-integration tests use the documented substrate (DB-backed `setupIntegrationDB` OR test-local `ModelsConfigInterface` impl wrapping a directly-constructed `credentiallb.Engine`); no test reaches into the JSON-backed `*models.ModelsConfig` and expects an engine. The test-local helper from Task 21 compiles and is used by the multi-credential distribution/affinity tests; the single-credential fast-path tests continue to use `models.NewModelsConfig()` (no engine; E-3 byte-identity).
11. **#11 — NEW 2026-08-21 (reviewer pass):** `TestHandler_ShutdownOrder` passes; the recorded order is `srv.Shutdown(ctx)` -> `credLB.Stop()` -> `dbStore.Close()` exactly. Phase 3 #11 contract is enforced end-to-end.
12. **#14 — NEW 2026-08-21 (reviewer pass):** The full output of `test/test_mock_credential_lb.sh` (PASS/FAIL summary + per-test hit counts) is pasted into the PR description. This is a Phase 5 exit precondition — the reviewer cannot verify the 11 scenarios passed without the paste.

---

## Amendment Changelog (2026-08-21 — leader rulings)

| Ruling | Section(s) amended | What changed |
|---|---|---|
| **A-1** | Objective; Context; In Scope #1 (engine tests), #4 (handler tests); Tests `TestEngine_Affinity_TokenSalted`, `TestEngine_Affinity_Anonymous`, `TestHandler_MultiCredentialAffinity_TokenSalted`, `TestHandler_MultiCredentialAffinity_Anonymous`, `TestHandler_MultiCredentialDistribution_TemplatedFirstMessage` (leader-mandated); Tasks #1, #2, #4, #5, #7; Files table (`test_helpers.go` `newAuthTokenPair`); Risks #3, #12 | Affinity tests must use token-salted keys. Two tokens sending the same first message → independent bindings. **NEW (leader-mandated)**: a templated-first-message distribution test that rotates tokens — N distinct tokens sending the SAME first message must distribute across credentials per weights. This catches the A-1 regression at every layer (engine unit, handler integration, E2E script). Anonymous (no token) affinity still holds via unsalted key. |
| **A-2** | Objective; In Scope #1 (`TestEngine_ExtractFirstUserMessage_Multimodal`), #4 (`TestHandler_MultimodalAffinity` flipped from fallback-assertion to affinity-assertion), #7 (E2E Test 2e); Tasks #1, #7; E2E script scenario matrix | Multimodal tests flip from no-affinity fallback assertions TO affinity assertions. Multimodal first message gets and keeps a binding; deterministic canonical JSON (precedent `hash_cache.go:172-186`). |
| **M-1** | In Scope #5 (SQLite migration test assertion flipped); Files table; Exit Criterion #4 | Migration 028 does NOT drop `credential_id` — it stays as a derived shadow written in the same UPDATE statement. SQLite migration tests now assert `PRAGMA table_info(models)` STILL includes `credential_id` after migration. |
| **M-1-test** | In Scope #5 (NEW mandatory test); Files table; Tasks #11; Exit Criterion #5; Risks #8 | **PG-gated 028 transaction test is MANDATORY pre-merge** (upgraded from best-effort). This is the repo's FIRST file-level transactional migration (PG migration 024 uses `BEGIN` only inside PL/pgSQL, NOT at the file level). The test bundles SQLite + pgx v5 against a real Postgres instance and proves the transaction commits on both dialects with the same-statement derived shadow write. Without it, migration 028 is NOT approved. |
| **E-1** | In Scope #1 (`TestEngine_JanitorPanicRecovery`); Risks #13 | Janitor outer RLock + per-model write locks (one at a time) — covered indirectly via race-detector test + the new panic-recovery test. |
| **E-2** | In Scope #1 (`TestEngine_WeightChangeInvalidation` filter-survivors), #5 (`TestModelsManager_EngineOnModelChanged` filter-survivors); Tasks #3, #12 | `OnModelChanged` filter-survivors (preserve bindings whose credential survives; drop only orphans) — NOT clear-all. Phase 5 asserts bindings are preserved across weight nudges. |
| **E-3** | In Scope #1 (`TestEngine_SingleCredentialFastPath`), #4 (`TestHandler_SingleCredentialBackwardCompat` + binding-map size assertion), #7 (E2E Test 4); Tasks #6, #14; Files table | Fast-path test asserts binding-map size == 0 after N single-credential requests. Assertable via `Engine.Stats()` (per E-4) or internal map size. The Phase 5 test-assertion is the regression catcher for the phase2/phase3 contradiction flagged in the architect council. |
| **E-4 (recommended)** | In Scope #1 (`TestEngine_JanitorPanicRecovery`, `TestEngine_Stats`); Files table (engine_test.go scope ~700 lines); Risks #5, #13 | Per-model RNG (`math/rand/v2`); `Engine.Stats()` accessor returning per-model hits/misses/bindings; janitor panic-recovery (`defer recover()` + continue). Phase 5 ships tests for all three. Non-blocking recommendations. |
| **W-1** | In Scope #1 (no engine test change), #4 (handler test name), #7 (E2E Test 5); Tasks #8; Risks #11 | `model_credential_selected` fires exactly once per first binding — driven by the engine's `newlyBound` return, NOT a per-request bool. Test renamed from `OncePerBinding` to `OncePerFirstBinding` to reflect semantic intent. The per-request bool heuristic (`bindingEmitted`) is REMOVED — the engine's `newlyBound` is the sole signal. |
| **W-2 + C2** | In Scope #1 (`TestEngine_EmptyKey_NoBinding`), #4 (`TestHandler_EmptyConversationKey_NoBinding`), #7 (E2E Test 2d); Tasks #1, #5, #8 | Empty-key no-binding test. Two requests with empty `conversationKey` may pick DIFFERENT credentials (fresh weighted pick per request); engine's binding map size for that model stays 0. The event test variant `TestHandler_EngineEventPublish_EmptyKey_NeverFires` (REVISED 2026-08-21 — C2; was `_FiresPerRequest`) asserts that empty-key first picks emit ZERO `model_credential_selected` events (engine returns `NewlyBound=false` because no binding was stored; event publish gated on `NewlyBound=true`). Together, the two tests pin the exact W-1 + W-2 + C-2 semantics. **Contract wins** (C2): prior text implying empty-key first picks emit the event is SUPERSEDED. |
| **E2E scenario matrix** | In Scope #7; Tasks #16 | `test/test_mock_credential_lb.sh` now has 11 scenarios (was 6): affinity, distribution, **templated-first-message distribution (leader-mandated)**, **anonymous distribution**, **empty-key distribution (W-2)**, **multimodal affinity (A-2)**, validation, single-cred backward compat + E-3, event-bus only-on-first (W-1), single-event-per-binding, **token-salted affinity independence (A-1)**. Mock 4001/4002 pattern preserved; auth store extended with two distinct API tokens. |

---

## Round 2 — Reviewer Punch-List (2026-08-21)

> **REVISED 2026-08-21 (reviewer pass):** The five items below are
> the surgical amendments from the reviewer punch-list. Each row ties
> to a specific section above that now carries a
> `> **REVISED 2026-08-21 (reviewer pass — …):**` blockquote. **Contract
> wins**: the pins below are downstream law — Task 5/8/16/19/20/21/22,
> the Files-table rows, the In-Scope test descriptions, the E2E scenario
> matrix, the Exit criteria, and the operator-facing surfaces MUST mirror
> the shapes pinned here.

| # | Item | Section(s) updated | One-line summary |
|---|------|---------------------|------------------|
| **C1** | `/v1/messages` internal path in scope (handler integration test + E2E scenario + grep-guard exit criterion) | In Scope #4 (NEW `TestHandler_AnthropicMessagesAffinity`); Files table (`handler_integration_test.go`); In Scope #7 (E2E Test 8 — `/v1/messages` affinity); Tasks #16, #19 (NEW), #22 (NEW); Exit Criterion #9 (NEW) | Anthropic `/v1/messages` is the PRIMARY client endpoint (Claude Code) and MUST get LB coverage at the handler layer + the E2E layer. The handler integration test uses the DB-backed `setupIntegrationDB` substrate (per #4 — `NewInternalHandler` calls `ModelsManager.ResolveInternalConfigWithAffinity`); the E2E scenario (Test 8) drives Anthropic-shaped requests through `/v1/messages`. Both verify same `(tokenID, firstUserMessage)` → same upstream credential. Grep-guard: `grep -n "ResolveInternalConfig(" pkg/proxy/internal_handler.go` returns 0. |
| **C2** | Empty-key `NewlyBound=false` invariant pinned — empty-key events NEVER fire | In Scope #4 (`TestHandler_EmptyConversationKey_NoBinding` — was "fires for EACH request" → NOW "does NOT fire for any of the 10 requests"); In Scope #11 (renamed `TestHandler_EngineEventPublish_EmptyKey_NeverFires`; was `_FiresPerRequest`); In Scope #7 (E2E Test 2d — added "assert ZERO events emitted"); Tasks #5, #8; Files table (`handler_test.go`); Risks #11; Exit Criterion #8 (NEW) | `conversationKey == ""` ⇒ fresh weighted pick per call, **NO binding stored**, engine returns `NewlyBound = false`. The caller does **NOT** publish `model_credential_selected`. `TestHandler_EngineEventPublish_EmptyKey_NeverFires` asserts 0 events for 5 empty-key requests (was asserting 5 events — REVERSED per C2). **Contract wins**: prior text implying empty-key first picks emit the event is SUPERSEDED across all five sites. |
| **#4** | Test substrate fix — JSON-backed `ModelsConfig` does NOT own an engine; only `ModelsManager` does | In Scope #4 (substrate pin in `TestHandler_AnthropicMessagesAffinity` description); Files table (`handler_integration_test.go` — substrate split documented); Tasks #5, #21 (NEW); Exit Criterion #10 (NEW) | Provide BOTH substrates for engine-integration tests (per test class): (a) DB-backed `setupIntegrationDB` (production-shaped), or (b) test-local `ModelsConfigInterface` implementation wrapping a directly-constructed `credentiallb.Engine`. Single-credential fast-path tests keep using `models.NewModelsConfig()` (no engine needed; E-3 byte-identity). The Anthropic integration test (C1) uses the DB-backed substrate because `NewInternalHandler` only sees a `ModelsManager`. |
| **#11** | Shutdown sequence verification — `srv.Shutdown` → `credLB.Stop` → `dbStore.Close` | Files table (`handler_integration_test.go` — NEW `TestHandler_ShutdownOrder`); Tasks #20 (NEW); Exit Criterion #11 (NEW) | Add `TestHandler_ShutdownOrder` that records call order via wrapped mocks, triggers SIGINT, and asserts the recorded order matches the Phase 3 #11 contract exactly. The integration test is the merge gate that enforces the load-bearing sequence. |
| **#14** | E2E script output paste into PR description is a Phase 5 exit precondition | Exit Criterion #3 (bumped 11 → 12 scenarios; paste into PR description); Task #16 (12 scenarios); Tasks #22 (NEW); Exit Criterion #12 (NEW) | `test/test_mock_credential_lb.sh` runs 12 scenarios (added Test 8 `/v1/messages` affinity per C1). The full PASS/FAIL summary + per-test hit counts is **pasted into the PR description as a merge gate** (NOT a nicety). The reviewer cannot verify the 12 scenarios passed locally without the paste. |
